# 项目任务记忆

> 作者：Monkeycode

### 全站运营语言任务6
- 完成日期：2026-08-28；作者：Monkeycode。
- 任务摘要：重写家长跟进、少儿体育消课与续费、会员权益和真人陪跑服务文案，保持既有接口、路由、权限、公式和内部枚举兼容。
- 涉及文件：`frontend/src/views/growth/SalesCoach.vue`、`frontend/src/views/PrivateAgentHub.vue`、`frontend/src/views/tools/RenewalRateEducation.vue`、`ClassConsumptionRateEducation.vue`、`ClassRateEducation.vue`、`frontend/src/views/Membership.vue`、`frontend/src/components/MembershipOutcomeMatrix.vue`、`frontend/src/views/growth/ServiceCenter.vue`、专项测试、任务清单和项目文档。
- 关键结果：家长状态按新咨询到报名/暂未报名顺序完整展示；体育服务使用出勤、训练表现、教练反馈、剩余课时、续费和老带新语言；系统与真人顾问边界明确且无结果保证。专项及相关回归26/26、Vite构建770 modules通过；扫描剩余82处，均不在任务6范围。
- 限制：浏览器无活动标签页，未完成真实页面快照；未部署、未迁移、未提交；下一步从任务7开始。

## 2026-08-22

### 本地生活知识操作系统实施与验收
- 完成日期：2026-08-22；作者：Monkeycode。
- 任务摘要：将本地生活知识库转化为儿童培训机构经营诊断、行动卡、聚合指标、周复盘和顾问升级的内部知识操作系统；不复制源库、不执行源库脚本、不部署、不做线上迁移。
- 涉及目录或文件：`backend/src/data/`、`backend/src/services/`、`backend/src/routes/`、`backend/src/models/`、`backend/scripts/`、`frontend/src/api/growth.js`、`frontend/src/views/growth/`、`README.md`、`.monkeycode/specs/local-life-knowledge-os/`。
- 关键结果：知识治理、结构化检索、统一 AI Runner、获客诊断、聚合指标、周复盘、顾问告警、API 与前端闭环完成；客户侧不暴露第三方原文、CRM 或个人明细。
- 直接验证：`test:knowledge-os` 10/10、`test:local-life-acceptance` 4/4、`test:growth-platform` 5/5、`test:advisor-events` 2/2、`test:deep-runner` 6/6、`test:douyin-diagnosis` 3/3、媒体/渠道适配器 5/5、`test:quick-plan` 全部通过；`smoke:growth-platform` 通过；前端构建 769 modules 通过；关键后端文件 `node --check` 通过。
- 限制：测试环境 MySQL 不可用时使用 Mock DB；未部署、未执行线上迁移。

### 平台基础能力第1.1项：机构成员服务
- 完成日期：2026-08-22；作者：Monkeycode。
- 涉及目录或文件：`backend/src/services/organization.js`、`backend/src/models/mockDb.js`、`backend/src/services/productEvents.js`、`backend/scripts/organization-permissions.test.js`、`.monkeycode/specs/platform-foundation/tasklist.md`、`README.md`。
- 关键结果：新增机构角色能力矩阵、成员列表/新增/角色变更/撤销、默认机构切换服务；成员操作通过服务端机构成员关系校验；Mock DB 同步支持新增成员查询与更新语义；专项测试2/2通过。
- 关键进展：新增统一机构权限中间件，支持按权限能力和角色限制路由，并先验证机构成员关系；中间件专项测试2/2通过。
- 关键进展：已补齐机构成员、默认机构和网点 CRUD API，并在路由层绑定成员/网点权限；机构网点专项测试2/2通过。
- 关键进展：已补齐机构数据库与 Mock DB 结构，新增成员/默认机构/网点索引与机构权限审计日志，并记录成员变更和默认机构切换；机构结构回归7/7通过。
- 关键进展：已将机构角色权限接入获客资源、文件存储、异步任务和服务交付路由；文件管理、任务管理及服务读写均执行服务端机构成员与角色校验；专项回归7/7通过。
- 关键进展：新增角色允许/拒绝矩阵、成员生命周期、默认机构切换、网点隔离、顾问分配和跨机构访问测试；权限矩阵专项测试3/3通过，累计机构权限回归10/10通过。
- 阶段检查点：知识治理10/10、本地生活4/4、增长平台5/5、顾问事件2/2、Quick Plan全套、深度测试6/6、抖音诊断3/3，前端生产构建通过。
- 新阶段进展：完成文件存储2.1，新增本地文件存储适配器，使用 `PRIVATE_STORAGE_DIR` 配置根目录，服务端生成机构隔离存储键，并拒绝绝对路径与目录穿越；适配器及既有文件链路6/6通过。
- 新阶段进展：完成文件存储2.2，增加 MIME 白名单、扩展名匹配、100MB 大小限制、声明大小与实际内容一致性校验、SHA-256 哈希和路径安全校验；专项文件测试2/2、既有文件链路5/5通过。
- 新阶段进展：完成文件存储2.3，签名读取加入机构范围，校验资产归属、ready状态、保留期和物理文件存在性；补齐临时资产、过期清理和删除幂等语义；文件生命周期专项3/3、既有文件链路5/5通过。
- 新阶段进展：完成文件存储2.4，新增机构隔离资产列表、单资产状态查询和非法状态拒绝；文件创建、上传、签名读取和删除继续绑定机构文件权限；文件专项回归9/9通过。
- 新阶段进展：完成文件存储2.5，新增按机构隔离的过期资产扫描、物理文件清理和 pending 临时资产回收任务；清理任务接入同机异步 Worker，并按机构与日期幂等；清理任务及文件专项回归通过。
- 新阶段进展：完成文件存储2.6，补齐适配器契约、非法 MIME/扩展名/大小、哈希、跨机构签名、过期读取、路径穿越、物理文件缺失和上传失败回收测试；文件安全验收矩阵12/12通过。
- 文件存储阶段检查点 2.7 已通过：文件存储专项及既有增长平台回归 13/13 通过，前端生产构建 769 modules 通过。
- 异步任务可靠性 3.1 已完成：MySQL 与 Mock DB 补齐 `locked_by`、`locked_at`、`lease_expires_at`、`next_run_at` 和 `result_idempotency_key` 字段及查询索引；字段专项及既有任务回归 7/7 通过，关键模型语法检查通过。
- 异步任务可靠性 3.2 已完成：任务领取改为数据库条件更新式原子 claim，仅允许 queued 到期任务或 processing 租约过期任务被领取；新增 Worker 锁定标识、租约清理和完成状态条件更新，防止多 Worker 重复执行。专项及兼容回归 9/9 通过。
- 异步任务可靠性 3.3 已完成：增加 `cancel_token` 与 `cancel_requested_at`，取消操作使用机构范围和状态条件更新；Worker 完成/失败写入必须仍持有 processing 锁定，取消后不会覆盖为 succeeded；handler 获得取消检查和 0–99 受控进度回调。专项及兼容回归 11/11 通过。
- 异步任务可靠性 3.4 已完成：可重试失败进入 `failed_retryable` 并按 1/2/4 秒指数退避，通过 `next_run_at` 调度；达到 3 次尝试后进入 `failed_final`；错误记录 `transient`/`permanent` 分类；数据库幂等键冲突时重新读取已有任务。专项及兼容回归 13/13 通过。
- 异步任务可靠性 3.5 已完成：任务默认生成 `result_idempotency_key`，handler 可通过 `writeResult` 受控写入业务结果；相同结果幂等键重复写入只保留首次结果，冲突键拒绝覆盖；任务完成使用已有结果保护，避免重试覆盖业务结果。专项及兼容回归 15/15 通过。
- 异步任务可靠性 3.6 已完成：新增 `backend/src/workers/asyncTaskWorker.js` 独立 Worker 入口，注册文件清理和媒体任务处理器；API 进程不再启动任务轮询，新增 `npm run worker`；支持启动失败诊断、SIGTERM/SIGINT 停止和关闭定时器。Worker 自然退出验证及异步任务/增长平台回归 15/15 通过。
- 异步任务可靠性 3.7 已完成：新增任务提交入口，仅允许已注册处理器类型；查询使用 `resources:read`，提交/取消/重试使用 `tasks:manage`，机构范围由服务端权限中间件确定。白名单与异步任务/增长平台回归 16/16 通过。
- 异步任务可靠性 3.8 已完成：新增 `test:async-reliability` 专项矩阵，覆盖多 Worker claim、租约恢复、取消竞态、进度边界、指数退避、失败分类、结果幂等、白名单提交和 Worker 自然退出；专项矩阵 11/11 通过，文件清理/增长平台兼容回归 6/6 通过。
- 异步任务阶段检查点 3.9 已通过：可靠性专项 11/11、文件清理与增长平台兼容 6/6、媒体/渠道适配器和机构 API/权限回归 9/9，累计 26/26 通过；API 进程与独立 Worker 关键文件语法检查通过。异步任务可靠性与独立 Worker 阶段完成。
- 三阶段集成验收 4.1 已通过：异步专项 11/11、机构/存储/增长/媒体/渠道回归 30/30、Quick Plan 全套 49/49，累计 90/90 通过；未部署、未执行线上迁移。
- 平台基础能力 4.2 已通过：全部 `backend/src/**/*.js` 语法检查通过；Worker 自然退出和 Quick Plan API 进程生命周期测试 2/2 通过。
- 平台基础能力 4.3 已通过：前端 `npm run build` 成功，Vite 转换 769 modules，机构、文件和增长页面无构建回归。
- 平台基础能力最终验收记录：机构权限、服务器文件存储、异步任务可靠性与独立 Worker 已完成；集成回归 90/90、后端源文件语法检查通过、前端生产构建 769 modules 通过。验证环境 MySQL 不可用时使用 Mock DB。最终检查点 4.5 已通过：平台基础能力任务清单全部完成，所有设计验收项均有自动化测试或构建/语法证据；未部署、未执行线上迁移。
- 限制：音视频时长校验尚未实施；不部署线上。

## 2026-08-23

### 发布前代码审计与模拟验收
- 完成日期：2026-08-23；作者：Monkeycode。
- 任务摘要：对当前未提交变更执行高强度代码审计、后端全量测试、真实运行时增长冒烟和前端生产构建；未部署、未迁移线上数据。
- 关键结果：代码审查 0 findings；`node --test scripts/*.test.js` 113/113 通过；`npm run smoke:growth-platform` 通过（organizationId=1、score=85、tasks=1、touchpoints=7、alert=closed、migratedPlans=1）；前端 `npm run build` 通过，769 modules transformed。
- 依赖审计：backend 6项、frontend 7项依赖风险，包含高危 Axios/form-data 等；未执行 `npm audit fix --force`。
- 限制：MySQL 不可用，测试使用 Mock DB；真实 MySQL、线上部署、外部 ASR/OCR 和生产基础设施未验证。

### 核心功能业务闭环测试
- 完成日期：2026-08-23；作者：Monkeycode。
- 任务摘要：使用匿名少儿篮球机构样本验证经营输入、分层知识、诊断、行动计划、指标、周复盘和顾问升级完整链路。
- 涉及文件：`backend/scripts/core-business-loop.test.js`、`backend/src/services/aiRunner.js`、`backend/package.json`、`.monkeycode/docs/核心功能业务闭环测试报告-2026-08-23.md`、`README.md`。
- 关键结果：命中6条获客、体验课和投流知识；诊断主问题为“到店未形成正价成交”，生成2项7天行动并触发人工顾问；专项1/1、全量114/114通过。
- 修复：补齐 `childName` 脱敏；Runner知识审计改用 `snippetCount`。
- 结论：后端业务链条件通过；老板端缺少真实资料输入和一键行动/复盘，未配置LLM时仅返回 `needs_review`，暂不具备客户直接交付闭环。

### 儿童培训7天业务闭环实施完成
- 完成日期：2026-08-23；作者：Monkeycode。
- 任务摘要：使用四类匿名聚合样本完成“获客→预约/到店→体验课→正价成交”的结构化7天闭环，不使用真实追光小牛数据。
- 涉及文件：`backend/src/services/acquisition/businessLoop.js`、`backend/src/services/acquisition/index.js`、`backend/scripts/fixtures/child-training-loop-fixtures.js`、`backend/scripts/business-loop-protocol.test.js`、`frontend/src/views/growth/AcquisitionEngine.vue`、`.monkeycode/specs/child-training-7day-loop/tasklist.md`、`.monkeycode/docs/核心功能业务闭环测试报告-2026-08-23.md`、`README.md`。
- 关键结果：匿名样本专项4/4、后端全量117/117、前端Vite生产构建通过；老板端已提供问题选择、聚合指标表单、诊断卡、一键7天行动计划、阶段快照和四态复盘；行动计划继承诊断动作、验证指标和停止条件。
- 限制：无LLM时状态保持`needs_review`；测试使用Mock DB；未接真实机构数据、OCR/语音/CRM/支付/真实供应商；未部署、未迁移、未提交。

### 全站运营语言体系设计与实施计划
- 完成日期：2026-08-23；作者：Monkeycode。
- 任务摘要：完成全站程序员口吻盘点，确认以机构老板/校长为首要用户、采用实战陪跑顾问口吻，并将客户全链路及内部技术后台纳入运营语言治理。
- 涉及文件：`docs/superpowers/specs/2026-08-23-sitewide-operations-copy-design.md`、`.monkeycode/specs/sitewide-operations-copy/tasklist.md`、`README.md`。
- 关键结果：形成按经营流程重构的语言标准，覆盖导航、首页、诊断、抖音招生、家长跟进、续费、会员、错误状态、后台角色和技术信息；实施计划共11个阶段，用户确认无需补充并要求执行全部测试任务。
- 边界：餐饮、美业入口仅从首阶段客户前台隐藏，不删除代码、接口或历史数据；本阶段只完成设计与计划，未开始全站代码改造，未部署、未迁移、未提交。

### 全站运营语言基础设施任务1
- 完成日期：2026-08-23；作者：Monkeycode。
- 任务摘要：建立统一儿童培训经营词典、运营状态与错误翻译、AI服务边界文案和客户侧技术敏感词扫描。
- 涉及文件：`frontend/src/constants/operationsLanguage.js`、`frontend/src/api/request.js`、`frontend/scripts/operations-copy-scan.js`、`frontend/scripts/operations-language.test.js`、`frontend/package.json`、`.monkeycode/specs/sitewide-operations-copy/tasklist.md`。
- 关键结果：新增客户侧错误脱敏和动作化提示；新增状态、指标及AI边界测试5/5通过；前端生产构建通过（770 modules）。
- 限制：全站仍有待后续阶段治理的既有页面文案；未部署、未迁移、未提交。

### 全站运营语言任务2
- 完成日期：2026-08-23；作者：Monkeycode。
- 任务摘要：收口客户导航、路由标题和首页经营入口，首阶段前台聚焦儿童素质培训。
- 涉及文件：`frontend/src/components/NavBar.vue`、`frontend/src/router/index.js`、`frontend/src/views/Home.vue`、`frontend/src/components/BattlePlanPreview.vue`、`frontend/src/constants/toolCatalog.js`、`frontend/scripts/navigation-operations-language.test.js`、`frontend/package.json`。
- 关键结果：客户导航采用校长经营语言；首页围绕咨询、预约体验、到店、报名、续费及7天行动组织；`visibleIndustryEntries`仅展示`education`，其他行业代码和路由保留；导航测试3/3、基础运营语言测试5/5、前端构建通过。
- 限制：诊断、专项页面和后台仍有旧技术文案，后续任务继续治理；未部署、未迁移、未提交。

### 全站运营语言任务3
- 完成日期：2026-08-23；作者：Monkeycode。
- 任务摘要：重写公共工具目录、经营表格、工具详情、服务方案及公共状态文案。
- 涉及文件：`frontend/src/constants/toolCatalog.js`、`frontend/src/components/SheetTemplate.vue`、`frontend/src/components/ToolDetail.vue`、`frontend/src/components/MembershipOutcomeMatrix.vue`、`frontend/scripts/shared-operations-copy.test.js`、`frontend/package.json`。
- 关键结果：公共目录不再用智能体、生成器、分析器命名；教培CAC/LTV/ROI、人效和场地利用率增加中文口径；按钮、空状态、错误提示和结果区改为具体经营动作；专项测试4/4、基础测试5/5、导航测试3/3、前端构建770 modules通过。
- 扫描边界：全站非严格扫描仍有103处待治理内容，集中在后续诊断、抖音、小红书、家长跟进、后台及后端内部技术文件；未放宽扫描规则。
- 限制：未部署、未迁移、未提交。

### 全站运营语言任务4
- 完成日期：2026-08-23；作者：Monkeycode。
- 任务摘要：重写校长经营工作台、招生闭环、经营体检入口、五步问卷、经营建议报告及AI服务边界。
- 涉及文件：`frontend/src/views/growth/OwnerWorkbench.vue`、`AcquisitionEngine.vue`、`frontend/src/views/Diagnosis.vue`、`DiagnosisQuestionnaire.vue`、`DiagnosisReport.vue`、`frontend/scripts/core-diagnosis-operations-copy.test.js`、`frontend/package.json`、任务清单和项目文档。
- 关键结果：核心链路改为了解校区和主推课程、填写本周招生数据、找主要问题、确定最多两个优先动作、七天后复盘；报告固定展示主要问题、判断依据、可能原因、本周先做、暂时不要做、观察指标、停止或求助条件；数据不足显示需要顾问确认。
- 验证结果：核心专项5/5、公共组件4/4、基础语言5/5、导航3/3、前端构建770 modules通过；扫描从103处降至92处。
- 页面检查：本地Vite服务正常启动；浏览器扩展未连接，无法完成浏览器快照检查，已关闭临时服务并清理日志。
- 限制：剩余项集中在任务5—8的抖音、小红书、家长跟进、后台和后端内部技术内容；未部署、未迁移、未提交。

### 全站运营语言任务5
- 完成日期：2026-08-28；作者：Monkeycode。
- 任务摘要：重写抖音招生工作台、抖音经营判断、15天行动安排、视频数据复盘及后端用户可见结果模板。
- 涉及文件：`frontend/src/views/DouyinAgentHub.vue`、`frontend/src/views/douyin/DiagnosisAgent.vue`、`QuickPlanAgent.vue`、`VideoDiagnoserAgent.vue`、`frontend/scripts/douyin-operations-copy.test.js`、`backend/src/routes/douyinAgents.js`、`backend/src/services/douyin/quickPlanGenerator.js`、`quickPlanTemplates.js`、`quickPlanStrategies.js`、任务清单和项目文档。
- 关键结果：前台围绕主推课程、家长问题、拍摄发布、咨询、到店与报名组织；诊断最多两个动作；15天安排明确动作、负责人、时间、完成标准和结果登记；复盘公开翻译播放、私信、咨询、到店与报名指标；后端新增系统规则、模型生成、模型补充和规则兜底来源元数据，`advisorConfirmed` 默认 `false`。
- 验证结果：抖音前端专项5/5、前端相关回归17/17、Quick Plan 49/49、抖音诊断3/3、路由语法与前端构建770 modules通过；扫描从92处降至85处。
- 限制：浏览器扩展未连接，本轮未执行真实浏览器页面检查；未部署、未迁移、未提交；任务6—8未开始。


### ȫվ��Ӫ��������7
- ������ڣ�2026-08-28�����ߣ�Monkeycode��
- ����ժҪ�������ڲ���ְ̨�����ԡ�ƽ̨��ɫ�߽硢�������÷��ʺ���־/����������Ϣ��¶��
- �漰Ŀ¼���ļ���`backend/src/routes/admin.js`��`backend/src/middleware/logger.js`��`backend/src/services/advisor.js`��`backend/scripts/admin-role-redaction.test.js`��`frontend/src/views/Admin.vue`�������嵥����Ŀ�ĵ���
- �ؼ������ƽ̨��̨����ɷ���˽�ɫ��������������������־����������Ա/ƽ̨����Ա�ɶ�����־�ݹ��Ƴ��ֻ��š�Token����Կ���������롢�������ƾ�ݣ�������Կֻ�������֣�error.log���ݾ���־����ר��4/4����������ع�3/3��Vite����770 modulesͨ����
- ���ƣ�ɨ��ʣ��79������Ҫ��������8��������β������������޻��ǩҳ��δ����ʵҳ����գ�δ����δǨ�ơ�δ�ύ������8δ������
