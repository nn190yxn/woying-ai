# 需求实施计划

> 作者：Monkeycode  
> 2026-08-22 状态：MVP实施清单已全部完成；Mock DB验收、媒体/渠道/顾问测试、增长烟测、关键旧版测试、后端语法检查和前端生产构建均通过。真实ASR/OCR仍按环境变量接入，未配置时使用明确人工降级。

- [x] 1. 建立机构、角色与资源权限基础
  - [x] 1.1 创建机构和成员关系数据模型；在正式数据库迁移中增加 `organizations`、`organization_members`、`institution_locations`，保留一用户多机构能力并为MVP提供默认机构。（设计规格 5.1、13）；为机构状态、成员角色和唯一关系定义数据库约束及索引。
  - [x] 1.2 实现机构仓储与业务服务；新增机构创建、查询、更新、默认机构选择和成员关系读取逻辑。（设计规格 4.2、5.1）；保持现有 `users` 登录身份不变，将行业、城市等业务画像逐步转移到机构域。
  - [x] 1.3 实现机构级授权策略；新增机构成员校验、顾问分配校验和平台管理员校验中间件。（设计规格 9.1）；移除新增模块对 `annual` 等同管理员的依赖，会员权益和管理身份分别判断。
  - [x] 1.4 提供机构API与前端状态；新增机构建档、当前机构读取和切换接口，并创建 `stores/organization.js`。（设计规格 3、4.1）；MVP界面默认进入单一机构，不暴露不必要的复杂组织管理入口。
  - [x] 1.5 编写机构隔离和角色权限测试；覆盖老板、校长、市场负责人、顾问、平台管理员的允许与拒绝路径。（设计规格 9.1、11）
  - [x] 1.6 编写属性测试：机构资源隔离；属性P1：任意两个不同机构及资源ID组合，未授权用户均不能读取或修改其他机构资源。（设计规格 9.1）

- [x] 2. 建立私有文件与异步任务基础设施
  - [x] 2.1 创建通用文件资产模型与私有存储适配器；增加文件元数据、存储键、哈希、保留期限和状态字段，禁止在数据库保存音频二进制。（设计规格 5.3、9.2）；定义私有上传、短时签名读取、提前删除和到期清理接口。
  - [x] 2.2 创建异步任务模型和任务执行器；增加 `async_tasks`，实现幂等键、任务状态、进度、重试次数、错误码和资源关联。（设计规格 5.5、9.3）；MVP采用数据库任务表和独立Node worker，保留后续切换Redis队列的适配边界。
  - [x] 2.3 实现任务查询、重试和取消API；前端可查询 `queued`、`processing`、`succeeded`、`failed_retryable`、`failed_final`、`needs_review`、`cancelled` 状态。（设计规格 9.3）；只允许可重试失败重新执行，避免重复创建业务结果。
  - [x] 2.4 实现文件安全校验和清理策略；校验MIME、扩展名、大小、音频时长与哈希，阻止不支持文件进入转写流程。（设计规格 6.2、9.2）；实现原始录音默认90天到期和用户主动删除入口。
  - [x] 2.5 编写文件和异步任务集成测试；覆盖私有访问、签名URL、幂等提交、部分成功、重试和删除链路。（设计规格 9、11）
  - [x] 2.6 编写属性测试：任务幂等性；属性P2：同一机构、资源和幂等键被任意次数提交，只产生一个有效任务和一份最终业务结果。（设计规格 5.5、9.3）

- [x] 3. 检查点：完成平台基础能力
  - [x] 3.1 已完成基础初始化、机构默认加载和 Mock 兼容烟测；完整测试仍需继续。,如有疑问请询问用户；验证机构隔离、私有存储、异步任务和现有登录流程可共同运行。

- [x] 4. 实现AI、知识库与Skill配置化运行层
  - [x] 4.1 定义Skill清单、评分规则和输出Schema；创建Skill注册格式，包含 `skill_code`、版本、输入输出Schema、评分维度、知识范围、品牌配置和降级策略。（设计规格 7）；将新签和续费Skill整理为运行时配置，保留追光小牛资料作为方法论来源但不形成外部路径依赖。
  - [x] 4.2 实现Skill注册器和版本解析；按产品域、渠道、场景和版本加载启用Skill，拒绝未知或禁用Skill。（设计规格 7）；将Skill版本写入每次AI运行和销售评分结果。
  - [x] 4.3 实现分层知识库检索；按儿童素质培训行业、获客/销售产品域、抖音/小红书渠道、销售场景和Skill版本过滤检索。（设计规格 7）；兼容现有 `kbService.js`，避免全量Markdown无差别注入。
  - [x] 4.4 实现统一AI任务运行器；编排Skill、品牌配置、脱敏上下文、知识检索、模型调用、JSON Schema校验和 `ai_runs` 落库。（设计规格 5.5、7）；区分可安全降级、可重试和必须人工复核，禁止伪造成功结果。
  - [x] 4.5 实现AI敏感信息过滤和审计摘要；在发送给模型前过滤儿童姓名、手机号等无关个人信息，普通日志只记录脱敏运行摘要。（设计规格 9.2）
  - [x] 4.6 编写Skill和结构化输出测试；覆盖版本选择、知识范围、Schema失败、备用模型和人工复核路径。（设计规格 7、9.3）
  - [x] 4.7 编写属性测试：结构化输出约束；属性P3：任意成功状态的AI运行都必须满足对应Skill输出Schema，并保存确切Skill版本。（设计规格 7）

- [x] 5. 实现AI获客引擎后端闭环
  - [x] 5.1 创建获客领域数据库迁移和仓储；增加 `acquisition_projects`、`acquisition_channels`、`acquisition_diagnoses`、`acquisition_action_plans`、`acquisition_snapshots`、`acquisition_assets`。（设计规格 5.2）；项目支持长期运行，快照只保存汇总指标而不保存客户明细。
  - [x] 5.2 实现获客项目和渠道API；提供项目创建、更新、归档、渠道配置和历史查询接口。（设计规格 3.2、6.1）；所有读写应用机构授权策略。
  - [x] 5.3 抽取并适配现有抖音诊断能力；将 `douyinAgents.js` 中诊断、快速计划和复盘能力封装为获客域服务，保留旧接口兼容期但不继续堆叠业务。（设计规格 3.2、4.2）；将15天计划迁移为项目下可选行动模板，而非唯一计划实体。
  - [x] 5.4 抽取并适配现有小红书能力；将 `xhsAgents.js` 的账号、内容和增长建议接入统一获客项目、诊断和成果模型。（设计规格 3.2、6.1）
  - [x] 5.5 实现平台截图和少量指标诊断；接收截图或汇总指标，异步完成OCR/视觉解析、输入标准化和AI诊断。（设计规格 3.2、6.1）；输出明确区分用户输入、系统计算、AI判断、引用依据、建议动作和人工确认事项。
  - [x] 5.6 实现获客行动计划和阶段复盘；根据诊断生成策略、执行动作、素材需求、风险提醒和可选15天模板。（设计规格 3.2、6.1）；保存阶段快照，生成下一阶段建议并写入我的成果。
  - [x] 5.7 编写获客引擎单元和集成测试；覆盖抖音、小红书、截图、计划、复盘、历史查询及旧数据迁移路径。（设计规格 10、11）
  - [x] 5.8 编写属性测试：不保存客户明细；属性P4：任意获客快照输入经标准化后，仅保存允许的汇总指标，不持久化姓名、电话或逐条客资。（设计规格 2.2、5.2）

- [x] 6. 实现老板端设计系统与AI获客引擎界面
  - [x] 6.1 实现温暖专业设计系统；将 `ui-design.md` 中的墨绿、米白、暖金色彩、字体、圆角、间距、阴影和状态定义为前端设计令牌与基础组件。；实现 `AppSidebar`、按钮、卡片、标签、异步状态、空状态、错误状态和需要复核状态，确保键盘焦点、WCAG AA对比度和44px触控目标。（UI设计规格 2—4、10—12）
  - [x] 6.2 创建老板工作台和一级导航；增加AI获客引擎、AI销售教练、我的成果、陪跑服务和机构设置独立入口。（设计规格 3）；首页展示当前问题、上次进度、AI发现和下一步建议，不展示CRM漏斗。
  - [x] 6.3 创建获客引擎独立首页；展示独立价值说明、获客目标入口、最近项目和结果报告。（设计规格 3.2）；支持抖音、小红书、直播、本地推、市场活动和知识问答的目标式入口。
  - [x] 6.4 创建获客项目、诊断和计划页面；实现机构背景、渠道选择、截图/指标输入、异步进度、诊断报告和行动计划界面。（设计规格 6.1）
  - [x] 6.5 创建获客历史与阶段复盘页面；展示历次诊断、方案、截图分析、阶段快照和下一阶段建议。（设计规格 3.4、6.1）
  - [x] 6.6 完成响应式Web和PWA基础适配；确保老板可在桌面和平板完成诊断、查看方案和上传截图。（设计规格 4、10）
  - [x] 6.7 编写设计系统、获客组件和路由测试；覆盖独立入口、异步状态、错误恢复、权限拒绝和响应式关键断点。（设计规格 3、9、11）

- [x] 7. 检查点：完成AI获客引擎MVP
  - [x] 7.1 确保所有测试通过,如有疑问请询问用户；验证抖音、小红书、截图诊断、行动计划、阶段复盘和老板端主流程。

- [x] 8. 实现AI销售教练后端闭环
  - [x] 8.1 创建销售录音和分析数据模型；增加 `sales_recordings`、`transcription_jobs`、`sales_analyses`、`sales_evidence`、`training_tasks`、`sales_score_snapshots`。（设计规格 5.3）；保存录音授权状态、场景、分析版本、原话证据和纵向快照。
  - [x] 8.2 实现录音上传与授权确认API；支持老板上传、文件校验、私有存储、删除和分析任务提交。（设计规格 6.2、9.2）；销售人员使用姓名或匿名编号归属，不要求注册员工账号。
  - [x] 8.3 实现ASR转写适配器；支持异步提交、任务查询、分段文本、说话人片段、失败重试和手工文本降级。（设计规格 6.2、9.3）；转写成功后可独立重跑评分，无需重新上传音频。
  - [x] 8.4 实现场景识别与人工确认；MVP识别新签实操和续费沟通，返回置信度；低置信度结果进入人工确认。（设计规格 3.3、6.2、13）
  - [x] 8.5 实现新签实操结构化评分；使用配置化Skill输出总分、维度分、原话证据、成交风险、替换话术和训练任务。（设计规格 3.3、7）
  - [x] 8.6 实现续费沟通结构化评分；使用配置化Skill检查成果回顾、新需求、进阶价值、断档风险、异议和推进动作。（设计规格 3.3、7）
  - [x] 8.7 实现原话证据和训练任务服务；将证据关联到时间戳、维度和严重程度，保存可直接使用的替换话术。（设计规格 3.3、5.3）；从分析结果生成下次训练任务和完成状态。
  - [x] 8.8 实现历史评分与纵向比较；按机构、销售对象、场景和评分版本生成趋势，避免不同评分版本被直接错误比较。（设计规格 3.3、5.3）
  - [x] 8.9 编写销售教练单元和集成测试；覆盖上传、转写、场景确认、新签、续费、证据、训练任务、纵向比较和删除。（设计规格 10、11）
  - [x] 8.10 编写属性测试：报告证据完整性；属性P5：任意成功销售分析的扣分或风险结论，都必须关联原话证据或明确标记为无可定位证据。（设计规格 3.3、6.2）
  - [x] 8.11 编写属性测试：录音删除策略；属性P6：任意用户提前删除或到期清理后，原音不可再访问；转写和报告按配置保留且不暴露原存储地址。（设计规格 9.2、13）

- [x] 9. 实现老板端AI销售教练和成果界面
  - [x] 9.1 创建销售教练独立首页；展示功能价值、上传入口、最近报告和能力变化，不依赖员工工作台。（设计规格 3.3）
  - [x] 9.2 创建录音上传和任务进度页面；实现授权确认、场景选择、文件上传、转写/评分状态、失败重试和删除。（设计规格 6.2、9.2、9.3）
  - [x] 9.3 创建销售分析报告页面；按已确认的“左侧评分摘要＋右侧证据正文”结构展示总分、维度、原话时间戳、主要问题、成交风险、替换话术和训练任务。（设计规格 3.3；UI设计规格 7.3）
  - [x] 9.4 创建历史趋势和成果页面；按销售对象与场景展示前后评分变化、训练任务和Skill版本。（设计规格 3.4、5.3）
  - [x] 9.5 优化平板端音频上传与报告阅读；桌面使用230px左侧栏，平板横屏压缩为图标栏，竖屏转单列；保持响应式Web/PWA，不建设独立员工应用。（设计规格 2.1、4、10；UI设计规格 4、7）
  - [x] 9.6 编写销售教练前端测试；覆盖授权确认、上传、异步状态、低置信度场景选择、报告证据跳转和删除。（设计规格 9、11）

- [x] 10. 检查点：完成AI销售教练MVP
  - [x] 10.1 确保所有测试通过,如有疑问请询问用户；验证新签和续费从上传到报告、训练任务、历史趋势的完整链路。

- [x] 11. 实现陪跑服务和顾问最小后台
  - [x] 11.1 创建服务交付数据模型；增加 `service_programs`、`service_touchpoints`、`service_alerts`、`consultant_notes`。（设计规格 5.4、8）；支持30天获客冲刺、90天标准陪跑和创始人增长顾问计划。
  - [x] 11.2 实现服务节点生成规则；30天冲刺生成启动、第15天和第30天节点；90天陪跑生成启动、第30/60/90天节点。（设计规格 2.3、5.4）；创始人顾问计划按每两周配置生成沟通节点。
  - [x] 11.3 实现异常触发规则；根据执行中断、截图异常、销售连续低分、共性短板和服务到期生成 `service_alerts`。（设计规格 8.1）；规则输出问题、影响、AI可解决部分和建议服务，不进行高频弹窗。
  - [x] 11.4 实现服务推荐和咨询入口；在获客报告、销售报告和陪跑服务页展示上下文相关推荐，并携带诊断摘要进入咨询。（设计规格 8.2）
  - [x] 11.5 实现顾问后台最小API和页面；提供被分配机构、获客摘要、销售重点报告、服务节点、异常列表、顾问记录和阶段总结。（设计规格 8.3）；顾问只能访问被分配机构，平台管理员操作进入审计日志。
  - [x] 11.6 编写服务节点、异常和顾问权限测试；覆盖三类服务节点、异常去重、上下文推荐和越权拒绝。（设计规格 8、9、11）
  - [x] 11.7 编写属性测试：服务节点确定性；属性P7：任意服务开始日期与服务类型生成的正式沟通节点数量和相对日期均符合配置且无重复。（设计规格 5.4、8）

- [x] 12. 完成成果聚合、兼容迁移与产品收口
  - [x] 12.1 实现“我的成果”聚合API和页面；聚合获客诊断、行动计划、截图分析、销售报告、训练任务、评分趋势和顾问反馈。（设计规格 3.4）
  - [x] 12.2 实现现有抖音计划与复盘的兼容迁移；将 `douyin_quick_plans` 和 `douyin_review_records` 适配或迁移到机构获客项目，保留版本和来源标记。（设计规格 4、5.2、10）；迁移过程中不改变旧数据，不将15天重新设为产品主周期。
  - [x] 12.3 调整一级导航和旧功能入口；将AI获客引擎和AI销售教练设为独立一级产品入口，隐藏或降级与第一阶段边界冲突的泛行业、私域和CRM式入口。（设计规格 2、3）
  - [x] 12.4 增加产品事件和运行质量记录；记录机构建档、首次诊断、录音分析、报告查看、服务推荐和咨询提交事件。（设计规格 11）；不在普通分析事件中写入音频、完整转写或个人敏感信息。
  - [x] 12.5 编写迁移和成果聚合集成测试；覆盖旧计划迁移、成果排序、机构隔离、版本标记和重复执行。（设计规格 10、11）

- [x] 13. 最终检查点：完成MVP技术验收
  - [x] 13.1 确保所有测试通过,如有疑问请询问用户；验证老板工作台、AI获客引擎、AI销售教练、我的成果和陪跑服务主链路。；验证机构越权、录音未授权访问、敏感日志、任务幂等、失败重试和旧数据兼容。

## Mock DB 最终验证矩阵（2026-08-22）

> 作者：Monkeycode

| 验证项 | 证据 | 结果 |
|---|---|---|
| 机构隔离、私有资产、签名读取与删除 | `node --test scripts/growth-platform-acceptance.test.js` | 通过 |
| 并发幂等、成功结果唯一、失败重试约束 | 同上 | 通过 |
| AI Schema 与 Skill 精确版本 | 同上 | 通过 |
| 获客快照汇总字段白名单 | 同上 | 通过 |
| 三类服务触点确定性、跨机构拒绝 | 同上 | 通过 |
| 销售手工转写、评分、训练任务、旧迁移重复执行 | `npm run smoke:growth-platform` | 通过并自然退出 |
| 完整旧版 Quick Plan 回归 | `npm run test:quick-plan` | 49/49 通过并自然退出 |
| 前端路由、组件与响应式产物 | 当前未配置前端测试框架，按约束不新增大型依赖；`npm run build` | 通过（769 modules） |
| 后端语法 | `node --check` 后端入口及本次变更的 db/mockDb/任务/获客/logger/验收测试文件 | 通过 |

注：已定位验收测试120秒不退出的实际原因为日志轮转定时器保持事件循环；定时器现已 `unref`。旧版 saved API 测试的 Windows libuv 断言由测试清理阶段强制 `process.exit` 引发，现改为等待服务器子进程退出。两类测试均可自然退出。
