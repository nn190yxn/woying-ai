# 需求实施计划

- [x] 1. 建立全量工具深测资产基线
  - [x] 1.1 生成真实工具清单快照
    - 从 `frontend/src/constants/toolCatalog.js`、后端路由和 `.monkeycode/docs/industry-tool-access-matrix.md` 汇总工具 code、名称、入口路径、端点、会员等级、行业归属和能力分组
    - 输出到 `docs/全量工具专业度深度测试方案.md` 的覆盖矩阵，替换历史 `TESTED-*` 状态为规范状态集合
    - 覆盖依据：`docs/全量工具专业度深度测试方案.md` 使用规则、`docs/工程开发进度.md` 的全接口内测交付口径
  - [x] 1.2 校准测试环境与访问入口
    - 将测试基线中的公网入口更新为 `https://woyai.cn`，保留 `http://124.223.3.175` 当前 Nginx 404 现状说明
    - 明确本地后端、线上 PM2、HTTPS API 健康检查、JWT 获取方式和禁止记录敏感值规则
    - 覆盖依据：`docs/线上验收执行记录.md` 固定环境、当前线上验证结果
  - [x] 1.3 统一深测状态与评分字段
    - 将状态限定为 `PENDING`、`PASS`、`FAIL`、`SKIP`，补充 `BLOCKED` 仅用于鉴权或环境阻塞记录
    - 固化答案正确性、链路通顺性、专业深度、行业用词、结构完整性、知识库引用、风险提示、行动建议、会员引导和降级状态字段
    - 覆盖依据：`docs/全量工具专业度深度测试方案.md` 专业度评估维度
  - [x] 1.4 执行工具清单覆盖校验
    - 对比前端工具目录、后端已挂载端点和测试矩阵，列出缺失、重复、仅前端存在、仅后端存在的工具
    - 将差异写回深测方案的“覆盖差异记录”

- [x] 2. 补齐标准 payload 与输入协议校验
  - [x] 2.1 创建或恢复 `backend/scripts/test-payloads.json`
    - 按 A-J 分组写入每个工具的标准 payload，字段必须与 `/api/generate/:toolCode`、小红书、抖音和私域端点协议一致
    - 对计算器类工具复用 `CALCULATORS.inputs` 和 `toolPayloadNormalizer` 口径，避免运行链路与测试链路分叉
    - 覆盖依据：`backend/scripts/deep-test-runner.js`、`docs/全量工具专业度深度测试方案.md` 标准测试输入
  - [x] 2.2 增加 payload 覆盖索引
    - 为每条测试用例记录 `code`、`name`、`group`、`endpoint`、`payload`、`expectedShape`、`industryTerms` 和 `riskChecks`
    - 确保小红书、抖音、私域专项端点保留领域内层结构检查项
    - 覆盖依据：统一外层协议和 `result` 领域字段保留要求
  - [x] 2.3 增加脱敏与敏感字段检查
    - 在测试资产中禁止写入 API Key、数据库密码、JWT_SECRET、支付密钥或 `.env` 真实值
    - 记录只允许出现环境变量键名存在性、HTTP 状态和响应摘要
    - 覆盖依据：`docs/线上验收执行记录.md` 使用规则
  - [x] 2.4 执行输入协议批量校验
    - 运行深测执行器的输入协议校验，确认 `INPUT_MISMATCH` 为 0
    - 如存在缺参，按工具 code 回写缺失字段和修正建议

- [x] 3. 强化深测执行器与输出报告
  - [x] 3.1 扩展 `backend/scripts/deep-test-runner.js` 的评分输出
    - 在现有 `benchmarks/actions/riskNotes/kbEnhanced/structureDepth/cta` 基础上增加 `correctness`、`flowHealth`、`termQuality`、`domainDepth` 和 `shapeCompat` 摘要字段
    - 保留 `DEGRADED`、`INPUT_MISMATCH`、`STRUCTURE_COMPAT` 输出，便于区分系统错误和内容质量问题
    - 覆盖依据：运营专家视角测试目标与现有执行器结构
  - [x] 3.2 生成 Markdown 结果写回能力
    - 将每个工具的 HTTP 状态、degraded、engineType、summary、专业度分、失败原因和证据摘要写回 `docs/全量工具专业度深度测试方案.md`
    - 失败项必须包含复现命令、失败点、判断原因、建议修复动作和是否需要人工介入
    - 覆盖依据：`docs/线上验收执行记录.md` 通用反馈模板
  - [x] 3.3 增加可分组执行参数
    - 支持按 `group`、`endpointPrefix`、`code` 执行，便于分批测试 A-J 组和单工具复测
    - 输出每组通过率、降级数、低分数、结构兼容数和阻塞数
    - 覆盖依据：逐工具测试且失败不中断后续工具的执行规则
  - [x] 3.4 为执行器编写回归测试
    - 覆盖输入缺参、HTTP 失败、degraded、结构兼容、低分 FAIL 和 PASS 六类结果
    - 校验报告字段脱敏且不写入真实密钥

- [x] 4. 覆盖前端入口与权限链路
  - [x] 4.1 建立页面入口矩阵
    - 从 `frontend/src/router/index.js` 和工具目录生成页面路径、工具 code、所需会员等级和预期 API 端点映射
    - 覆盖首页、工具库、行业页、诊断页、会员页、登录页、小红书、抖音、私域和单工具详情页
    - 覆盖依据：当前已验证的 SPA 主要路由和工具目录
  - [x] 4.2 补齐鉴权与权限测试步骤
    - 设计未登录、无 token、免费会员、年费会员四类权限场景，覆盖 401、升级引导、免费次数和可访问状态
    - 对受保护 API 记录无 token 返回 401 的基线，深测使用测试账号或安全 token 签发方案
    - 覆盖依据：`backend/src/middleware/auth.js`、会员权限架构文档
  - [x] 4.3 增加前端 API 链路检查
    - 验证前端统一使用 `/api` 前缀，401 跳转和错误提示走 `frontend/src/api/request.js`
    - 检查工具提交表单字段与后端 payload 协议一致
    - 覆盖依据：前端请求层和输入协议治理要求
  - [x] 4.4 执行页面入口轻量验收
    - 对所有一级页面和抽样工具页执行 HTTP 200、资源 200、无空白页、无明显布局阻塞检查
    - 将入口异常记录到线上验收执行记录

- [x] 5. 执行专业内容深测分组
  - [x] 5.1 建立 A-J 分组执行顺序
    - A 组小红书专项，B 组营销 RAG，C 组经营/制度/内容通用工具，D 组诊断与高阶工具，E-F 组餐饮计算器，G 组教培计算器，H 组美业计算器，I 组营销推广计算器，J 组抖音与私域专项
    - 每组先跑输入协议和结构检查，再跑内容评分，最后跑单工具复测
    - 覆盖依据：`docs/全量工具专业度深度测试方案.md` 分组结构
  - [x] 5.2 定义运营专家审查口径
    - 对每个工具检查结论是否可被老板执行，是否包含数字口径、行业阈值、城市/门店规模边界、复核提醒和下一步动作
    - 对行业用词检查餐饮、教培、美业、生活服务、小红书、抖音、私域的专业术语是否准确
    - 覆盖依据：用户提出的答案正确性、专业深度和行业用词目标
  - [x] 5.3 建立失败分级规则
    - P0 为接口不可用、鉴权异常、前端入口断链、响应结构破坏；P1 为 degraded、输入协议不匹配、关键字段缺失；P2 为专业度低、用词泛化、建议不可执行；P3 为文案润色和记录补充
    - 每个失败项记录修复归属、复测命令和验收标准
    - 覆盖依据：上线前全面人工内测交付标准
  - [x] 5.4 执行 A-J 全量接口深测
    - 按分组运行深测执行器，失败不中断后续工具
    - 将每个工具状态、专业度总分和失败原因写回深测方案
  - [x] 5.5 执行运营专家人工复核
    - 对低分、降级、结构兼容但内容薄弱的工具逐条复核输出文本
    - 记录行业术语、行动建议、风险提示和会员引导的具体缺陷

- [x] 6. 汇总交付记录与修复队列
  - [x] 6.1 生成最终测试汇总
    - 汇总总工具数、已测数、PASS、FAIL、SKIP、BLOCKED、degraded、INPUT_MISMATCH、平均专业度分和各组通过率
    - 列出 P0/P1/P2/P3 修复队列，明确可进入内测、需先修复、需产品确认三类结论
    - 覆盖依据：测试结束后汇总通过率、失败清单和需要修复工具列表
  - [x] 6.2 写回线上验收执行记录
    - 按模板追加本轮深测执行记录，包含命令、实际输出摘要、状态、证据、风险说明和下一步
    - 环境或鉴权阻塞必须写入 `BLOCKED` 记录，并说明所需测试账号或 token 签发方案
    - 覆盖依据：`docs/线上验收执行记录.md` 记录制度
  - [x] 6.3 形成可交付内测说明
    - 输出当前版本是否达到“可交付甲方团队进行全面人工内测”的判断
    - 明确人工内测重点：高价值工具、低分工具、权限链路、支付/会员入口、移动端首屏和核心行业场景
    - 覆盖依据：`.monkeycode/MEMORY.md` 当前阶段最终工作节点
  - [x] 6.4 执行修复后回归抽测
    - 对所有 P0/P1 修复项运行单工具复测和对应页面入口验收
    - 更新修复队列状态和最终通过证据

- [x] 7. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户
