# 我赢AI

> 本次第7阶段API与前端最小闭环交付作者：Monkeycode。新增结构化诊断依据、周复盘与顾问触发接口，并在获客页提供受限摘要展示。 - 中小企业 AI 经营工具平台

> 作者：Monkeycode

面向儿童素质类培训机构，优先从少儿体育培训的真实业务、案例和资源开始验证，围绕“经营体检 → 作战计划 → 执行工具 → 数据复盘 → 会员升级”提供 AI 获客与陪跑能力。

## 项目边界

- 唯一项目根目录：`E:\程序开发\我赢AI\woying-ai`
- 后续代码、文档、调研、设计、测试和交付产物全部保存在本目录内。
- 第一阶段ICP：儿童素质类培训机构。
- 首个验证样本：少儿体育培训机构。
- 商业模式：软件 + 陪跑服务。
- 医美作为后续候选行业，第一阶段不并行开发。

## 项目结构

- `frontend/` - 前端项目（Vue 3 + Vite）
- `backend/` - 后端项目（Express.js）
- `miniapp/` - 微信小程序（uni-app + Vue 3）
- `knowledge-base/` - 行业知识库
- `skills/` - 诊断 skill 定义
- `.monkeycode/docs/` - DeepWiki 风格项目文档
- `.monkeycode/specs/` - 需求、设计和实施规格
- `docs/` - 工程文档
- `docs/superpowers/specs/2026-08-22-child-training-growth-platform/design.md` - 增长平台设计规格
- `docs/superpowers/specs/2026-08-22-child-training-growth-platform/ui-design.md` - 老板端UI设计规范
- `docs/superpowers/specs/2026-08-22-local-life-knowledge-os-design.md` - 本地生活知识操作系统接入设计规格
- `.monkeycode/specs/child-training-growth-platform/tasklist.md` - 详细实施任务及验证矩阵

## 儿童素质培训增长平台

- 机构隔离、独立平台角色、私有文件、签名访问和异步任务。
- AI获客引擎：长期项目、抖音/小红书统一适配、截图OCR、诊断、行动方案、历史和阶段复盘。
- AI销售教练：授权上传、ASR或手工降级、新签/续费识别、可解释评分、原话证据、训练任务和趋势。
- 服务交付：30天冲刺、90天陪跑、创始人顾问计划、确定性触点、风险提醒和服务推荐。
- 顾问工作台：仅访问被分配机构，聚合获客、销售和服务摘要，平台管理员操作进入脱敏审计。
- 我的成果：按机构聚合，不返回音频地址或完整转写。
- 产品事件：事件名和payload白名单，不记录姓名、电话、音频或完整转写。
- 旧版抖音计划与复盘支持保留来源、版本的幂等迁移，15天计划仅为可选模板。
- 分层知识检索按行业、产品域、渠道、场景和Skill版本过滤并限制注入长度。
- 本地生活知识OS第1—3阶段已落地：只读清单冻结与SHA-256、JSON白名单和脱敏、幂等知识治理模型、儿童培训首批规范知识及结构化检索。
- 源知识仅盘点，不复制原库、不执行源脚本；运行 `cd backend && npm run knowledge:manifest` 时清单只写入项目 `.monkeycode/manifests/`。
- 结构化知识默认隔离冲突、撤回、归档、过期及高隐私对象，并按机构基线、证据等级、派生链和字符预算检索；旧知识接口保持兼容。
- 本地生活知识OS第4—6阶段已落地（作者：Monkeycode）：Skill注册与统一AI Runner、聚合漏斗指标/周复盘、顾问告警触发与人工接管；保留旧获客API和Mock DB兼容。
- 本地生活知识OS第8阶段最终验收已完成（作者：Monkeycode）：补齐旧抖音/Skill/行动计划/快照兼容专项测试，修复Runner嵌套敏感字段脱敏与Mock DB告警重开，知识/增长/顾问/Quick Plan回归及前端构建通过。
- 桌面、平板和竖屏响应式适配，提供基础 Web App manifest。
- 2026-08-23（作者：Monkeycode）：完成儿童培训“获客→预约/到店→体验课→正价成交”7天结构化闭环。匿名样本专项 4/4、后端全量 117/117、前端 Vite 构建通过；默认无 LLM 时保持 `needs_review`，测试使用 Mock DB，未部署、迁移或提交。
- 2026-08-23（作者：Monkeycode）：确认全站运营语言体系改造设计与实施计划。前台将聚焦儿童素质培训，以机构老板/校长为首要用户，采用实战陪跑顾问口吻；客户页面和内部后台均纳入治理，所有测试任务列为必做。设计见 `docs/superpowers/specs/2026-08-23-sitewide-operations-copy-design.md`，计划见 `.monkeycode/specs/sitewide-operations-copy/tasklist.md`。
- 2026-08-23（作者：Monkeycode）：完成运营语言任务2，主导航改为本周经营、招生行动、家长跟进、经营复盘、经营记录和陪跑服务；首页改为儿童培训机构经营工作台；客户侧行业筛选仅显示教培，餐饮、美业等代码和历史路由保留。导航测试3/3、基础语言测试5/5、前端构建通过。
- 2026-08-23（作者：Monkeycode）：完成运营语言任务3，公共工具目录改为经营问题语言并补充CAC、LTV、ROI、人效和场地利用率中文解释；经营表格、工具详情及服务方案统一为填写、以前记录、下载经营记录、本周建议和顾问复盘口吻。公共组件文案测试4/4、基础语言测试5/5、导航测试3/3、前端构建770 modules通过。
- 2026-08-23（作者：Monkeycode）：完成运营语言任务4，校长工作台、招生闭环、经营体检问卷和报告统一围绕本周问题、招生数据、最多两个优先动作、七天复盘和顾问确认；报告固定展示主要问题、判断依据、可能原因、本周先做、暂时不要做、观察指标及停止或求助条件。核心诊断文案测试5/5，三组回归12/12，前端构建770 modules通过；全站扫描剩余92处后续治理项。
- 2026-08-28（作者：Monkeycode）：完成运营语言任务5，抖音工作台、经营判断、15天行动和视频复盘改为主推课程、家长问题、拍摄发布、咨询、到店与报名语言；系统规则、模型生成、模型补充和规则兜底增加兼容来源标识，顾问确认默认不伪造。抖音前端专项5/5、前端相关回归17/17、Quick Plan 49/49、抖音诊断3/3及构建770 modules通过；全站扫描剩余85处后续治理项。
- 2026-08-28（作者：Monkeycode）：完成运营语言任务6，家长跟进按“新咨询→待联系→待邀约→已约体验→体验后回访→已报名→暂未报名”组织；少儿体育续费与消课统一使用出勤、训练表现、教练反馈、剩余课时和老带新语言；会员及陪跑服务明确系统整理提醒与真人顾问确认纠偏边界。专项及相关回归26/26、构建770 modules通过；全站扫描剩余82处，留待任务7—8治理。
- 2026-08-28（作者：Monkeycode）：完成运营语言任务7，后台按机构运营、陪跑交付、客户服务、财务管理和技术设置划分；平台管理员权限由服务端角色表解析，运行配置与排障日志受技术权限保护；日志递归脱敏手机号、Token、密钥、完整输入、请求体和凭据，并兼容 error.log/旧日志名。专项权限与脱敏测试4/4、机构隔离回归3/3、前端构建770 modules通过；扫描剩余79处，属于任务8及后续长尾治理；未部署、未迁移、未提交，未启动任务8。

## 媒体供应商配置

- `ASR_ENDPOINT` / `ASR_API_KEY`
- `OCR_ENDPOINT` / `OCR_API_KEY`
- 适配器使用 `multipart/form-data` 的 `file` 字段及可选 Bearer。
- 未配置真实供应商时任务进入 `needs_review`，继续使用手工转写或人工确认，不伪造成功。

## 启动方式

### 前端

```bash
cd frontend
npm install
npm run dev
```

前端默认端口为 `5173`。联调后端默认端口 `3001` 时设置 `VITE_API_TARGET=http://localhost:3001`。

### 后端

```bash
cd backend
npm install
NODE_ENV=development npm run dev
```

### 微信小程序

```bash
cd miniapp
npm install
npm run dev:mp-weixin
```

## 本地预览

- 校长经营工作台：`http://127.0.0.1:5173/growth`
- 本周招生行动：`http://127.0.0.1:5173/growth/acquisition`
- 家长沟通复盘：`http://127.0.0.1:5173/growth/sales-coach`
- 我的成果：`http://127.0.0.1:5173/growth/results`
- 陪跑服务：`http://127.0.0.1:5173/growth/services`

## MVP验证

```bash
cd backend
npm run test:growth-platform
npm run smoke:growth-platform
npm run test:quick-plan
node --test scripts/media-adapters.test.js
node --test scripts/acquisition-adapters.test.js
npm run test:advisor-events

cd ../frontend
npm run build
```

验收覆盖机构资源隔离、私有资产访问与删除、任务并发幂等和重试、AI Schema/Skill版本、获客快照白名单、服务触点确定性、媒体降级、旧数据迁移及前端生产构建。日志轮转定时器已使用 `unref`，测试环境直接使用 Mock DB；旧版 saved API 测试已移除强制 `process.exit` 并等待子进程退出，完整 Quick Plan 49项测试全部通过并自然退出。

## 线上运行

- 主站：`https://woyai.cn/`
- 健康检查：`https://woyai.cn/api/health`
- 运行方式：Nginx 托管前端静态资源并反向代理 `/api`，PM2 管理 Express 后端。
- 完整项目Wiki入口：`.monkeycode/docs/INDEX.md`

## 当前风险

- 真实 ASR/OCR 供应商尚未配置，当前使用人工降级路径。
- `/api/token-monitor/*` 线上未鉴权。
- 线上目录与本地仓库版本基线不一致，发布前必须单独确认；本任务不包含部署。
- 小程序支付、短信验证码和图片内容安全尚未形成生产闭环。
- 依赖审计仍有已知漏洞，未执行可能造成破坏性升级的 `npm audit fix --force`。


## 第7阶段 API 与前端闭环（作者：Monkeycode）

已补齐获客周复盘 API：按机构权限读取最新阶段快照，返回主问题、聚合证据、验证指标、停止条件，并通过去重键触发顾问告警；前端获客引擎展示诊断证据、主次原因、行动卡、依据类型、核验日期、needs_review、阶段快照及周复盘入口。客户侧继续只展示聚合结果，不展示第三方原文、CRM 或个人明细。

## 第8阶段验收矩阵（作者：Monkeycode）

| 范围 | 结果 | 证据 |
|---|---|---|
| 知识系统专项 | 通过 | npm run test:knowledge-os：10/10；test:local-life-acceptance：4/4 |
| 增长/顾问/媒体/渠道 | 通过 | growth-platform 5/5、advisor-events 2/2、media/adapters 5/5；smoke:growth-platform 通过 |
| Quick Plan与旧兼容 | 通过 | npm run test:quick-plan、deep-runner、douyin-diagnosis 全部通过 |
| 安全边界与聚合指标 | 通过 | 客户快照白名单；零分母仅在输入指标存在时输出 null，不泄露个人字段 |
| 构建与语法 | 通过 | frontend npm run build（769 modules）、backend 全部 src/*.js node --check 通过 |

本阶段不部署、不运行源知识库脚本、不复制源库、不执行线上迁移。

## 平台基础能力分阶段实施（作者：Monkeycode）

已启动机构权限、服务器文件存储和异步任务可靠性三阶段实施。本次完成机构权限服务第 1.1 项：角色能力矩阵、成员列表/新增/角色变更/撤销、默认机构切换，机构成员操作基于服务端成员关系；机构专项测试 2/2 通过。已补充统一机构权限中间件：支持按权限能力和角色限制路由，并先验证机构成员关系；专项中间件测试 2/2 通过。已补齐机构成员、默认机构和网点 CRUD API，并在路由层绑定成员/网点权限；机构网点专项测试 2/2 通过。已补齐机构数据库与 Mock DB 结构：成员索引、默认机构查询索引、网点状态字段和机构权限审计日志，并实现成员变更/默认机构切换审计记录；机构结构回归 7/7 通过。已将机构角色权限接入获客资源、文件存储、异步任务和服务交付路由；文件管理、任务管理及服务读写均执行服务端机构成员与角色校验。已补充角色允许/拒绝矩阵、成员生命周期、默认机构切换、网点隔离、顾问分配和跨机构访问测试；新增权限矩阵专项测试 3/3 通过，累计机构权限回归 10/10 通过。机构权限阶段检查点已通过：知识治理 10/10、本地生活 4/4、增长平台 5/5、顾问事件 2/2、Quick Plan 全部通过、深度测试 6/6、抖音诊断 3/3，前端生产构建通过。已完成文件存储 2.1：新增本地文件存储适配器，使用 `PRIVATE_STORAGE_DIR` 配置根目录，服务端生成机构隔离存储键，并拒绝绝对路径与目录穿越。文件适配器及既有文件链路 6/6 通过。已完成文件存储 2.2：增加 MIME 白名单、扩展名匹配、100MB 大小限制、声明大小与实际内容一致性校验、SHA-256 哈希和路径安全校验；音视频仍保留后续时长校验扩展点。专项文件测试 2/2，既有文件链路 5/5 通过。已完成文件存储 2.3：签名读取加入机构范围，读取时校验资产归属、`ready` 状态、保留期和物理文件存在性；补齐临时资产、过期清理和删除幂等语义。文件生命周期专项 3/3、既有文件链路 5/5 通过。已完成文件存储 2.4：新增机构隔离资产列表、单资产状态查询和非法状态拒绝；文件创建、上传、签名读取和删除继续绑定机构文件权限。文件专项回归 9/9 通过。已完成文件存储 2.5：新增按机构隔离的过期资产扫描、物理文件清理和 pending 临时资产回收任务；清理任务接入同机异步 Worker，并按机构与日期幂等。清理任务及文件专项回归通过。已完成文件存储 2.6：补齐适配器契约、非法 MIME/扩展名/大小、哈希、跨机构签名、过期读取、路径穿越、物理文件缺失和上传失败回收测试；文件安全验收矩阵 12/12 通过。文件存储阶段检查点 2.7 已通过：文件存储专项及既有增长平台回归 13/13 通过，前端生产构建 769 modules 通过。异步任务可靠性 3.1 已完成：MySQL 与 Mock DB 补齐 `locked_by`、`locked_at`、`lease_expires_at`、`next_run_at` 和 `result_idempotency_key` 字段及查询索引；幂等补列和字段专项测试通过。异步任务可靠性 3.2 已完成：任务领取改为数据库条件更新式原子 claim，仅允许 queued 到期任务或 processing 租约过期任务被领取；新增 Worker 锁定标识、租约清理和完成状态条件更新，防止多 Worker 重复执行。专项及兼容回归 9/9 通过。异步任务可靠性 3.3 已完成：增加 `cancel_token` 与 `cancel_requested_at`，取消操作使用机构范围和状态条件更新；Worker 完成/失败写入必须仍持有 processing 锁定，取消后不会覆盖为 succeeded；handler 获得取消检查和 0–99 受控进度回调。专项及兼容回归 11/11 通过。异步任务可靠性 3.4 已完成：可重试失败进入 `failed_retryable` 并按 1/2/4 秒指数退避，通过 `next_run_at` 调度；达到 3 次尝试后进入 `failed_final`；错误记录 `transient`/`permanent` 分类；数据库幂等键冲突时重新读取已有任务。专项及兼容回归 13/13 通过。异步任务可靠性 3.5 已完成：任务默认生成 `result_idempotency_key`，handler 可通过 `writeResult` 受控写入业务结果；相同结果幂等键重复写入只保留首次结果，冲突键拒绝覆盖；任务完成使用已有结果保护，避免重试覆盖业务结果。专项及兼容回归 15/15 通过。异步任务可靠性 3.6 已完成：新增 `backend/src/workers/asyncTaskWorker.js` 独立 Worker 入口，注册文件清理和媒体任务处理器；API 进程不再启动任务轮询，新增 `npm run worker`；支持启动失败诊断、SIGTERM/SIGINT 停止和关闭定时器。Worker 自然退出验证及异步任务/增长平台回归 15/15 通过。异步任务可靠性 3.7 已完成：新增任务提交入口，仅允许已注册处理器类型；查询使用 `resources:read`，提交/取消/重试使用 `tasks:manage`，机构范围由服务端权限中间件确定。白名单与异步任务/增长平台回归 16/16 通过。异步任务可靠性 3.8 已完成：新增 `test:async-reliability` 专项矩阵，覆盖多 Worker claim、租约恢复、取消竞态、进度边界、指数退避、失败分类、结果幂等、白名单提交和 Worker 自然退出；专项矩阵 11/11 通过，文件清理/增长平台兼容回归 6/6 通过。异步任务阶段检查点 3.9 已通过：可靠性专项 11/11、文件清理与增长平台兼容 6/6、媒体/渠道适配器和机构 API/权限回归 9/9，累计 26/26 通过；API 进程与独立 Worker 关键文件语法检查通过。异步任务可靠性与独立 Worker 阶段完成。三阶段集成验收 4.1 已通过：异步专项 11/11、机构/存储/增长/媒体/渠道回归 30/30、Quick Plan 全套 49/49，累计 90/90 通过；未部署、未执行线上迁移。平台基础能力 4.2 已通过：全部 `backend/src/**/*.js` 语法检查通过；Worker 自然退出和 Quick Plan API 进程生命周期测试 2/2 通过。平台基础能力 4.3 已通过：前端 `npm run build` 成功，Vite 转换 769 modules，机构、文件和增长页面无构建回归。平台基础能力最终验收记录（作者：Monkeycode）：机构权限、服务器文件存储、异步任务可靠性与独立 Worker 已完成；集成回归 90/90、后端源文件语法检查通过、前端生产构建 769 modules 通过。验证环境 MySQL 不可用时使用 Mock DB。最终检查点 4.5 已通过：平台基础能力任务清单全部完成，所有设计验收项均有自动化测试或构建/语法证据；本次未部署、未执行线上迁移。

## 2026-08-23 发布前代码审计与模拟验收（作者：Monkeycode）

- 审计范围：当前未提交变更；高强度代码审查未发现确认缺陷，结果为 0 findings。
- 后端全量测试：`node --test scripts/*.test.js`，113/113 通过。
- 真实运行时冒烟：`npm run smoke:growth-platform` 通过，返回机构隔离、诊断、任务、触点、告警和迁移结果：`organizationId=1`、`score=85`、`tasks=1`、`touchpoints=7`、`alert=closed`、`migratedPlans=1`。
- 前端生产构建：`npm run build` 通过，Vite 转换 769 modules。
- 依赖审计发现既有风险：backend 6项、frontend 7项（含 Axios/form-data 等高危项）；未执行 `npm audit fix --force`，避免破坏性升级。
- 限制：本次 MySQL 不可用，后端使用 Mock DB；未验证真实 MySQL、线上部署、外部 ASR/OCR 供应商和生产基础设施。

## 核心功能业务闭环测试（作者：Monkeycode）

使用匿名少儿篮球机构样本完成“经营输入 → 分层知识检索 → 结构化诊断 → 聚合指标 → 7天行动计划 → 周复盘 → 顾问升级”闭环测试。命中 `funnel-traffic`、`trial-offer`、`traffic-prohibition` 等6条知识，得到“先解决体验课承接与成交，不应继续扩大投流”的诊断；闭环专项1/1、后端全量114/114通过。

测试同时修复 `childName` 脱敏遗漏和Runner知识命中数审计错误。当前判断：后端知识与业务链可用，但客户侧尚未闭环——老板端缺少真实经营数据输入表单和一键行动/复盘流程，未配置LLM时诊断只进入 `needs_review`。详细报告：`.monkeycode/docs/核心功能业务闭环测试报告-2026-08-23.md`。