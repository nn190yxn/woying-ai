# 5月13日回退恢复执行计划

## 0. 执行原则

- 本轮恢复按逐一工具审核执行，覆盖所有工具，不只抽查重点工具。
- 每个工具必须核对前端目录、前端执行页、后端注册、计算公式、阈值、输出结构、知识库映射和知识库文件可用性。
- 每个工具恢复前先记录基线，恢复后更新 `docs/全工具公式与知识库基线.md`。
- 本地与线上均有有效改动，采用三方合并，不使用单边覆盖。
- 线上目录已冻结，恢复前继续以 `/workspace/audit-freeze/remote-tree/` 作为线上快照基线。

## 1. 当前基线

- 本地分支：`woai-ai-v1`
- 本地 HEAD：`482d8fb6f9b6b8949d0c8e9f7079dd8891a937d4`
- 线上快照：`/workspace/audit-freeze/remote-tree/`
- 线上冻结目录：`/home/ubuntu/woying-ai-freeze-20260513-195958`
- 差异报告：`/workspace/audit-freeze/盘点报告.md`
- 工具矩阵：`/workspace/audit-freeze/tool-matrix.json`
- 深度计算候选：`/workspace/audit-freeze/remote-tree/backend/backups/static-benchmark-final-20260512225736/calculatorEngine.js`

## 2. 优先级总览

| 优先级 | 工作项 | 目标 | 关键文件 | 验收标准 |
|---|---|---|---|---|
| P0 | 修复本地认证语法错误 | 本地后端恢复可运行 | `backend/src/middleware/auth.js` | `node --check backend/src/middleware/auth.js` 通过 |
| P0 | 建立公式与知识库基线文档 | 防止未来再次回退后无法定位正确版本 | `docs/全工具公式与知识库基线.md` | 每个已审核工具都有公式、阈值、知识库、恢复依据 |
| P1 | `toolCatalog.js` 三方合并 | 恢复约 60 个“智能体”命名与入口口径 | `frontend/src/constants/toolCatalog.js` | 本地工具 code 数量保持 98，智能体命名按线上基线恢复 |
| P1 | `kb-mapping.json` 合并 | 保留本地 82 个映射键，吸收线上有效结构 | `backend/src/config/kb-mapping.json` | 映射键不丢失，路径指向真实存在知识库文件 |
| P1 | 恢复深度计算智能体逻辑 | 恢复员工成本、ROI、营销推广、餐饮/教培/美业计算智能体 | `backend/src/services/calculatorEngine.js` | 深度输出包含 `benchmarks/actions/riskNotes/sections/extra` |
| P2 | 恢复私域前端到线上部署源 | 让线上部署源具备本地私域工作台 | `frontend/src/views/private/`、相关路由 | 私域入口和页面在部署源中可访问 |
| P2 | 小红书 13 个占位智能体补真实能力 | 去掉占位，接入真实表单和后端能力 | `frontend/src/views/xhs/`、`backend/src/routes/xhsAgents.js` | 每个占位工具有真实输入、输出和知识库链路 |
| P1 | 核心服务层回退恢复 | 恢复统一结果 schema、RAG 结构化解析、AI 重试/fallback 和降级兜底 | `backend/src/services/resultSchema.js`、`backend/src/services/ai.js`、`backend/src/services/engineRegistry.js`、`backend/src/services/failover.js` | 核心文件 `node --check` 通过，RAG 失败时返回 `degraded=true` 的结构化 fallback |
| P1 | 抖音与生成主链路恢复 | 恢复抖音知识库 + AI 链路、内容/营销工具模块注册和 guest mode 兼容 | `backend/src/routes/douyinAgents.js`、`backend/src/routes/generate.js`、`backend/src/tools/content.js`、`backend/src/tools/marketing.js`、`backend/src/services/promptBuilder.js` | 路由/工具模块 `node --check` 通过，内容/营销工具由 `rag` 链路承载，抖音智能体返回 `knowledge-ai` 元信息 |

## 3. 阶段 A：本地后端可运行修复

### A1. 修复 `auth.js`

- 目标：先让本地后端具备基本语法可运行能力，作为后续验证基础。
- 步骤：
  - 对比本地 `backend/src/middleware/auth.js` 与线上快照同名文件。
  - 找出本地 `Illegal return statement` 来源。
  - 保留本地有效改动，移除或合并重复残片。
  - 执行语法检查。
- 验收：
  - `node --check backend/src/middleware/auth.js` 通过。
  - 未改变认证中间件对登录态、会员等级和开发模式的既有语义。

## 4. 阶段 B：工具目录三方合并

### B1. 工具清单对齐

- 目标：恢复前端工具目录中的“智能体”命名，同时保持工具 code、分类和入口稳定。
- 输入：
  - 本地 `frontend/src/constants/toolCatalog.js`
  - 线上快照 `audit-freeze/remote-tree/frontend/src/constants/toolCatalog.js`
  - `audit-freeze/tool-matrix.json`
- 步骤：
  - 逐一核对 98 个工具 code。
  - 对每个工具记录本地名称、线上名称、是否应恢复智能体命名。
  - 保留本地新增或修复过的分类、权限、入口逻辑。
  - 恢复线上更正确的智能体名称和专项入口口径。
- 验收：
  - 工具 code 数量不减少。
  - 命名恢复记录写入 `docs/全工具公式与知识库基线.md`。
  - 前端构建通过。

## 5. 阶段 C：知识库映射合并

### C1. 映射键合并

- 目标：保留本地 82 个映射键，吸收线上有效映射结构和 section 精细化配置。
- 输入：
  - 本地 `backend/src/config/kb-mapping.json`
  - 线上快照 `audit-freeze/remote-tree/backend/src/config/kb-mapping.json`
  - 本地 `knowledge-base/`
  - 线上快照 `audit-freeze/remote-tree/knowledge-base/`
  - 线上快照 `audit-freeze/remote-tree/backend/knowledge-base/`
- 步骤：
  - 列出全部映射键并按工具 code 对齐。
  - 对每个映射检查知识库文件是否存在。
  - 修复指向缺失文件的映射路径。
  - 补齐计算智能体缺失映射，尤其是员工成本、ROI、利润、现金流、人效类。
- 验收：
  - 映射键总数不低于本地现有 82 个。
  - 每个映射的 `knowledgeFiles.path` 在当前工作区可找到对应文件或有明确替代路径。
  - 基线文档记录每个工具的知识库路径和 section。

## 6. 阶段 D：深度计算智能体恢复

### D1. 恢复范围

- 员工成本类：餐饮、教培、美业。
- ROI/回报类：通用 ROI、餐饮/教培/美业回报率、活动 ROI、投流 ROI。
- 营销推广类：节日营销、裂变、促销、投放、平台经营相关工具。
- 餐饮计算智能体：食材成本、毛利、净利、损耗、人效、翻台、外卖、菜品结构等。
- 教培计算智能体：续费、课消、满班率、场地、人效、获客、回本、现金流等。
- 美业计算智能体：品项、人工、人效、转化、耗卡、床位、仪器、会员卡、回本等。

### D2. 单工具审核模板

每个工具按以下字段审计并写入 `docs/全工具公式与知识库基线.md`：

| 字段 | 说明 |
|---|---|
| 工具 code | 前后端统一 code |
| 正确名称 | 恢复后的名称 |
| 入口/页面 | 前端入口或执行页 |
| 输入字段 | 用户需要填写的字段 |
| 核心公式 | 计算公式和派生字段 |
| 阈值/基准 | 判断区间、行业参考和风险区间 |
| 知识库来源 | `kb-mapping.json` 路径与 section |
| 内置知识库 | `calculatorEngine.js` 内置 KB 常量 |
| 正确版本依据 | 本地、线上当前、线上备份哪个版本作为依据 |
| 恢复动作 | 需要保留、合并或重写的内容 |
| 验收方式 | 单测、语法检查、接口调用、前端构建 |

### D3. 恢复判定规则

- 若线上 `static-benchmark-final` 有 `benchmarks/actions/riskNotes` 且本地缺失，优先恢复深度结构。
- 若本地 `kb-mapping.json` 有更多映射键，保留本地映射并修正路径。
- 若线上当前版和备份版仅文案差异，优先采用更审慎的“经验观察/需校准”版本。
- 若本地存在后续修复，例如输入校验、统一响应结构、注册污染修复，保留本地修复。

## 7. 阶段 E：私域前端恢复到部署源

- 目标：把本地已有私域前端能力恢复到线上部署源范围。
- 范围：
  - `frontend/src/views/PrivateAgentHub.vue`
  - `frontend/src/views/private/*.vue`
  - 路由、入口、工具目录、权限配置。
- 验收：
  - 私域入口可访问。
  - 私域页面不再出现线上缺目录问题。
  - 后端 `privateAgents.js` 路由与前端调用匹配。

## 8. 阶段 F：小红书占位智能体补真实能力

- 目标：13 个占位页面从“智能体开发中”恢复为真实可用能力。
- 步骤：
  - 逐一列出 17 个小红书页面，其中标记 13 个占位。
  - 对每个占位页明确输入字段、后端 endpoint、知识库来源、输出结构。
  - 优先复用 `backend/src/routes/xhsAgents.js` 中已有真实能力。
  - 对 placeholder 路由补实际规则或 RAG 链路。
- 验收：
  - 每个页面有真实表单。
  - 每个 endpoint 返回结构化结果。
  - 知识库路径存在并能被检索或注入。

## 9. 回归验证

- 后端语法检查：关键 JS 文件逐个 `node --check`。
- 前端构建：`npm run build`。
- 计算器接口：对恢复过的每个工具至少执行一次典型参数调用。
- 知识库路径：逐个确认映射文件存在。
- 文档一致性：每个恢复工具必须在 `docs/全工具公式与知识库基线.md` 有记录。

## 10. 执行记录

- 2026-05-13：创建本计划，按用户确认的优先级固化执行顺序和逐一工具审核规则。
- 2026-05-13：恢复核心服务层线上深度结构，新增 `resultSchema.js`，补回 `ai.js` 重试/fallback、`engineRegistry.js` RAG 解析与统一包装、`failover.js` RAG 降级兜底；核心文件语法检查和后端 build 脚本验证通过。
- 2026-05-13：恢复 `douyinAgents.js` 知识库 + AI 主链路和 `rule-based-rag` fallback；恢复 `generate.js` 对内容/营销知识库 AI 工具模块的注册、guest 用户安全处理与输入 payload 归一；新增 `content.js`、`marketing.js`、`promptBuilder.js`，路由/工具模块语法检查和后端 build 脚本验证通过。
