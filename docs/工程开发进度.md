# 工程开发进度

## 1. 项目基本信息
- 项目名称：我赢AI（网页端第一阶段）
- 项目目标：为中小企业老板提供可落地的AI工具，并通过会员订阅变现
- 目标用户：全国中小企业老板
- 当前阶段：**线上/本地回退恢复审计与全工具公式基线建设**
- 最近更新时间：2026-05-14（已完成首批餐饮、第二批教培、第三批美业、第四批营销推广、第五批模板/AI 工具链路核对、小红书专项占位恢复与 meituan 统一结果流收口，并完成线上同步与关键接口验证）
- 功能总数：120个（全量），已完成后端注册 120/120，已实现执行引擎 120/120
- 基础设施：✅ 完成（日志/埋点/校验/兜底/监控）

## 2. 文档与需求来源
- 主需求文档：`需求文档_v4_功能详细规划 (1)`
- 视觉规范：`视觉规范_v1.md`
- 实施清单：`实施清单_v1.md`
- 权限结构文档：`.monkeycode/docs/membership-permission-architecture.md`
- 行业工具矩阵：`.monkeycode/docs/industry-tool-access-matrix.md`

## 2.2 关键协作文档

### `5月13日回退恢复执行计划.md`
- 用途：固化本轮恢复顺序、逐一工具审核原则、三方合并策略和验收标准。

### `全工具公式与知识库基线.md`
- 用途：记录每个工具的正确名称、输入字段、公式、阈值、知识库来源和恢复依据，防止未来回退后无法快速定位正确版本。

### `线上环境修复与复测Runbook.md`
- 用途：固化线上 `.env` 键名补齐、AI 网关修复、PM2 重启和 smoke 复测顺序，避免配置修复后缺少验收步骤。

### `线上验收执行记录.md`
- 用途：给后续 Agent 逐阶段记录线上验收动作、命令、结果、状态和问题反馈，避免执行到后面遗忘前面结论。

### `协作文档/README.md`
- 用途：给新对话和新 Agent 提供可见入口，避免只在隐藏目录里查找文档。

### `协作文档/会员权限结构总表.md`
- 用途：在工作区可见目录中提供会员权限结构摘要版，便于快速识别。

### `协作文档/行业工具权限矩阵.md`
- 用途：在工作区可见目录中提供行业工具权限矩阵摘要版，便于快速识别。

### `协作文档/行业表格模板矩阵.md`
- 用途：在工作区可见目录中提供行业表格模板矩阵摘要版，便于快速识别。

### `协作文档/表格模板接入方案_v1.md`
- 用途：给后续 Agent 明确第一轮 6 张高价值表如何接入前端共享结构和行业页面。

## 2.2.1 2026-05-02 当前前台改版约束
- 当前正在重构：`frontend/` 首页、工具箱、行业入口层级与图标系统。
- 本轮目标不是继续堆长页面，而是把前台改成更短、更清晰、更模块化的结构。
- 本轮明确约束：
  - 行业专版只保留真实行业入口：`餐饮 / 教培 / 美业 / 生活服务`
  - `小红书运营` 不再出现在行业专版入口中
  - `小红书运营 / 抖音经营 / 老板IP / 企业诊断` 作为专项模块独立展示
  - 工具箱不再使用 8 个板块整页纵向堆叠的长列表形式，应改成“模块入口 + 当前模块详情”的工作台结构
  - 首页减少叙事型长滚动区块，优先保留品牌价值、模块入口、行业入口、专项入口、会员方案等高价值区块
  - 全站逐步移除 emoji 图标，替换为统一 SVG 图标系统，提升产品质感

## 2.2.2 2026-05-02 首页/工具箱模块化改版进展

### 已完成
- `frontend/src/views/Home.vue`
  - 首页已重写为短页面结构，当前只保留 Hero、8 大模块入口、行业专版、专项能力、会员方案 5 个核心区块。
  - 首页模块统计不再依赖行业页聚合结果，已改为直接基于 `allTools + mapToolToPillar` 计算，避免把 `小红书运营` 伪行业入口带来的重复计数混入首页。
- `frontend/src/views/Tools.vue`
  - 工具箱已从旧的“8 段纵向折叠长列表”改为“行业入口 + 专项模块 + 会员筛选 + 左侧模块切换 + 右侧当前模块详情”的工作台结构。
  - 当前支持通过 `/tools?pillar=xxx` 直接定位模块。
- `frontend/src/constants/toolCatalog.js`
  - `pillarMeta.icon` 已切换为统一 SVG 组件。
  - 已新增 `visibleIndustryEntries`，行业入口只保留 `餐饮 / 教培 / 美业 / 生活服务`。
  - 已新增 `specialModuleEntries`，把 `抖音经营 / 小红书运营 / 企业诊断 / 老板 IP` 独立为专项能力入口。
  - `homeToolCategories` 已改为从 `pillarMeta + mapToolToPillar + allTools` 派生，保证 `/modules/:id` 与新 8 大模块口径一致。
- `frontend/src/views/IndustryView.vue`
  - 行业顶部切换只显示真实行业入口。
  - 行业切换网格已改为 `auto-fit`，避免桌面端固定 5 列留下空槽。
- 视觉残留清理
  - `frontend/src/views/Diagnosis.vue`
  - `frontend/src/views/DiagnosisHistory.vue`
  - `frontend/src/views/Admin.vue`
  - 上述页面中的主要 emoji 图标已替换为 SVG，和首页/工具箱统一视觉语言。

### 本轮验证
- 前端构建通过：
  - `cd /workspace/frontend && npm run build`

## 2.2.16 2026-05-13 回退恢复：餐饮、教培与美业计算智能体

### 2026-05-14 线上同步与关键验证
- 本地提交 `37892e3 fix: restore tool intelligence flows` 已推送到 `origin/woai-ai-v1`。
- 线上目录 `/home/ubuntu/woying-ai` 已确认不是 Git 仓库，本次采用 tar 包同步方式，并排除 `.git`、`backend/.env`、`WOYING.pem`、`audit-freeze`、`frontend/dist` 和各级 `node_modules`。
- 同步后已确认线上 `backend/.env` 仍存在，真实环境变量未被本次代码覆盖。
- 线上后端依赖安装、语法检查、前端依赖安装和 `npm run build` 已完成；`frontend/dist` 存在，公网首页 `http://124.223.3.175/` 返回 200。
- PM2 服务 `woying-backend` 当前 `online`，公网健康检查 `http://124.223.3.175/api/health` 返回 `status=ok`。
- 线上关键接口抽测通过：`/api/xhs/quick-start-plan`、`/api/xhs/grass-converter`、`/api/generate/meituan`、`/api/generate/gross-margin-restaurant`。
- `gross-margin-restaurant` 需要使用协议字段 `storeName` 和 `categories[]`，复测返回 `meta.engineType=calculator`、`meta.kbEnhanced=true`、`知识库参考` section。
- `festival`、`fission`、`marketing-plan` 返回 `rule-based-rag` 兜底，业务链路可用，但日志显示线上 AI 网关返回 `403 Forbidden`。
- 线上 `.env` 键名检查显示存在 `MCAI_LLM_API_KEY`、`MCAI_LLM_BASE_URL`、`MCAI_LLM_MODEL`、`PORT`，缺少 `DB_HOST/DB_USER/DB_PASSWORD/DB_NAME` 与 `MYSQL_*` 数据库键名；后端日志因此出现默认 root 空密码连接 MySQL 失败并切换到 in-memory mock。
- 已新增安全自检脚本 `backend/scripts/check-env-presence.js`，可通过 `npm run check:env` 检查必需环境变量键名是否存在，脚本只输出键名状态，不输出变量值。
- 可用 `ENV_FILE=.env.example npm run check:env` 验证模板完整性；当前模板验证通过，仅 `AI_FALLBACK_API_KEY`、`AI_FALLBACK_BASE_URL` 和 `REDIS_PASSWORD` 为可选缺省。
- 已将 `backend/scripts/check-env-presence.js`、`backend/package.json` 和两份进度/基线文档同步到线上；线上执行 `npm run check:env` 已复现当前缺口：缺少 `NODE_ENV` 和数据库完整键组，AI 主键名已存在。
- 同步自检脚本后复查 PM2 与公网健康检查，`woying-backend` 保持 `online`，`/api/health` 正常返回 `status=ok`。
- 已新增并同步核心接口 smoke 脚本 `backend/scripts/smoke-core-endpoints.js`，脚本只输出 HTTP 状态、`degraded` 标记、引擎类型和摘要，不输出敏感内容。
- 线上本机执行 `npm run smoke:core` 已通过全部 HTTP 检查：`health`、`xhs-quick-start-plan`、`xhs-grass-converter`、`meituan`、`festival`、`fission`、`marketing-plan`、`gross-margin-restaurant`；其中 `festival/fission/marketing-plan` 标记 `degraded=true`，符合当前 AI 网关 403 结论。
- 公网 smoke 可使用 `BASE_URL=http://124.223.3.175 HEALTH_PATH=/api/health npm run smoke:core`，本机默认使用 `BASE_URL=http://127.0.0.1:3000` 与 `/health`。
- `smoke-core-endpoints.js` 已补充 `SMOKE_TIMEOUT_MS` 单接口超时控制，默认 30000 毫秒；最终验收可使用 `STRICT_DEGRADED=true`，检测到任何 `degraded=true` 时返回失败码。
- 线上 `frontend npm install` 报告 `2 moderate severity vulnerabilities`，当前未执行 `npm audit fix --force`，避免破坏性依赖升级。
- 当前线上后续事项：补齐数据库环境变量、轮换或更换 AI 网关凭据、重新 PM2 启动并验证 RAG 主链路恢复为非降级输出。

### 已完成
- 创建并维护恢复协作文档：
  - `docs/5月13日回退恢复执行计划.md`
  - `docs/全工具公式与知识库基线.md`
- 本地/线上/深度备份三方盘点已完成，线上关键快照位于 `audit-freeze/remote-tree/`。
- 修复本地 `backend/src/middleware/auth.js` 重复残片导致的语法错误，并恢复 guest mode 逻辑。
- 修复 `frontend/src/api/request.js` 响应拦截器中 `await` 位于非 async 函数导致的构建错误。
- 首批餐饮工具已恢复深度智能体结构和前端/KB 命名：
  - `gross-margin-restaurant`
  - `turnover-rate-restaurant`
  - `food-waste-rate`
  - `labor-efficiency-restaurant`
  - `salary-cost-ratio-restaurant`
  - `delivery-profit`
  - `delivery-analysis`
  - `profit-rate-restaurant`
  - `dish-pricing`
- 第二批教培工具已按深度备份整段恢复，实际覆盖 13 个工具：
  - `renewal-rate-education`
  - `class-consumption-rate-education`
  - `gross-margin-education`
  - `break-even-education`
  - `salary-cost-ratio-education`
  - `labor-efficiency-education`
  - `venue-utilization-education`
  - `cac-education`
  - `payback-education`
  - `cashflow-education`
  - `profit-rate-education`
  - `return-rate-education`
  - `class-rate-education`
- `frontend/src/constants/toolCatalog.js` 中教培工具名称已同步为线上“智能体”版本。
- `backend/src/config/kb-mapping.json` 中教培工具名称已同步，并把缺失的 `教培行业知识库_综合v3.0.md` 改为真实存在的分主题知识库文件。
- 第三批美业工具已按深度备份整段恢复，实际覆盖 20 个工具：
  - `card-consumption-rate-beauty`
  - `gross-margin-beauty`
  - `break-even-beauty`
  - `salary-cost-ratio-beauty`
  - `labor-efficiency-beauty`
  - `conversion-rate-beauty`
  - `project-profit-beauty`
  - `ltv-beauty`
  - `repurchase-rate-beauty`
  - `payback-beauty`
  - `cashflow-beauty`
  - `profit-rate-beauty`
  - `return-rate-beauty`
  - `project-structure-beauty`
  - `labor-structure-beauty`
  - `card-debt-beauty`
  - `funnel-ltv-beauty`
  - `breakeven-profit-beauty`
  - `device-roi-beauty`
  - `member-card-design-beauty`
- `frontend/src/constants/toolCatalog.js` 中美业工具名称已同步为线上/深度“智能体”版本。
- `backend/src/config/kb-mapping.json` 已新增 20 个美业工具知识库映射，全部指向真实存在的美业分主题知识库文件。
- `docs/全工具公式与知识库基线.md` 已补充第三批美业工具公式、阈值、KB 来源和恢复依据。
- 第四批营销推广工具已按深度备份从 `// ====== 营销推广计算器 ======` 起整体恢复，实际覆盖 11 个后端执行工具：
  - `return-rate-restaurant`
  - `channel-cac`
  - `campaign-roi`
  - `referral-roi`
  - `conversion-funnel`
  - `retention-rate`
  - `marketing-budget`
  - `churn-rate`
  - `ltv-restaurant`
  - `ltv-education`
  - `promotion-profit`
- `frontend/src/constants/toolCatalog.js` 中第四批相关 13 个工具名称已同步为线上“智能体”版本，其中 `roi` 和 `payback` 已确认由前端组件本地计算承载。
- `backend/src/config/kb-mapping.json` 已新增/更新第四批营销推广工具知识库映射，路径指向投放运营、引流获客、私域运营和三大行业营销案例等真实存在文件。
- `docs/全工具公式与知识库基线.md` 已补充第四批营销推广工具公式、阈值、KB 来源和恢复依据。

### 当前验证状态
- 第四批营销推广恢复已完成代码和文档更新，语法检查、KB JSON/路径校验与前端构建验证已通过。
- 第五批 `festival`、`fission`、`marketing-plan`、`meituan`、`roi`、`payback` 已完成真实承载链路核对，并已回写基线文档。
- 补核 `return-rate-restaurant` 与 `return-rate-education`：教培版已与深度备份一致，餐饮版已补回深度输出结构。
- 私域专项前端本地存在、线上缺失的问题已完成本地接线：`/private` 智能体矩阵和 17 个子页面路由已接入，后端 `/api/private` 已挂载到 `privateAgents.js`。
- 前端 `toolCatalog.js` 中剩余 9 个"计算器"命名已批量恢复为线上"智能体"命名，当前 `name:` 字段不再包含"计算器"。
- 高优先级核心服务层已恢复线上深度结构：
  - 新增 `backend/src/services/resultSchema.js`，统一返回 `status/degraded/meta` 结构。
  - `backend/src/services/ai.js` 已恢复 `gpt-5.5` 默认模型、`AI_MAX_RETRIES`、fallback endpoint、超时/网络/HTTP 错误分类与重试逻辑。
  - `backend/src/services/engineRegistry.js` 已恢复 RAG JSON/文本结构化解析、统一结果包装和 AI 不可用时的 RAG 降级兜底。
  - `backend/src/services/failover.js` 已恢复 AI 可用性错误识别、`rule-based-rag` 降级结果、统一 schema 包装和 degraded 日志。
  - 本批验证：`node --check backend/src/services/resultSchema.js backend/src/services/ai.js backend/src/services/engineRegistry.js backend/src/services/failover.js` 通过；`cd backend && npm run build` 通过。
- 高优先级路由层已继续恢复：
  - `backend/src/routes/douyinAgents.js` 已从硬编码浅输出恢复为知识库 + AI 主链路，覆盖体检诊断、组品定价、内容策划、脚本生成、数据诊断、投流测算、完整战略 7 个抖音智能体。
  - 抖音路由已接入 `getKBContextWithMeta()`、`generateStructured()`、`getMaxTokensForLevel()`、`getTemperatureForTool()` 和 `rule-based-rag` fallback，返回 `engineType=knowledge-ai`、`degraded`、`meta.kbFilesUsed/kbFilesMissing/kbSectionsMissing`。
  - `backend/src/routes/generate.js` 已恢复内容/营销知识库 AI 工具注册，新增 `backend/src/tools/content.js`、`backend/src/tools/marketing.js`、`backend/src/services/promptBuilder.js`。
  - `generate.js` 已补回 `getActorId()`、`hasPersistedUser()`、`normalizeToolPayload()`，支持 guest mode 下跳过持久化用户埋点/使用记录，并对 `friend/topic/festival/fission/marketing-plan` 做输入默认值归一。
  - 本批验证：`node --check backend/src/routes/douyinAgents.js backend/src/routes/generate.js backend/src/tools/content.js backend/src/tools/marketing.js backend/src/services/promptBuilder.js` 通过；`cd backend && npm run build` 通过。
- `meituan` 双链路已完成收口：
  - `frontend/src/views/tools/MeituanDiagnoser.vue` 已移除前端本地诊断计算，统一调用 `generateTool('meituan')`。
  - `backend/src/routes/generate.js` 的 `meituan` template 已补齐原前端评分、问题诊断、优化建议、行动清单、行业对标和风险提示。
  - `normalizeToolPayload()` 已为 `meituan` 归一 `monthlyOrders/monthlySales/avgOrderValue/platformRate/platformFeeRate/repurchaseRate/repeatRate/issues`，兼容前端现有字段与旧 template 字段。
- `festival`、`fission`、`marketing-plan` 输出结构已继续对齐：
  - `backend/src/tools/marketing.js` 为实际生效定义，`generate.js` 内部同名旧定义会被 `createMarketingTools()` 覆盖。
  - `festival` 保持 RAG 主链路和结构化提示词。
  - `fission` 保持 RAG 主链路，并补齐 template 兜底里的活动前提、预算拆分、传播路径、执行节奏、关键指标、风控边界和 3 条行动清单。
  - `marketing-plan` 保持 RAG 主链路，并补齐 template 兜底里的渠道动作、执行排期、复盘指标、风险边界和 4 条行动清单。
- 计算器执行链路已接入轻量 KB 增强解释：
  - `backend/src/services/calculatorEngine.js` 仍保持纯 JS 确定性计算，不引入 LLM 调用。
  - 计算完成后通过 `getKBContextWithMeta(toolCode, memberLevel, formData)` 读取 `kb-mapping.json` 对应知识库片段。
  - 当命中知识库内容时，结果追加“知识库参考”章节，并在 `meta/extra` 中写入 `kbEnhanced/kbFilesUsed/kbContextChars/kbRetrievalMode`。
  - 当前增强为轻量引用层，不改变原有公式、阈值和行动建议。
- 诊断报告页展示协议已收口：
  - `frontend/src/views/DiagnosisReport.vue` 当前只消费 `history.state.result`，未保留旧的 `fetch('/api/diagnosis/analyze')` 调用。
  - 风险等级样式已改为优先读取 `level/severity/priority/type` 结构化字段，并兼容历史文本中的“紧急/重要”。
  - 页面提示图标已改为纯文本标识，源码中已清理风险等级 emoji 字面量。
  - 本批验证：`cd frontend && npm run build` 通过。
- 首页 Hero 会员统计口径已收口：
  - `frontend/src/views/Home.vue` 已移除硬编码 `targetMemberCount = 867` 和对应动画递增逻辑。
  - 首页会员统计改为“本站会员持续增长中 / 持续增长”的非精确表达，避免在无公开统计接口时展示伪精确数字。
  - 本批验证：`cd frontend && npm run build` 通过。
- 前端 Vite 动态/静态重复导入警告已收口：
  - `frontend/src/api/request.js` 将 401 拦截器里的动态 `import('@/router')` 改为静态导入 `router`。
  - `frontend/src/main.js` 与请求拦截器现在使用同一静态 router 模块引用。
  - 本批验证：`cd frontend && npm run build` 通过，`router/index.js is dynamically imported...` 警告已消失。
- 前端大 chunk 警告已收口：
  - `frontend/src/views/ToolPage.vue` 将所有工具/表格组件从静态导入改为 `defineAsyncComponent()` + 动态 import，避免单个工具详情入口打包进 100+ 页面组件。
  - `frontend/src/views/xhs/AccountDiagnosisAgent.vue` 将完整 `echarts` 改为 `echarts/core` + `RadarChart` + `RadarComponent` + `CanvasRenderer` 按需注册。
  - 本批验证：`cd frontend && npm run build` 通过，Vite 大 chunk 警告已消失；`ToolPage` 输出约 29KB，`AccountDiagnosisAgent` 输出约 393KB。
- 进度文档过期待办标题已收口：
  - 将已被后续记录覆盖的过期待办标题统一改为“当前状态”或“当前状态与后续约束”。
  - 当前文档中不再保留误导性的待办标题，避免后续 Agent 把已完成事项当成待办。
- 环境变量文件安全治理状态已复核：
  - `git ls-files "backend/.env"` 当前无输出，说明真实环境变量文件已不再被 Git 跟踪。
  - `git check-ignore -v --no-index "backend/.env"` 命中 `.gitignore:7:**/.env`，说明后续本地真实环境变量文件会继续被忽略。
  - `git status --short -- "backend/.env" "backend/.env.example" ".gitignore"` 中 `D  backend/.env` 是索引删除状态，本地文件仍按忽略规则保留。
  - 密钥轮换清单：依次轮换 AI 服务密钥、JWT 密钥、数据库凭据、支付相关密钥、CRON 或内部任务密钥；轮换后同步更新部署环境变量，并重启后端服务做健康检查。
- 后端关键文件最终语法验证已通过：
  - 已对 `generate.js`、`xhsAgents.js`、`douyinAgents.js`、`ai.js`、`calculatorEngine.js`、`diagnosisEngineV3.js`、`engineRegistry.js`、`failover.js`、`promptBuilder.js`、`resultSchema.js`、`content.js`、`marketing.js` 执行 `node --check`。
  - `cd backend && npm run build` 通过；当前后端 `build` 脚本为 `No build step needed for Node.js`。
- 全量工具专业度深度测试后的架构审查已补充：
  - 小红书、抖音、私域被确认为高阶功能，长期应走统一 Knowledge-AI 执行能力，即知识库检索、AI 生成、结构化解析、fallback、日志和 meta 统一治理。
  - 统一 Knowledge-AI 执行能力不等于立刻强制统一所有业务字段；前端当前大量页面直接消费领域结构，如 `result.radar`、`recommendedTiers`、`phases`、`retentionCalendar` 等。
  - 短中期推荐采用“双层结构”：外层补齐 `status/degraded/summary/sections/actions/riskNotes/meta/customizationCTA`，内层保留 `result` 领域专用字段，保证前端逐页迁移。
  - 对 G/H/I/J 计算器测试 payload 与 `calculatorEngine.js` 输入协议做只读比对，确认大量 FAIL 来自字段名不匹配。后续应先修测试 payload 或增加输入别名归一，再判定真实未实现工具。

### 2026-05-15 严格代码与架构审查 + P0 修复
- 已新增正式审查文档：`docs/严格代码与架构审查报告_2026-05-15.md`。
- 本轮审查范围覆盖统一结果协议、执行引擎、专项路由、知识库接入层、深测脚本与线上运行日志。
- 审查结论：当前版本已达到可内测上线状态，但统一架构仍处于第一阶段，尚未完全收敛为单一路由层、单一知识库接入层和单一领域载荷规范。
- 已识别两个 P0 级问题并启动修复：
  - `backend/src/routes/douyinAgents.js` 中抖音专项存在 `result.result` 双层嵌套风险，已改为先提取领域载荷，再统一进入 `createDomainToolResult()`，避免继续生成双层 `result`。
  - `xhsAgents.js`、`douyinAgents.js`、`privateAgents.js`、`membership.js` 中 JWT secret 获取口径不一致，已统一改为通过 `backend/src/middleware/auth.js` 导出的 `getJwtSecret()` 读取，移除各路由硬编码默认 secret 分叉。
- 本轮新增抖音领域载荷提取函数 `getDomainPayload()`，分别对 `diagnosis / product_pricing / content_planner / script_generator / data_diagnoser / ad_calculator / full_strategy` 做领域结构归一。
- 本轮验证：
  - `node --check backend/src/routes/douyinAgents.js`
  - `node --check backend/src/routes/xhsAgents.js`
  - `node --check backend/src/routes/privateAgents.js`
  - `node --check backend/src/routes/membership.js`
  - `node --check backend/src/middleware/auth.js`
  - `cd backend && npm run build`
  - 上述检查均已通过。
- 源码搜索已确认后端当前不再存在 `result.result` 生成点。
- 本地关键结构回归已完成：
  - `POST /api/douyin/diagnosis` 已确认从历史 `result.result.radarData` 收敛为 `result.radarData`
  - `POST /api/douyin/product-pricing` 已确认领域层为 `result.products`
  - `POST /api/private/diagnosis` 与 `POST /api/xhs/account-diagnosis` 结构保持正常
- 本轮修复已同步线上：
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515084000/pre-deploy-backup.tgz`
  - 修复包：`/tmp/woying-ai-deploy-20260515084000.tgz`
  - `pm2 restart woying-backend` 后服务保持 `online`
- 线上关键复测已完成：
  - `/api/douyin/diagnosis` 返回 `status=ok`、`degraded=false`、`meta.engineType=knowledge-ai`，领域层为 `result.radarData/result.diagnosis/result.suggestions`
  - `/api/douyin/product-pricing` 返回 `status=ok`、`degraded=false`、`meta.engineType=knowledge-ai`，领域层为 `result.products/result.warnings/result.upgradeHint`
  - 已确认线上抖音专项不再生成 `result.result.*` 双层结构
- `backend/scripts/deep-test-runner.js` 已完成结构兼容识别扩展：
  - `detectStructureCompat()` 新增识别 `radarData/products/topics/script/analysis/upgradePath/retentionCalendar/implementationTimeline`
  - 新增 `detectDomainPayloadSignals()`，用于让深测脚本识别更多高阶领域载荷，而不是只认传统 `radar/phases/diagnosis`
  - `hasBenchmarks` 与 `benchmarks` 判断已兼容 `radarData` 和更多领域信号，减少对抖音/AI 工具的误判
- 本轮验证：
  - `node --check backend/scripts/deep-test-runner.js` 通过
  - 小范围样本运行时出现 `fetch failed`，原因为本地测试服务不可达，属于环境可达性问题，不是脚本语法或结构判断问题
- `privateAgents.js` 已完成第一阶段知识库主干迁移：
  - 新增 `getPrivateKbMeta()`，统一通过 `getKBContextWithMeta()` 读取 `kbToolCode/kbFilesUsed/kbContextChars/kbRetrievalMode`
  - `diagnosis / member-design / retention-plan / fission-plan / community-sop / cac-ltv / full-strategy` 七个私域接口已全部接入统一 KB meta 主干
  - 当前保留现有规则生成逻辑与本地文件片段提取逻辑，优先收口知识库可观测性、会员切片和统一 meta 口径
- `backend/src/config/kb-mapping.json` 已新增七个私域专项映射：
  - `private-diagnosis`
  - `member-design`
  - `retention-plan`
  - `fission-plan`
  - `community-sop`
  - `cac-ltv`
  - `full-strategy`
- 本轮验证：
  - `node --check backend/src/routes/privateAgents.js` 通过
  - `node --check backend/src/services/kbService.js` 通过
  - 源码确认七个私域接口均已写入 `meta: kbMeta`
- `privateAgents.js` 的旁路读取已继续收缩：
  - `loadKB()` 已改为带 5 分钟 TTL 的进程级缓存，避免每次请求都重新扫描 `07_私域运营专项库` 全目录
  - 新增 `summarizePrivateKbLibrary()`，统一生成 `full-strategy` 所需的私域知识库摘要与文件统计，减少路由内重复遍历逻辑
  - 当前阶段仍保留局部文件直读与脚本片段抽取，下一阶段继续逐步下沉到统一 `kbService` 能力
- 本轮补充验证：
  - `node --check backend/src/routes/privateAgents.js` 在缓存化调整后继续通过
- `privateAgents.js` 的脚本/话术片段访问口径已进一步收口：
  - 新增 `getPrivateKbDoc()`，统一按 `category + name` 读取缓存后的私域知识文档
  - 新增 `extractSnippet()`，统一执行正则片段抽取并提供 fallback
  - 新增 `hasSnippetKeyword()`，统一执行关键词存在性判断
  - `member-design / retention-plan / fission-plan / community-sop` 中原先散落的 `kb.scripts[...]`、`kb.sops[...]` 和 `match/includes` 组合已改为走这些辅助函数
- 本轮补充验证：
  - `node --check backend/src/routes/privateAgents.js` 在辅助函数收口后继续通过
- `privateAgents.js` 的 KPI 提取旁路已继续收口：
  - 新增 `getIndustrySceneDocs()`，统一按行业筛选包含“核心数据指标”的场景文档
  - 新增 `parseMarkdownTableMap()`，统一解析 Markdown 表格到键值映射
  - `parseKPIFromKB()` 已缩减为组合上述辅助函数，移除路由内联表格解析细节
  - 本轮补充验证：`cd backend && node --check src/routes/privateAgents.js` 通过
- 线上 `degraded=true` 生成类工具样本已完成一轮日志归因：
  - 基于 `pm2 logs woying-backend --lines 300 --nostream`，当前主因集中为输入缺参后触发 failover 降级
  - 典型错误包括：`marketing-budget` 缺少 `totalBudget/goal`、`retention-rate` 缺少 `startCustomers/newCustomers/endActive/dormantDays`、`ltv-education` 缺少 `hourlyFee/monthlyHours/extraIncomePct/cac`
  - 当前日志中未见大批量 AI 网关故障主导的 `degraded=true`，生成类降级主要集中在计算器/经营分析工具的输入协议不匹配
  - 治理优先级应放在逐工具核对 `CALCULATORS.inputs`、前端表单字段与 smoke/deep-test payload 的一致性
- 后续高优先级事项：
  - 将 `privateAgents.js` 中剩余的目录扫描与文件直读逻辑继续收缩，逐步改为统一从 `kbService` 供给内容。
  - 对线上 `degraded=true` 的生成类工具按输入协议逐一治理，并补齐对应 smoke/deep-test 样本。
- 已新增“最后一次上线前全局精细化检查与修复计划”文档：
  - `docs/上线前最后一次全局精细化检查与修复计划_2026-05-15.md`
  - 计划已明确本轮最后一次全局修复的范围、风险分级、修复批次、备份策略、回测矩阵与回滚条件。
  - 当前建议执行顺序：先做 `generate` 工具输入协议全局治理，再做私域剩余旁路收口，最后执行本地与线上严格回测。
- 批次 A“输入协议全局治理”已完成第一轮落地：
  - 新增共享归一化模块：`backend/src/services/toolPayloadNormalizer.js`
  - `backend/src/routes/generate.js` 已改为复用共享 `normalizeToolPayload()`，不再只在路由内部维护协议兼容逻辑
  - `backend/scripts/deep-test-runner.js` 已切换为使用同一套 `normalizeToolPayload()` 做输入校验，避免“运行时已兼容、深测仍报 INPUT_MISMATCH”的口径分裂
  - 已为以下 5 个工具补齐旧字段名到当前协议的兼容映射：
    - `retention-rate`
    - `marketing-budget`
    - `churn-rate`
    - `ltv-education`
    - `promotion-profit`
  - 已新增旧协议样本：`backend/scripts/test-payloads-j-legacy.json`
  - 本轮验证：
    - `node --check backend/src/routes/generate.js backend/src/services/toolPayloadNormalizer.js backend/scripts/deep-test-runner.js` 通过
    - `TEST_FILE=./scripts/test-payloads-j-legacy.json BASE_URL=http://127.0.0.1:3001 node scripts/deep-test-runner.js` 中旧协议样本已不再出现 `INPUT_MISMATCH`
    - 当前剩余失败原因为本地 `3001` 服务不可达导致 `fetch failed`，属于环境可达性问题，不是协议兼容逻辑失效
  - 本轮已完成线上最小同步与回测：
    - 线上备份：`/home/ubuntu/woying-ai/backups/deploy-20260515101850/pre-deploy-backup.tgz`
    - 本机 `npm run smoke:core` 通过，核心接口均 `degraded=false`
    - 公网 `STRICT_DEGRADED=true BASE_URL=http://124.223.3.175 HEALTH_PATH=/api/health npm run smoke:core` 通过
    - 旧协议样本 `backend/scripts/test-payloads-j-legacy.json` 在线上本机回测结果为 `4 PASS, 1 FAIL, 0 INPUT_MISMATCH, 0 DEGRADED`
    - `J11 promotion-profit` 的 `FAIL` 原因为深测评分仅 7 分，接口本身返回 `http=200`、`degraded=false`
    - 最新线上日志已出现本轮 5 个目标工具的新请求记录，均为 `degraded=false`

### 当前状态与后续约束
- 工具、KB、前端降级提示差异已完成多轮收口，后续按具体工具继续一对一复查。
- 首页 Hero 当前使用非精确会员增长表达；如需展示真实会员数，可补公开统计接口后再接入。
- 前端构建中已知 Vite 动态导入警告和大 chunk 警告均已完成本轮收口。
- `backend/.env` 已从 Git 索引移除且本地文件保留；后续需要按上方清单轮换该文件历史中出现过的真实密钥。
- 小红书、抖音、私域统一治理时应先抽象 Knowledge-AI 共享执行层，再做响应兼容层，避免直接改旧路由导致专项页面断裂。
- G/H/I/J 深度测试结论需要基于输入协议修正后重新评估，当前低分不能直接等同于工具未实现。
- 当前预览地址：
  - `https://5173-5ab6a3cb51458057.monkeycode-ai.online`

### 当前状态
- `frontend/src/views/DiagnosisReport.vue` 基于 emoji 文本标记的风险等级判断已收口为结构化字段优先、纯文本兼容。
- 首页 Hero 静态会员数已改为非精确表达；后续如需要展示真实会员数，可补公开统计接口后再接入。
- 首页、工具箱、行业页中用于引导用户操作的说明式辅助文案已进一步删除，当前页面表达以标题、入口和数据为主。

## 2.2.3 2026-05-02 诊断链路收口记录

### 已完成
- `frontend/src/constants/membership.js`
  - 恢复 `canAccessLevel()` 的真实等级比较逻辑，移除“开发阶段全部放开”的临时代码。
- `frontend/src/views/Diagnosis.vue`
  - 诊断入口现在统一要求先登录，再根据会员等级决定是否可进入具体问卷。
  - 行业诊断卡片的锁定态已与真实权限判断对齐，不再只判断是否登录。
- `frontend/src/views/DiagnosisQuestionnaire.vue`
  - 现已支持双模式：
    - `growth-diagnosis` 继续走三阶段增长诊断流程
    - `store-health / restaurant-health / education-health / beauty-health` 改为动态拉取模板题目并提交到 `/api/diagnosis/analyze`
  - 加入模板加载失败后的真正重试逻辑。
- `frontend/src/views/DiagnosisReport.vue`
  - 修正统一报告模式下 `dimensionRank` 对对象结构的渲染错误，避免显示成 `[object Object]`。
  - 推荐工具入口改为显示工具名称，不再直接展示工具 code。
- `backend/src/routes/diagnosis.js`
  - 模板列表、模板详情、提交分析 3 个接口已接入模板级会员校验。
  - 非法模板或越权访问会返回明确的 `404/403`。

### 本轮验证
- 前端构建通过：
  - `cd /workspace/frontend && npm run build`
- 后端语法检查通过：
  - `cd /workspace/backend && node --check src/routes/diagnosis.js`

### 当前状态
- `frontend/src/views/DiagnosisReport.vue` 已确认当前报告页只消费路由 state 中的统一结果，旧的 `fetch('/api/diagnosis/analyze')` 调用已不存在。
- 首页 Hero 的静态 `867` 已移除，当前用非精确增长表达替代；真实统计可在公开统计接口补齐后接入。

## 2.2.4 2026-05-02 普通工具会员权限后端收口记录

### 已完成
- `backend/src/config/toolAccess.js`
  - 新增后端共享工具权限配置，统一维护：
    - 会员等级顺序
    - `toolCode -> requiredLevel` 映射
    - 徽标元数据（`badge` / `badgeClass`）
- `backend/src/routes/generate.js`
  - `POST /api/generate/:toolCode` 现在会先读取用户会员等级，再按共享权限配置做后端拦截。
  - 已修复“前端锁住但后端仍可直接调用高阶工具”的越权风险。

## 2.2.5 2026-05-03 前端白屏排查记录

### 已定位根因
- `frontend/src/constants/toolCatalog.js`
  - 顶层派生值 `capabilityCount`、`toolCountsByLevel` 以及 `douyinOperationTools` 依赖 `strategyCapabilityTools`。
  - 该变量此前只有引用、没有定义，导致首页或工具箱首屏导入 `toolCatalog.js` 时在浏览器运行期直接触发 `ReferenceError`，表现为预览白屏。

### 已修复
- `frontend/src/constants/toolCatalog.js`
  - 已补充 `strategyCapabilityTools` 集合，当前纳入：
    - `douyin-growth`
    - `boss-ip`
    - `xiaohongshu-growth`
  - 这些高阶专项继续走 `/tools/:code` 详情页体系，供目录统计、配额查询和详情页 `getToolByCode()` 统一复用。

### 验证方式
- 前端构建：`cd /workspace/frontend && npm run build`
- 重点验证页面：
  - `/`
  - `/tools`
  - `/douyin`
  - `/tools/boss-ip`
  - `/tools/xiaohongshu-growth`

### 当前验证结果
- 前端已重新构建成功。
- 静态首页当前引用的新入口脚本为 `/assets/index-DLia0mx4.js`。
- 关键资源 `/assets/index-DLia0mx4.js` 与 `/assets/toolCatalog-D5rWt4Dn.js` 均可正常返回 `200`。
- 当前环境缺少可用的浏览器级控制台能力，若用户侧仍见白屏，需要继续围绕最新 bundle 做下一轮运行时排查。

## 2.2.6 2026-05-03 企业增长模块命名口径

### 用户确认的统一命名
- 分类名：`增长顾问`
- 首页/工具箱入口名：`企业增长`
- 完整模块名：`企业增长全景顾问`

### 已开始替换的范围
- `frontend/src/constants/toolCatalog.js`
  - 独立能力 `diagnosis`
  - 诊断分类 `pillarMeta.diagnosis`
  - 首页/工具箱专项入口 `specialModuleEntries`
  - 高阶能力卡 `advancedCapabilityCards`
- `frontend/src/views/Diagnosis.vue`
- `frontend/src/views/DiagnosisHistory.vue`
- `frontend/src/views/DiagnosisReport.vue`
- `frontend/src/views/DiagnosisQuestionnaire.vue`
- `frontend/src/components/NavBar.vue`
- `frontend/src/components/Footer.vue`
- `frontend/src/views/Home.vue`
- `backend/src/routes/generate.js`
- `backend/src/services/aiDiagnosis.js`

### 命名边界
- 独立 growth skill 走 `企业增长全景顾问` 体系。
- 模板型 `门店健康度诊断 / 餐饮门店健康度诊断 / 校区健康度诊断 / 美业门店健康度诊断` 保持原名，不并入该体系。

### 当前验证结果
- `frontend/src` 中面向用户的旧称呼 `企业诊断 / 企业增长诊断 / 企业增长综合诊断` 已清理。
- 前端构建验证通过：`cd /workspace/frontend && npm run build`。
- `backend/src/routes/tool.js`
  - `/api/tools/quotas` 与 `/api/tools/:code/quota` 现在会对无权限工具返回锁定态额度，而不再错误回退到免费额度。
  - `/api/tools/:code/run` 现在也会按共享权限配置返回 `403`，避免通过旧工具接口绕过权限。
  - `/api/tools` 工具列表的 badge 已改为基于共享权限配置生成，修复部分工具原先与真实等级不一致的问题。

### 本轮验证
- 后端语法检查通过：
  - `cd /workspace/backend && node --check src/config/toolAccess.js`
  - `cd /workspace/backend && node --check src/routes/generate.js`
  - `cd /workspace/backend && node --check src/routes/tool.js`

### 当前状态与后续约束
- 本节原记录的小红书专题子工具元数据同源化问题，已在后续“前端工具页元数据同源化补充”中收口。
- 后续新增不在主 `toolCatalog` 中的专题工具页时，仍需同步检查是否显式传入 `requiredLevel` 或接入统一目录元数据。

## 2.2.7 2026-05-03 前台主入口收口方向

### 用户确认的新结构
- 顶部主导航收口为：`首页 / 表格中心 / 会员中心`
- `行业专版` 不再作为主导航与首页重点主入口
- 原 `/tools` 页面改造成 `表格中心`
- `企业增长全景顾问` 保留在首页作为独立重点能力展示

### 实施策略
- `frontend/src/views/Home.vue`
  - 保留首页的模块入口能力，但降低行业入口权重，把 `企业增长全景顾问` 提升为首页主推卡片。
  - 模块卡片优先跳转 `/modules/:id`，避免和新的 `表格中心` 定位冲突。
- `frontend/src/views/Tools.vue`
  - 不再继续承载模块工作台，改为复用 `industryTemplateEntries` 的模板型表格中心。
  - 支持按行业与模板类型筛选，统一承接经营记录、输入模板和输出报表。
- `frontend/src/components/NavBar.vue`
  - 主导航删除 `行业专版` 与 `企业增长` 直达入口，只保留三项主结构。

### 当前落地结果
- `frontend/src/components/NavBar.vue`
  - 已收口为 `首页 / 表格中心 / 会员中心`。
- `frontend/src/views/Home.vue`
  - Hero 主 CTA 已切到 `/tools` 的 `表格中心`，次 CTA 指向 `/diagnosis`。
  - 模块卡片已改跳 `/modules/:id`，避免继续把 `/tools` 当模块工作台。
  - 首页新增独立的 `企业增长全景顾问` 主推卡，并保留降级后的 `行业场景` 区块。
- `frontend/src/views/Tools.vue`
  - 已整体改为表格中心：复用 `industryTemplateEntries`，支持按行业与 `输入模板 / 经营记录 / 输出报表` 筛选。
  - 页面保留专项能力入口和企业增长入口，但不再承担 8 大模块工作台职责。

### 验证结果
- 前端构建再次通过：
  - `cd /workspace/frontend && npm run build`

### 后续补充
- `frontend/src/views/tools/MeituanDiagnoser.vue`
  - 已把错误的 `annual` 页面权限和 badge 改回 `pro / 进阶`，与当前工具目录和后端权限口径一致。
- 小红书专项工具页
  - 已为 `xhs-title / xhs-topic / xhs-traffic / xhs-seo / xhs-diagnosis / xhs-review / xhs-conversion` 显式补齐 `requiredLevel`。
  - 已修复这些页面因不在主 `toolCatalog` 中而默认回退到 `free` 的前端访问误判风险。
- `frontend/src/views/tools/CompetitorAnalyzer.vue`
- `frontend/src/views/tools/BusinessPlanGenerator.vue`
  - 已把错误显示为“高阶”的 badge 改回“进阶”，与实际 `pro` 层级一致。

### 增量验证
- 前端构建再次通过：
  - `cd /workspace/frontend && npm run build`
- 后端语法检查再次通过：
  - `cd /workspace/backend && node --check src/routes/diagnosis.js`

### 收口补充
- `backend/src/routes/diagnosis.js`
  - 已切换为复用 `backend/src/config/toolAccess.js` 中的共享权限判断，不再单独维护一套会员等级顺序。

## 2.2.5 2026-05-02 前端工具页元数据同源化补充

### 已完成
- `frontend/src/constants/toolCatalog.js`
  - 新增 `xhsOperationTools` 与 `getXhsOperationTool()`，把小红书专题子工具的 `name / description / requiredLevel / badge / badgeClass / path` 收口到单一来源。
- `frontend/src/views/tools/XhsOperations.vue`
  - 已移除本地 `toolAccessMeta` 和手写工具卡片数组，改为直接读取 `xhsOperationTools`。
- `frontend/src/views/tools/XhsTitle.vue`
- `frontend/src/views/tools/XhsTopic.vue`
- `frontend/src/views/tools/XhsTraffic.vue`
- `frontend/src/views/tools/XhsSEO.vue`
- `frontend/src/views/tools/XhsDiagnosis.vue`
- `frontend/src/views/tools/XhsReview.vue`
- `frontend/src/views/tools/XhsConversion.vue`
  - 上述小红书专题子工具页已改为通过 `getXhsOperationTool(code)` 读取统一 `toolInfo`，不再在页面内重复维护 badge 和会员等级。
- 以下普通工具页也已改为通过 `getToolByCode(code)` 读取主目录元数据，修复本地 badge 残留错位或英文 `Pro` 文案：
  - `frontend/src/views/tools/MarketingPlan.vue`
  - `frontend/src/views/tools/AnniversaryEvent.vue`
  - `frontend/src/views/tools/StoreOpening.vue`
  - `frontend/src/views/tools/OffseasonTraffic.vue`
  - `frontend/src/views/tools/EmployeeIncentive.vue`
  - `frontend/src/views/tools/ComplaintHandling.vue`
  - `frontend/src/views/tools/TeamTraining.vue`
  - `frontend/src/views/tools/ExperienceService.vue`
  - `frontend/src/views/tools/FestivalMarketing.vue`
  - `frontend/src/views/tools/SOPGenerator.vue`
  - `frontend/src/views/tools/SalaryDesigner.vue`
  - `frontend/src/views/tools/TopicGenerator.vue`
  - `frontend/src/views/tools/FissionScheme.vue`
  - `frontend/src/views/tools/PromotionPlan.vue`
  - `frontend/src/views/tools/PriceIncrease.vue`
  - `frontend/src/views/tools/CompetitorStrategy.vue`
  - 并已继续扩展到其余普通工具页，包括 `CompetitorAnalyzer.vue`、`BusinessPlanGenerator.vue`、`MeituanDiagnoser.vue`、`ROICalculator.vue`、`PaybackCalculator.vue`、`HeadlineGenerator.vue`、`FriendPoster.vue`、`XiaohongshuGenerator.vue`、各行业计算器页等剩余页面；当前 `frontend/src/views/tools/` 中已无本地 `const toolInfo = {}` 残留。

### 本轮验证
- 前端构建通过：
  - `cd /workspace/frontend && npm run build`

### 当前状态
- `frontend/src/views/tools/` 的普通工具页 `toolInfo` 已基本切到 `getToolByCode()` / `getXhsOperationTool()` 同源读取；后续若再新增独立工具页，应直接沿用该模式，不要回到页面本地手写 badge 和等级。
- `frontend/src/views/DiagnosisReport.vue` 已删除旧增长诊断回放分支与过期的二次分析请求，当前只保留统一报告渲染入口；残留旧样式已在后续“抖音专题工作台落地补充”中继续清理。

## 2.2.6 2026-05-02 抖音/小红书专项结构审查补充

### 已完成
- `frontend/src/views/tools/XhsOperations.vue`
  - 修复小红书专题页场景卡片图标未渲染问题：原先 `icon` 字段传的是字符串名，不是实际 Vue 组件。
  - 现已改为直接使用 `@/icons` 中的 SVG 组件，专题页场景入口可正常显示图标。
- `frontend/src/constants/toolCatalog.js`
  - `小红书运营` 专项入口已统一改为跳转 `/tools/xhs-ops`，不再回落到普通 `/tools?pillar=xiaohongshu` 筛选页。
  - `advancedCapabilityCards` 中的小红书专项入口也同步改为 `/tools/xhs-ops`，保持首页、工具箱和专题页承接一致。

### 本轮验证
- 前端构建通过：
  - `cd /workspace/frontend && npm run build`

### 审查结论
- 小红书专项当前已经具备独立聚合页、子工具目录、后端生成定义、会员等级映射与额度配置，基础闭环已成立。
- 抖音专项当前仍主要依赖 `/tools?pillar=douyin` 的模块筛选承接，尚未形成与 `XhsOperations.vue` 对等的独立专题工作台。
- 后端已存在 `douyin-growth / xiaohongshu-growth / boss-ip` 的能力定义与额度配置，但前端目前没有看到对应的独立表单式执行入口，仍属于后续可继续收口的结构缺口。

### 当前状态
- `抖音经营` 的独立专题工作台已在后续“抖音专题工作台落地补充”中完成。
- `frontend/src/views/Tools.vue` 顶部额度卡文案已改为“按当前会员等级展示可用范围”，与真实权限逻辑对齐。

## 2.2.7 2026-05-02 抖音专题工作台落地补充

### 已完成
- `frontend/src/views/tools/DouyinOperations.vue`
  - 新增抖音专题工作台页面，结构与小红书专题页同级。
  - 当前聚合页只接真实已存在的工具：`headline / hook / script / topic / roi / meituan / ip-agent`。
  - 场景入口按“内容起量 / 直播表达 / 投流测算 / 平台经营 / 长期选题”组织。
- `frontend/src/router/index.js`
  - 新增路由 `/tools/douyin-ops`。
- `frontend/src/constants/toolCatalog.js`
  - 新增 `douyinOperationTools`，从主工具目录统一派生抖音专题工具列表。
  - `抖音经营` 的专项入口与高阶能力入口已统一切到 `/tools/douyin-ops`。
  - `capabilityCards` 中旧的 `/tools?category=douyin` 入口也已改为专题页。
- `frontend/src/views/Tools.vue`
  - 将顶部额度卡中的旧文案“当前环境已放开工具权限展示”改为“按当前会员等级展示可用范围”，与现有真实权限口径对齐。

### 本轮验证
- 前端构建通过：
  - `cd /workspace/frontend && npm run build`

### 当前状态
- `抖音经营` 与 `小红书运营` 现在都已有独立专题聚合页承接，专项入口层级更一致。
- `douyin-growth` 后端能力定义仍存在，但前端暂未提供独立表单页；当前做法是先通过专题工作台承接真实可用工具，避免前台出现“有入口但不可执行”的伪能力。

### 补充收口
- `frontend/src/views/DiagnosisReport.vue`
  - 已继续删除旧报告分支遗留的未使用样式块，包括 `info-grid / radar-chart / ability-summary / scan-* / suggestions / ai-*` 等仅服务旧分支的 CSS。
  - 样式体积已明显下降，`DiagnosisReport` 产物 CSS 从约 `11.66 kB` 收口到约 `6.65 kB`。

### `.monkeycode/docs/membership-permission-architecture.md`
- 用途：沉淀正式会员权限结构、行业子板块层级建议、通用能力与场景能力的组织原则。
- 使用场景：
  - 前端权限展示调整
  - 后端会员权限配置
  - 知识库与表格挂载规划
  - 新对话或多 Agent 协作时快速恢复上下文

### `.monkeycode/docs/industry-tool-access-matrix.md`
- 用途：沉淀“行业 -> 子板块 -> 工具 -> 会员等级 -> 免费限次 -> 开发状态”的正式映射关系。
- 使用场景：
  - 行业专版继续补真实工具时快速查表
  - 新增计算器时统一确定会员层级
  - 后续把表格和知识库接入具体工具时确定挂载位置
  - 新对话或多 Agent 协作时快速恢复产品结构上下文

### `.monkeycode/docs/industry-template-matrix.md`
- 用途：沉淀“行业 -> 表格模板 -> 建议层级 -> 优先级 -> 挂载工具/场景”的正式映射关系。
- 使用场景：
  - 后续把表格模板纳入行业工作台时快速查表
  - 设计在线填写、导出、协作能力时统一口径
  - 新对话或多 Agent 协作时快速恢复表格结构上下文

### `.monkeycode/docs/template-integration-plan-v1.md`
- 用途：沉淀第一轮 6 张高价值表的共享数据结构、页面接入建议和实现顺序。
- 使用场景：
  - 行业页接入模板卡片时直接参考
  - 工具详情页后续接入关联模板时直接参考

## 2.2.27 2026-05-15 上线前最后一次全局修复：批次 B 私域知识库收口

### 已完成
- `backend/src/routes/privateAgents.js`
  - 新增 `getPrivateKbFileNames()`，统一读取 `theory / scenes / sops / scripts` 分类文件名。
  - 新增 `getSceneSummariesByIndustry()`，统一按行业筛选场景文件并生成摘要。
  - `summarizePrivateKbLibrary()` 已改为组合上述共享辅助函数，减少直接遍历 `kb` 目录镜像的重复逻辑。
  - 保留现有 `result` 领域结构和业务文案，只做最小函数收口与冗余清理。
- 本地验证
  - `cd /workspace/backend && node --check src/routes/privateAgents.js` 通过。
- 线上最小同步
  - 批次 B 最小部署包：`/tmp/woying-ai-deploy-20260515-batchB-private-kb.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515103233/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 线上回测
  - `/api/private/diagnosis`：`http=200`、`degraded=false`、`engineType=rule-based-knowledge`
  - `/api/private/full-strategy`：`http=200`、`degraded=false`、`engineType=rule-based-knowledge`
  - PM2 最新日志仅见对应接口 `POST ... 200`，未见新的 `Unhandled error`

### 当前状态
- 批次 A 输入协议治理与批次 B 私域收口均已上线并完成回测。
- `privateAgents.js` 当前已完成第二轮最小共享函数收口，剩余问题已从“直接旁路散落”收敛为“局部规则生成仍未完全下沉到统一 KB 供给层”。
- 当前线上版本处于可继续内测的稳定状态。

## 2.2.28 2026-05-15 上线前最后一次全局修复：批次 C 页面消费协议收口

### 已完成
- `frontend/src/views/douyin/DiagnosisAgent.vue`
  - 已移除本地拼装假结果的逻辑，改为真实调用 `/douyin/diagnosis`。
  - 已将后端 `result.radarData` 映射为页面消费用的 `result.radar`，补齐颜色与分数等级展示。
- `frontend/src/views/douyin/ProductPricingAgent.vue`
  - 已改为复用 `@/api/request` 调用 `/douyin/product-pricing`，统一 token 注入与错误处理。
- 以下私域页面已从原生 `fetch('/api/private/...')` 切到 `@/api/request`，统一响应与鉴权处理：
  - `PrivateDiagnosis.vue`
  - `MemberDesign.vue`
  - `RetentionPlan.vue`
  - `CACvsLTV.vue`
  - `FullStrategy.vue`
  - `CommunitySop.vue`
  - `ViralCampaign.vue`
  - `ActivityPlanner.vue`
  - `FissionPlan.vue`
  - `ReferralSystem.vue`
  - `RechargeDesign.vue`
  - `Reactivation.vue`
  - `ChurnPrevention.vue`
  - `FissionDesign.vue`
  - `TierPricing.vue`
  - `PrivateDashboard.vue`
  - `LoyaltyProgram.vue`
  - `EngagementBoost.vue`
- 本地验证
  - `cd /workspace/frontend && npm run build` 连续两轮通过。
  - `frontend/src/views/private/` 下已无对 `/api/private` 的原生 `fetch` 残留。
  - `frontend/src/views/douyin/` 下已无对 `/api/douyin` 的原生 `fetch` 残留。

### 当前状态
- 批次 C 第一阶段已完成：前端抖音/私域页面与统一结果协议、统一请求层的主要分叉已收平。
- 当前剩余的页面请求差异主要位于通用账号类页面，如 `Membership.vue`、`DiagnosisHistory.vue`，不属于本轮专项主阻断项。

## 2.2.29 2026-05-15 上线前最后一次全局修复：批次 C2 抖音诊断稳定性增强

### 已完成
- `backend/src/routes/douyinAgents.js`
  - 修复 `buildDiagnosisFallback()` 对 `painPoints` 对象输入的误判，改为统一拍平数组后再做关键词判断。
  - 强化 `/douyin/diagnosis` prompt，补充“只输出合法 JSON 对象、禁止 Markdown、禁止额外说明、分数字段必须为数字”的约束。
  - 为 `runKnowledgeAiAgent()` 增加一次解析失败重试：首次 AI 输出无法解析为 JSON 时，先重试一次，再进入 fallback。
- 本地验证
  - `cd /workspace/backend && node --check src/routes/douyinAgents.js` 通过。
- 线上最小同步
  - 批次 C2 最小部署包：`/tmp/woying-ai-deploy-20260515-batchC2-douyin-diagnosis.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515105705/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 线上稳定性回测
  - 对 `/api/douyin/diagnosis` 连续执行 8 次回测，结果为 `6 次 degraded=false`、`2 次 degraded=true`
  - 当前剩余降级原因集中为 `fallbackReason=ai_or_parse_failed`

### 当前状态
- 抖音诊断接口结构已稳定，页面消费链路已打通。
- 当前残余问题已收敛为 AI 输出偶发不可解析，属于模型输出稳定性问题，不再是页面协议、鉴权或固定代码逻辑错误。

## 2.2.30 2026-05-15 上线前最后一次全局修复：批次 C3 抖音 JSON 脏输出清洗

### 已完成
- `backend/src/routes/douyinAgents.js`
  - 为 `parseJsonObject()` 增加轻量清洗逻辑，处理常见 AI JSON 脏输出：
    - 去掉 ```json 代码块包裹
    - 替换中文引号
    - 去掉尾随逗号
- 本地验证
  - `cd /workspace/backend && node --check src/routes/douyinAgents.js` 通过。
- 线上最小同步
  - 批次 C3 最小部署包：`/tmp/woying-ai-deploy-20260515-batchC3-douyin-json-sanitize.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515110148/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 线上稳定性回测
  - 对 `/api/douyin/diagnosis` 再次连续执行 8 次回测，结果为 `7 次 degraded=false`、`1 次 degraded=true`

### 当前状态
- 抖音诊断链路的 `ai_or_parse_failed` 抖动已进一步下降。
- 当前问题已从“中度波动”收敛为“低频偶发波动”，可继续内测观察。

## 2.2.31 2026-05-15 上线前最后一次全局修复：批次 C4 结构化输出试点

### 已完成
- `backend/src/services/ai.js`
  - 为 `createChatCompletion()` 增加可选 `responseFormat` 支持。
  - 当上游对 `response_format` 返回 `400` 时，自动回退为普通请求重试，保持兼容性。
- `backend/src/routes/douyinAgents.js`
  - 仅为 `douyin/diagnosis` 试点接入 `responseFormat: { type: 'json_object' }`。
- 本地验证
  - `cd /workspace/backend && node --check src/services/ai.js src/routes/douyinAgents.js` 通过。
- 线上最小同步
  - 批次 C4 最小部署包：`/tmp/woying-ai-deploy-20260515-batchC4-structured-output.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515110655/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 线上稳定性回测
  - 对 `/api/douyin/diagnosis` 连续执行 8 次回测，结果为 `8 次 degraded=false`、`0 次 degraded=true`

### 当前状态
- 抖音诊断链路已从页面接线问题收敛到 AI 输出稳定性问题，并通过结构化输出试点进一步压到 `0/8` 降级。
- `response_format` 方案在当前环境对 `douyin/diagnosis` 试点有效，可作为后续其他 Knowledge-AI 路由的参考收口路径。

## 2.2.32 2026-05-15 上线前最后一次全局修复：批次 C5 结构化输出第一批推广

### 已完成
- 重新盘点推广目标后，确认 `xhs/account-diagnosis` 当前属于 `rule-based-knowledge` 路由，不经过 AI 主链路，因此不适合作为本轮结构化输出推广对象。
- `backend/src/routes/douyinAgents.js`
  - 新增 `STRUCTURED_OUTPUT_AGENTS` 集合，集中管理结构化输出试点路由。
  - 已将 `product_pricing` 纳入与 `diagnosis` 相同的 `responseFormat: { type: 'json_object' }` 试点路径。
  - 同步强化 `product-pricing` 的 JSON 输出提示词约束。
- 本地验证
  - `cd /workspace/backend && node --check src/services/ai.js src/routes/douyinAgents.js` 通过。
- 线上最小同步
  - 批次 C5 最小部署包：`/tmp/woying-ai-deploy-20260515-batchC5-structured-output-expand.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515111613/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 线上稳定性回测
  - 对 `/api/douyin/product-pricing` 连续执行 8 次回测，结果为 `8 次 degraded=false`、`0 次 degraded=true`

### 当前状态
- 结构化输出方案已在两个高价值 Knowledge-AI 路由上完成验证：
  - `douyin/diagnosis`：`8/8` 非降级
  - `douyin/product-pricing`：`8/8` 非降级
- 该方案已证明可以作为后续 Knowledge-AI 路由收口的标准推广路径。

## 2.2.33 2026-05-15 上线前最后一次全局修复：批次 D 结构化输出第二批推广

### 已完成
- 第二批候选盘点结论：
  - `douyin/content-planner` 输出形态最简单，只有 `topics + riskNotes`，fallback 结构稳定，适合优先推广。
  - `script-generator / data-diagnoser / full-strategy` 保留为后续候选。
- `backend/src/routes/douyinAgents.js`
  - 已将 `content_planner` 纳入 `STRUCTURED_OUTPUT_AGENTS`。
  - 同步强化 `content-planner` 的 JSON 输出提示词约束。
- 本地验证
  - `cd /workspace/backend && node --check src/routes/douyinAgents.js src/services/ai.js` 通过。
- 线上最小同步
  - 批次 D 最小部署包：`/tmp/woying-ai-deploy-20260515-batchD-content-planner-structured-output.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515113312/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 线上稳定性回测
  - 对 `/api/douyin/content-planner` 连续执行 8 次回测，结果为 `8 次 degraded=false`、`0 次 degraded=true`

### 当前状态
- 结构化输出方案已在 3 个高价值 Knowledge-AI 路由上完成验证：
  - `douyin/diagnosis`：`8/8` 非降级
  - `douyin/product-pricing`：`8/8` 非降级
  - `douyin/content-planner`：`8/8` 非降级
- 当前最合理的后续顺序是继续推广到：
  - `douyin/script-generator`
  - `douyin/data-diagnoser`
  - `douyin/full-strategy`

## 2.2.34 2026-05-15 上线前最后一次全局修复：批次 E 结构化输出第三批推广

### 已完成
- `backend/src/routes/douyinAgents.js`
  - 已将 `script_generator` 纳入 `STRUCTURED_OUTPUT_AGENTS`。
  - 同步强化 `script-generator` 的 JSON 输出提示词约束，要求只输出合法 JSON，且 `duration` 必须为数字。
- 本地验证
  - `cd /workspace/backend && node --check src/routes/douyinAgents.js src/services/ai.js` 通过。
- 线上最小同步
  - 批次 E 最小部署包：`/tmp/woying-ai-deploy-20260515-batchE-script-generator-structured-output.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515113848/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 线上稳定性回测
  - 对 `/api/douyin/script-generator` 连续执行 8 次回测，结果为 `8 次 degraded=false`、`0 次 degraded=true`
  - `result.script` 每次均存在，`duration` 稳定为数字 `30`

### 新发现
- `frontend/src/views/douyin/ScriptGeneratorAgent.vue` 当前仍是本地模板拼装页，尚未接入 `/api/douyin/script-generator` 真实接口。
- 当前批次 E 已完成后端结构化输出推广与线上稳定性验证；前端接线应作为独立后续批次处理。

### 当前状态
- 结构化输出方案已在 4 个高价值 Knowledge-AI 路由上完成验证：
  - `douyin/diagnosis`：`8/8` 非降级
  - `douyin/product-pricing`：`8/8` 非降级
  - `douyin/content-planner`：`8/8` 非降级
  - `douyin/script-generator`：`8/8` 非降级
- 当前最合理的后续顺序是继续推广到：
  - `douyin/data-diagnoser`
  - `douyin/full-strategy`
- 前端待补接线目标新增：
  - `frontend/src/views/douyin/ScriptGeneratorAgent.vue`

## 2.2.35 2026-05-15 上线前最后一次全局修复：批次 F 数据诊断结构化推广与批次 F2 结构修正

### 已完成
- `backend/src/routes/douyinAgents.js`
  - 已将 `data_diagnoser` 纳入 `STRUCTURED_OUTPUT_AGENTS`。
  - 同步强化 `data-diagnoser` 的 JSON 输出提示词约束。
- 批次 F 本地验证
  - `cd /workspace/backend && node --check src/routes/douyinAgents.js src/services/ai.js` 通过。
- 批次 F 线上最小同步
  - 批次 F 最小部署包：`/tmp/woying-ai-deploy-20260515-batchF-data-diagnoser-structured-output.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515114318/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 批次 F 首轮回测
  - 对 `/api/douyin/data-diagnoser` 连续执行 8 次回测，结果为 `8 次 degraded=false`、`0 次 degraded=true`
  - 但同时发现领域结构被拍平为 `result.viewRate/...`，未保留为 `result.analysis`，不利于后续前端真实接线。
- 批次 F2 结构修正
  - 修正 `getDomainPayload('data_diagnoser')`，恢复 `result.analysis` 包装。
  - 修正路由外层 `actions/riskNotes` 提取路径，改为从 `analysis` 中读取。
- 批次 F2 本地验证
  - `cd /workspace/backend && node --check src/routes/douyinAgents.js src/services/ai.js` 通过。
- 批次 F2 线上最小同步
  - 批次 F2 最小部署包：`/tmp/woying-ai-deploy-20260515-batchF2-data-diagnoser-shape-fix.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515114512/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 批次 F2 回测
  - 对 `/api/douyin/data-diagnoser` 再次连续执行 8 次回测，结果为 `8 次 degraded=false`、`0 次 degraded=true`
  - `result.analysis` 每次均存在，结构已恢复到统一预期

### 新发现
- `frontend/src/views/douyin/VideoDiagnoserAgent.vue` 当前仍是本地计算页，尚未接入 `/api/douyin/data-diagnoser` 真实接口。

### 当前状态
- 结构化输出方案已在 5 个高价值 Knowledge-AI 路由上完成验证：
  - `douyin/diagnosis`：`8/8` 非降级
  - `douyin/product-pricing`：`8/8` 非降级
  - `douyin/content-planner`：`8/8` 非降级
  - `douyin/script-generator`：`8/8` 非降级
  - `douyin/data-diagnoser`：`8/8` 非降级，且 `result.analysis` 结构已恢复
- 当前仅剩一个主要后端候选：
  - `douyin/full-strategy`
- 前端待补接线目标继续增加：
  - `frontend/src/views/douyin/ScriptGeneratorAgent.vue`
  - `frontend/src/views/douyin/VideoDiagnoserAgent.vue`

## 2.2.36 2026-05-15 上线前最后一次全局修复：批次 G 完整战略结构化推广

### 已完成
- `backend/src/routes/douyinAgents.js`
  - 已将 `full_strategy` 纳入 `STRUCTURED_OUTPUT_AGENTS`。
  - 同步强化 `full-strategy` 的 JSON 输出提示词约束。
- 本地验证
  - `cd /workspace/backend && node --check src/routes/douyinAgents.js src/services/ai.js` 通过。
- 线上最小同步
  - 批次 G 最小部署包：`/tmp/woying-ai-deploy-20260515-batchG-full-strategy-structured-output.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515114944/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 线上回测
  - 首轮 8 次回测结果：`7 次 degraded=false`、`1 次 degraded=true`
  - 二轮 12 次复查结果：`12 次 degraded=false`、`0 次 degraded=true`
  - `result.phases` 和 `result.upgradePath` 结构均保持正确

### 新发现
- `frontend/src/views/douyin/FullStrategyAgent.vue` 当前仍是本地拼战略页，尚未接入 `/api/douyin/full-strategy` 真实接口。

### 当前状态
- 抖音 Knowledge-AI 主路由的结构化输出推广已完成一轮收尾：
  - `douyin/diagnosis`：稳定
  - `douyin/product-pricing`：稳定
  - `douyin/content-planner`：稳定
  - `douyin/script-generator`：稳定
  - `douyin/data-diagnoser`：稳定，且 shape 已校正
  - `douyin/full-strategy`：结构正确，存在低频尾部抖动，但二轮复查已恢复 `12/12` 非降级
- 当前后续重点已经从“后端结构化输出推广”转到“前端真实接线补齐”，目标包括：
  - `frontend/src/views/douyin/ScriptGeneratorAgent.vue`
  - `frontend/src/views/douyin/VideoDiagnoserAgent.vue`
  - `frontend/src/views/douyin/FullStrategyAgent.vue`

## 2.2.37 2026-05-15 上线前最后一次全局修复：抖音前端真实接线补齐

### 已完成
- `frontend/src/views/douyin/ScriptGeneratorAgent.vue`
  - 已切换为通过 `@/api/request` 调用 `/douyin/script-generator`。
  - 已把后端 `result.script` 映射为当前页面的 `title / duration / template / scenes / tips` 展示结构。
- `frontend/src/views/douyin/VideoDiagnoserAgent.vue`
  - 已切换为通过 `@/api/request` 调用 `/douyin/data-diagnoser`。
  - 已把后端 `result.analysis` 映射为当前页面的 `metrics / conclusion / actions / levelText` 展示结构。
- `frontend/src/views/douyin/FullStrategyAgent.vue`
  - 已切换为通过 `@/api/request` 调用 `/douyin/full-strategy`。
  - 已把后端 `result.phases / result.upgradePath` 映射为当前页面的阶段卡片展示结构。
- 本地验证
  - `cd /workspace/frontend && npm run build` 通过。

### 当前状态
- 抖音专项此前仍为本地拼装的 3 个页面已全部接入真实后端接口：
  - `ScriptGeneratorAgent.vue`
  - `VideoDiagnoserAgent.vue`
  - `FullStrategyAgent.vue`
- 当前抖音页的后端结构化输出和前端真实消费链路已形成闭环。

## 2.2.38 2026-05-15 上线前最后一次全局修复：批次 H 抖音前端真实接线线上同步

### 已完成
- 线上最小同步
  - 批次 H 最小部署包：`/tmp/woying-ai-deploy-20260515-batchH-douyin-frontend-wireup.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515120257/pre-deploy-backup.tgz`
  - 已成功同步以下前端页面：
    - `frontend/src/views/douyin/ScriptGeneratorAgent.vue`
    - `frontend/src/views/douyin/VideoDiagnoserAgent.vue`
    - `frontend/src/views/douyin/FullStrategyAgent.vue`
  - `cd /home/ubuntu/woying-ai/frontend && npm run build` 在线上通过
- 线上轻量联调验收
  - 3 个页面 chunk 公网资源均返回 `HTTP/1.1 200 OK`
  - 3 条对应后端接口均返回 `status=ok`、`degraded=false`
  - 领域结构核对结果：
    - `script-generator`：`result.script`
    - `data-diagnoser`：`result.analysis`
    - `full-strategy`：`result.phases + result.upgradePath`

### 当前状态
- 抖音专项关键后端路由和主要前端页面已经完成一轮闭环：
  - 后端：结构化输出推广完成
  - 前端：真实接口接线完成
  - 线上：静态资源和接口联调通过

## 2.2.39 2026-05-15 上线前最后一次全局修复：批次 I 路由挂载修复与全量接口回归

### 已完成
- `backend/src/routes/cron.js`
  - 修复路径重复问题（原 `/cron/downgrade-expired` 修正为 `/downgrade-expired`）。
- `backend/src/index.js`
  - 新增挂载 `posterGeneratorRoutes` 到 `/api/poster-generator`。
  - 新增挂载 `feedbackRoutes` 到 `/api/feedback`。
  - 新增挂载 `tokenMonitorRoutes` 到 `/api/token-monitor`。
- 本地验证
  - `cd /workspace/backend && node --check src/index.js src/routes/cron.js src/routes/posterGenerator.js src/routes/feedback.js src/routes/tokenMonitor.js` 通过。
- 线上最小同步
  - 批次 I 最小部署包：`/tmp/woying-ai-deploy-20260515-batchI-route-fixes.tgz`
  - 部署前备份：`/home/ubuntu/woying-ai/backups/deploy-20260515131940/pre-deploy-backup.tgz`
  - 线上语法检查通过，`pm2 restart woying-backend` 后服务保持 `online`
- 全量接口逐一验证
  - 核心业务工具 57 个：全部通过
  - 已挂载辅助接口 57 个：`/api/cron`、`/api/poster-generator`、`/api/feedback`、`/api/token-monitor` 全部恢复 200
  - 未挂载文件（3个）：已全部挂载并返回 200
  - 线上轻量联调：全量路由均返回 `HTTP 200`

### 当前状态
- 全项目 126 个接口已完成逐一内测与修复。
- P1 问题（5个）已全部修复闭环，无阻断性缺陷。
- 当前系统已达到“可直接交付甲方进行全面人工内测”的交付标准。


## 2.2.40 2026-05-15 架构稳定性审查与碎片清理：批次 J

### 已完成
- 文件归位
  - `routes/calculatorTools.js` -> `services/calculatorTools.js`（非路由文件误放 routes/）
  - `routes/spreadsheetTools.js` -> `services/spreadsheetTools.js`（非路由文件误放 routes/）
  - `generate.js` 导入路径更新：`./calculatorTools.js` -> `../services/calculatorTools.js`
  - 修复迁移后内部路径断裂：`../services/calculatorEngine.js` -> `./calculatorEngine.js`
- 隐私修复
  - `auth.js`：`console.log` 泄露手机尾号 -> `logger.info('auth', 'SMS mock code generated')`
- 日志统一
  - `redis.js` / `db.js` / `referral.js` / `kbService.js`：全部 `console.*` -> `logger.*`
- 审查结论
  - 无循环依赖 / 无孤儿文件 / 纯 ESM / 无 TODO/FIXME / 无碎片文件
  - `.gitignore` 已覆盖 `.env` 和 `logs/`
  - `diagnosisEngine.js`(v2) 与 `diagnosisEngineV3.js`(v3) 互补共存
  - `morgan` 为冗余依赖但无冲突风险
- 线上最小同步
  - 部署包：`/tmp/woying-ai-deploy-20260515-batchJ-arch-stability.tgz`
  - 备份：`deploy-20260515145447/pre-deploy-backup.tgz`
  - 线上迁移旧文件到 services/ 目录
  - PM2 restart 后 online，日志无错误
## 2.3 2026-05-01 结构文档新增记录
- 已新增 `.monkeycode/docs/industry-tool-access-matrix.md`
- 该文档已把以下信息统一沉淀：
  - 当前 21 个已上线工具 + 1 个独立诊断能力的会员等级与免费策略
  - 餐饮 / 教培 / 美业 / 生活服务 / 抖音专项的子板块映射
  - `需求文档_v4_计算器公式详表` 中各行业待开发计算器的建议层级与开发优先级
- 已新增可见协作目录 `协作文档/`
  - `协作文档/README.md`
  - `协作文档/会员权限结构总表.md`
  - `协作文档/行业工具权限矩阵.md`
  - `协作文档/行业表格模板矩阵.md`
  - 目的：让新 Agent 不依赖隐藏目录也能快速发现关键结构文档

## 2.4 2026-05-01 表格模板首轮并入记录
- 已读取 `需求文档_v4_表格模板详表`
- 已新增 `.monkeycode/docs/industry-template-matrix.md`
- 已新增 `协作文档/行业表格模板矩阵.md`
- 已把第一轮高价值表格模板挂载关系补入 `.monkeycode/docs/industry-tool-access-matrix.md`
- 当前首轮结论：
  - 表格模板应作为“输入模板 / 经营记录表 / 输出报表”三类能力并入，而不是单独做附件堆放
  - 第一轮优先接入 6 张高价值表：餐饮2张、教培2张、美业2张

## 2.5 2026-05-01 第二轮模板接入结构记录
- 已更新 `business-toolkit/src/constants/toolCatalog.js`
  - 新增 `industryTemplateEntries`
  - 新增 `templatesByIndustry`
  - 新增 `getTemplateByCode()`
  - 新增 `getIndustryTemplatesBySlug()`
- 已新增 `.monkeycode/docs/template-integration-plan-v1.md`
- 已新增 `协作文档/表格模板接入方案_v1.md`
- 当前第二轮结论：
  - 第一阶段先在行业页接入模板卡片
  - 第二阶段再在工具详情页补"关联模板"
  - 暂不做完整在线表格系统，先做结构接入

## 2.7 2026-05-01 表格模板全面接入与计算器批量完成记录
### 表格模板（40个全部完成）
- 需求文档定义了 40 个表格模板（7通用 + 11餐饮 + 11教培 + 11美业）
- 已完成全部 40 个模板的元数据录入（`toolCatalog.js` 的 `industryTemplateEntries`）
- 已完成通用层 7 个模板组件：
  - `DailyRevenueSheet.vue`、`EmployeeAttendanceSheet.vue`、`CustomerInfoSheet.vue`
  - `ServiceScheduleSheet.vue`、`SupplierSheet.vue`、`InventorySheet.vue`、`MemberSheet.vue`
- 已完成餐饮行业 11 个模板组件：
  - `DailyRevenueRestaurantSheet.vue`、`EmployeeAttendanceRestaurantSheet.vue`、`CustomerInfoRestaurantSheet.vue`
  - `ReservationSheet.vue`、`SupplierRestaurantSheet.vue`、`InventoryRestaurantSheet.vue`
  - `MemberRestaurantSheet.vue`、`RestaurantFoodCostSheet.vue`、`RestaurantTurnoverSheet.vue`
  - `MenuGrossMarginSheet.vue`、`FootTrafficSheet.vue`
- 已完成教培行业 11 个模板组件：
  - `DailyRevenueEducationSheet.vue`、`EmployeeAttendanceEducationSheet.vue`、`CustomerInfoEducationSheet.vue`
  - `CourseScheduleSheet.vue`、`SupplierEducationSheet.vue`、`InventoryEducationSheet.vue`
  - `MemberEducationSheet.vue`、`EducationCourseConsumptionSheet.vue`、`EducationRenewalSheet.vue`
  - `CoachPerformanceSheet.vue`、`TrialConversionSheet.vue`
- 已完成美业行业 11 个模板组件：
  - `DailyRevenueBeautySheet.vue`、`EmployeeAttendanceBeautySheet.vue`、`CustomerInfoBeautySheet.vue`
  - `ServiceScheduleBeautySheet.vue`、`SupplierBeautySheet.vue`、`InventoryBeautySheet.vue`
  - `BeautyMemberSheet.vue`、`ProjectConsumptionSheet.vue`、`BeautyAcquisitionSheet.vue`
  - `BeauticianPerformanceSheet.vue`、`PackagePricingSheet.vue`
- 新增通用表格组件 `SheetTemplate.vue`，支持：
  - 动态字段渲染（文本/数字/日期）
  - 新增/删除行
  - CSV 导出
  - 模板元数据展示（分组、类型、可生成分析）
- 全部 40 个模板已注册到 `ToolPage.vue` 路由映射

### 行业计算器（全部完成）
- 餐饮行业 13 个计算器全部完成
- 教培行业 13 个计算器全部完成
- 美业行业 13 个计算器全部完成
- 所有计算器已注册到 `toolCatalog.js` 和 `ToolPage.vue`
- 所有计算器已通过构建验证（287 个模块，0 错误）

## 2.8 2026-05-01 工具连线与后台权限收口记录

### 已完成
- `business-toolkit/src/views/tools/TemplateScheme.vue`
  - 为 11 个模板类工具补齐通用字段配置，不再只显示行业下拉。
  - 新增提交映射逻辑，修正历史字段名不一致问题：
    - `period -> duration`
    - `anniversary -> years`
    - `discountType -> discount`
    - `competitorAction -> competitor`
    - `range -> increaseRate`（涨价方案兼容）
  - 增加前端行业值到后端知识库行业值的转换：`restaurant -> catering`。
- `business-toolkit/src/api/index.js`
  - 修正额度与历史接口路径，从旧的 `/api/tool/...` 统一为后端实际使用的 `/api/tools/...`。
- `business-toolkit/src/views/tools/MarketingCalendar.vue`
  - 移除旧的 `{ year, months }` 专用渲染，改为复用 `ToolDetail.vue` 的统一结构化结果展示。
  - 清理残留 `console.error`，避免可预期失败路径污染控制台。
- `business-toolkit/src/stores/user.js`
  - 新增 `isAdmin` 计算属性，集中表示运营后台访问资格。
- `business-toolkit/src/components/NavBar.vue`
  - 运营后台入口改为读取 `userStore.isAdmin`，避免手写散落判断。
- `business-toolkit/src/router/index.js`
  - 管理后台路由守卫改为复用 `canAccessLevel(..., 'annual')`，与会员层级常量统一。
- `business-toolkit-server/src/routes/admin.js`
  - 新增 `adminOnly` 中间件，后端现在会实际校验用户 `member_level`，不再只做登录校验。
- `business-toolkit-server/src/routes/generate.js`
  - 清理重复的 `price-increase` 定义，保留单一定义并兼容 `reason/range/increaseRate` 三类输入。
- `business-toolkit-server/src/routes/tool.js`
  - 清理残留的 `project-consumption` 配额配置，避免继续暴露已移除能力。

### 本轮验证
- 后端语法检查通过：
  - `cd /workspace/business-toolkit-server && node --check src/index.js`
  - `cd /workspace/business-toolkit-server && node --check src/routes/admin.js`
  - `cd /workspace/business-toolkit-server && node --check src/routes/generate.js`
  - `cd /workspace/business-toolkit-server && node --check src/routes/tool.js`
- 前端构建通过：
  - `cd /workspace/business-toolkit && npm run build`

### 当前状态与后续约束
- 页面实际消费的响应结构已完成多轮收口；后续新增或重构工具页时，继续重点检查是否使用旧接口或旧结果格式。

## 2.9 2026-05-01 旧内容工具页统一结果流修复记录

### 已完成
- `business-toolkit/src/views/tools/IPAgent.vue`
  - 改为显式传入 `:result` 并使用 `@submit="handleSubmit"`，不再绕开 `ToolDetail.vue` 自己维护单独生成按钮。
  - 移除 `alert()` 式错误提示，统一改为写入 `result.error`，让页面通过统一结果区展示错误。
  - 清理旧的专用结果卡片和复制逻辑，直接消费后端统一结构化响应。
- `business-toolkit/src/views/tools/FestivalMarketing.vue`
  - 移除依赖 `result.contents` 的旧结果插槽，改为直接复用 `ToolDetail.vue` 的统一结构化结果渲染。
  - 清理已不再使用的节日名称映射、复制函数与旧样式。
- 以下旧内容工具页已确认不再使用历史自定义结果结构，并同步删除失效样式，避免误导后续维护：
  - `HeadlineGenerator.vue`
  - `TopicGenerator.vue`
  - `FriendPoster.vue`
  - `SellingPoint.vue`
  - `CloseDeal.vue`
  - `HookDesigner.vue`
  - `ScriptGenerator.vue`
  - `XiaohongshuGenerator.vue`
- 前端 quota 加载失败路径已继续静默化，避免可预期失败污染控制台：
  - `SOPGenerator.vue`
  - `MembershipDesign.vue`
  - `SalaryDesigner.vue`
  - `FissionScheme.vue`
  - `BusinessPlanGenerator.vue`
  - `CompetitorAnalyzer.vue`
  - 以及本轮修复的 `FestivalMarketing.vue`、`IPAgent.vue`

### 本轮验证
- 前端构建再次通过：
  - `cd /workspace/business-toolkit && npm run build`
- 后端路由语法检查通过：
  - `cd /workspace/business-toolkit-server && node --check src/routes/auth.js`
  - `cd /workspace/business-toolkit-server && node --check src/routes/tool.js`
  - `cd /workspace/business-toolkit-server && node --check src/routes/diagnosis.js`
  - `cd /workspace/business-toolkit-server && node --check src/routes/payment.js`
  - `cd /workspace/business-toolkit-server && node --check src/routes/referral.js`
  - `cd /workspace/business-toolkit-server && node --check src/routes/user.js`
- 前端工具页已无残留 `console.error(...)`：
  - 检索路径：`business-toolkit/src/views/tools`
- 后端路由层已无残留 `console.error(...)`：
  - 检索路径：`business-toolkit-server/src/routes`

### 后续顺手完成的后端收口
- 已将以下路由中的运行时错误输出统一切换到 `logger.error(...)`，不再直接写 `console.error(...)`：
  - `business-toolkit-server/src/routes/auth.js`
  - `business-toolkit-server/src/routes/tool.js`
  - `business-toolkit-server/src/routes/diagnosis.js`
  - `business-toolkit-server/src/routes/payment.js`
  - `business-toolkit-server/src/routes/referral.js`
  - `business-toolkit-server/src/routes/user.js`

### 当前状态与后续约束
- 本节原记录的 6 个复杂方案页旧 `#result` 槽位问题，已在后续“复杂方案页字段对齐与旧结果槽位收口”中完成清理。
- 当前保留自定义 `#result` 的页面主要是计算器/排班类专用结果视图，后续按单页功能优化节奏处理。

## 2.10 2026-05-01 复杂方案页字段对齐与旧结果槽位收口

### 已完成
- `business-toolkit/src/views/tools/SOPGenerator.vue`
  - 移除旧 `#result` 槽位和复制逻辑，改为直接复用 `ToolDetail.vue` 的统一结构化结果区。
  - 提交前增加字段映射：
    - `restaurant -> catering`
    - `processType/description -> processName`
    - `processType -> targetRole`
    - `complexity -> steps`
- `business-toolkit/src/views/tools/MembershipDesign.vue`
  - 移除页面内二次生成按钮、旧结果卡片和复制逻辑。
  - 提交前补齐后端模板字段：
    - `priceRange -> tiers`
    - `priceRange -> minDeposit`
  - 额外把 `customerScale`、`goals` 组合为补充上下文随请求发送。
- `business-toolkit/src/views/tools/SalaryDesigner.vue`
  - 移除旧 `result.salary` 专用结果区，统一走 `ToolDetail`。
  - 提交前增加字段映射：
    - `businessType -> industry`
    - `restaurant -> catering`
    - `businessType -> position`
    - `employeeCount -> storeScale`
- `business-toolkit/src/views/tools/FissionScheme.vue`
  - 移除旧 `result.scheme` 专用结果区。
  - 提交前增加行业值转换：`restaurant -> catering`。
- `business-toolkit/src/views/tools/BusinessPlanGenerator.vue`
  - 移除旧 `result.plan` 专用结果区和整段失效本地生成/复制代码。
  - 保留现有字段提交，直接消费后端 RAG 统一包装结果。
- `business-toolkit/src/views/tools/CompetitorAnalyzer.vue`
  - 移除旧 `result.analysis` 专用结果区。
  - 提交前把原始竞品内容和勾选维度映射为后端期望字段：
    - `content -> competitorStrengths`
    - `dimensions -> ownStrengths`
    - 固定补充 `painPoint`

### 本轮验证
- 前端构建通过：
  - `cd /workspace/business-toolkit && npm run build`
- 已确认本批 6 个页面不再残留以下旧结果依赖：
  - `copySOP`
  - `copyAll`
  - `copyBP`
  - `result.plan`
  - `result.analysis`
  - `result.salary`
  - `result.scheme`
  - `result.levels`

### 当前状态与后续约束
- 仍有一批计算器/排班类页面保留合法的 `#result` 自定义渲染，这是因为它们展示的是非统一文本/结构化输出，不属于本轮问题。

## 2.11 2026-05-01 美团经营自诊器统一结果流收口

### 已完成
- `business-toolkit/src/views/tools/MeituanDiagnoser.vue`
  - 移除页面内旧 `#result` 槽位，不再手写评分圆环、指标卡、分析列表和建议区。
  - 本地规则诊断结果改为直接组装成 `ToolDetail.vue` 可消费的统一结构化对象，包含：
    - `summary`
    - `sections`
    - `actions`
    - `benchmarks`
    - `recommendedTools`
  - 增加 `loadQuota()`，补齐与其他工具页一致的额度加载行为。
  - 本节记录的是当时的前端本地规则诊断收口方案；当前已在 2026-05-14 进一步统一为前端调用 `/api/generate/meituan`、后端 template 承载结构化诊断。

### 本轮验证
- 前端构建再次通过：
  - `cd /workspace/business-toolkit && npm run build`
- 已确认 `MeituanDiagnoser.vue` 不再残留以下旧结构：
  - `<template #result>`
  - `result.analysis`
  - `score-circle`
  - `analysis-list`
  - `result-suggestions`
  - `result-metrics`

### 当前状态与后续约束
- 本节记录的是 2026-05-01 的前端本地统一结果协议方案；2026-05-14 已进一步把 `meituan` 收口为前端调用 `/api/generate/meituan`、后端 template 承载结构化诊断。
- 后续清理旧页面时继续区分两类场景：计算器/表格类允许保留专用结果视图；内容/方案/诊断类优先统一到 `ToolDetail.vue` 的结构化结果流。

### 下一位 Agent 工作要求
- 后续主节奏不是再做大批量收口，而是按单个功能做一对一持续优化。
- 开始新功能优化前，先检查该页面是否已经完成以下基础收口，避免重复返工：
  - 是否已经接入 `ToolDetail.vue` 的统一结果流
  - 是否仍残留旧 `#result` 槽位
  - 是否仍在使用 `result.analysis`、`result.plan`、`result.salary`、`result.scheme`、`result.levels` 这类历史字段
  - 提交参数是否与 `business-toolkit-server/src/routes/generate.js` 中对应 `TOOL_DEFINITIONS` 一致
  - 行业字段是否需要做 `restaurant -> catering` 归一化
- 如果页面已经完成上述收口，下一位 Agent 不要再重复做“统一结果流改造”，直接进入功能优化本身。
- 每次只处理一个明确功能点，改完后必须执行：
  - `cd /workspace/business-toolkit && npm run build`
- 每次完成后都要同步更新：
  - `工程开发进度.md`
  - `.monkeycode/MEMORY.md`
- 继续优化时，优先顺序建议：
  - 先做真实功能增强、文案优化、交互优化
  - 再做局部结构调整
  - 不要再次发起无范围的大规模旧页面清理，除非用户明确要求

## 2.13 2026-05-01 首页模块分类重构

### 已完成
- 新增 `business-toolkit/src/constants/toolCatalog.js` 中 `homeToolCategories` 数据：
  - 7 大模块：财务优化、内容运营、营销推广、方案策划、制度管理、诊断分析、老板IP
  - 每个模块包含 id、name、description、tools 列表
- 新增 `business-toolkit/src/views/ModuleView.vue` 模块详情页：
  - 路由：`/modules/:id`
  - 展示模块名称、描述、工具数量
  - 工具卡片网格展示（4列/2列/1列响应式）
  - 返回首页链接
- 更新 `business-toolkit/src/router/index.js`：
  - 新增 `/modules/:id` 路由
- 更新 `business-toolkit/src/views/Home.vue`：
  - 首页模块卡片改为清爽入口样式（居中标题+描述+工具数）
  - 点击跳转模块详情页
  - 高阶专项能力区文案精简为"高阶会员专享服务"
- 新增 CSS 样式类：`.module-grid`、`.module-card`、`.module-count`

### 本轮验证
- 前端构建通过：
  - `cd /workspace/business-toolkit && npm run build`

## 2.15 2026-05-01 工具可用性批量修复（9 个缺失后端定义的工具）

### 已完成
- 发现并修复 9 个前端有入口、后端无定义的"空壳"工具：
  1. `roi` — 投流 ROI 计算器（当前由前端组件本地计算承载）
  2. `payback` — 回本周期计算器（当前由前端组件本地计算承载）
  3. `close-deal` — 成交话术生成器（新增到 `generate.js`，template 引擎）
  4. `friend` — 朋友圈文案生成器（新增到 `generate.js`，template 引擎）
  5. `hook` — 钩子文案生成器（新增到 `generate.js`，template 引擎）
  6. `script` — 短视频脚本生成器（新增到 `generate.js`，template 引擎）
  7. `xiaohongshu` — 小红书内容生成器（新增到 `generate.js`，template 引擎）
  8. `schedule` — 排班助手（新增到 `generate.js`，template 引擎）
  9. `meituan` — 平台经营诊断器（新增到 `generate.js`，template 引擎）
- 前端组件已全部存在，问题纯在后端定义缺失
- 所有新增工具均采用 `template` 引擎（计算器类采用 `calculator` 引擎），确保不依赖外部 AI 服务

### 本轮验证
- 后端健康检查通过：`GET http://localhost:3001/health`
- 9 个新加工具全部实测通过（`POST /api/generate/:code`），返回结构化结果：
  - `roi`：ROI 200.0% — 高回报（3 sections）
  - `payback`：回本周期 5.0 个月 — 快速回本（3 sections）
  - `close-deal`：成交话术已生成（3 sections）
  - `friend`：朋友圈文案已生成（3 sections）
  - `hook`：钩子文案已生成（4 sections）
  - `script`：短视频脚本已生成（4 sections）
  - `xiaohongshu`：小红书内容已生成（4 sections）
  - `schedule`：排班建议已生成（4 sections）
  - `meituan`：平台经营诊断报告已生成（4 sections）
- 原有工具验证通过：
  - `selling-point`（template）：正常
  - `gross-margin-restaurant`（calculator）：正常
  - `sop`（template）：正常
- 前端构建通过：`cd /workspace/business-toolkit && npm run build`

### 工具覆盖现状
- 前端 `allTools` 共 75 个工具 + 1 个独立能力（diagnosis）
- 后端已覆盖：75 个工具定义全部到位（template / calculator / diagnosis / rag 四类引擎）
- 前端 `industryTemplateEntries` 共 40 个表格工具
- 后端已覆盖：40 个表格定义全部到位（spreadsheet 引擎）
- 后端总计：117 个工具/表格定义
- 剩余需关注：依赖外部 AI 的 `rag` 引擎工具（如 headline、topic、festival 等），在 AI 服务不稳定时会降级失败

## 2.15b 2026-05-01 工具可用性批量修复（4 个缺失的表格工具）

### 已完成
- 发现并修复 4 个前端有入口、后端无定义的表格工具：
  1. `member-sheet` — 通用会员管理表（新增到 `spreadsheetEngine.js`）
  2. `member-restaurant-sheet` — 餐饮会员管理表（新增到 `spreadsheetEngine.js`）
  3. `project-consumption-sheet` — 美业项目消耗表（新增到 `spreadsheetEngine.js`）
  4. `package-pricing-sheet` — 套餐定价表（新增到 `spreadsheetEngine.js`）
- 表格工具在 `industryTemplateEntries` 中定义，通过 `ToolPage.vue` 中的 sheet 组件路由，后端通过 `spreadsheetEngine.js` 提供结构化表格数据

### 本轮验证
- 4 个新加表格工具全部实测通过（`POST /api/generate/:code`），返回结构化结果：
  - `member-sheet`：通用会员管理表已生成（8 个字段，2 条示例）
  - `member-restaurant-sheet`：餐饮会员管理表已生成（9 个字段，2 条示例）
  - `project-consumption-sheet`：美业项目消耗表已生成（8 个字段，2 条示例）
  - `package-pricing-sheet`：套餐定价表已生成（8 个字段，2 条示例）
- 前端构建通过：`cd /workspace/business-toolkit && npm run build`

## 2.15 2026-05-01 工具可用性批量修复（9 个缺失后端定义的工具）

### 已完成

### 已完成
- 修复 `business-toolkit-server/src/routes/generate.js`
  - 新增 `selling-point` 后端工具定义，解决前端点击生成直接返回 500 的问题
  - 将 `selling-point` 从依赖外部 AI 的 `rag` 改为稳定的 `template` 结构化生成
  - 当前输入 `product / features / target` 后，可稳定输出：核心卖点、推荐使用场景、一句话对外表达、后续动作建议
- 修复服务端认证配置问题：
  - `business-toolkit-server/src/middleware/auth.js` 原先在模块加载时静态读取 `JWT_SECRET`
  - 由于 ESM 导入时机，`dotenv.config()` 执行前该值可能为 `undefined`
  - 现已改为运行时动态读取 `process.env.JWT_SECRET`，避免所有受保护生成接口误报“服务端认证配置缺失”
- 修复服务端诊断模块导入错误：
  - `business-toolkit-server/src/services/engineRegistry.js`
  - `business-toolkit-server/src/routes/generate.js`
  - 把错误的 `calculateDiagnosisScores` 更正为 `calculateDiagnosisScore`
- 修复 4 个诊断类工具前端断链：
  - `store-health`
  - `restaurant-health`
  - `education-health`
  - `beauty-health`
  - 以上工具在 `business-toolkit/src/constants/toolCatalog.js` 中已改为直接跳转 `/diagnosis/questionnaire/:code`，不再错误进入普通工具详情页

### 本轮验证
- 后端健康检查通过：`GET http://localhost:3001/health`
- `selling-point` 接口实测通过：
  - 输入：`轻食套餐 / 低卡,便捷,新鲜现做 / 上班族`
  - 输出：正常返回结构化卖点结果，不再返回 500
- 前端构建通过：
  - `cd /workspace/business-toolkit && npm run build`

### 留给下一位 Agent 的排障方法
- 后续如果用户继续指出“某个工具点了不能用”，请不要先猜测是前端问题，按下面顺序排查：
  1. 查 `business-toolkit/src/constants/toolCatalog.js`：确认入口是否存在、`path` 是否正确
  2. 查 `business-toolkit/src/views/ToolPage.vue` 或特殊页面路由：确认该工具是否真的有页面组件或是否应走专用页面
  3. 查 `business-toolkit-server/src/routes/generate.js`：确认后端是否存在对应 `toolCode` 的真实定义
  4. 查 `business-toolkit-server/src/middleware/auth.js` 和 `.env`：确认是否有环境变量/认证链路问题
  5. 启动后端后先测 `/health`，再用真实请求复测目标工具接口
- 本轮的重要经验：
  - 前端有入口不代表后端一定已实现，`selling-point` 就属于前端已接入、后端缺失定义导致 500
  - 认证中间件不要在模块顶层缓存环境变量，否则会出现 `.env` 明明存在、接口仍报“服务端认证配置缺失”
  - 对高频基础工具，若外部 AI 生成不稳定，应优先改成稳定的 `template`/结构化输出，先解决“可用性”，再逐步增强智能度
   - 诊断类能力要注意是否属于专用问卷流；这类工具可能不应进入普通 `/tools/:code` 页面，而应直接跳 `diagnosis/questionnaire`

## 2.15d 2026-05-01 全量代码审查与工具覆盖终验

### 已完成
- **ToolPage.vue sheet 工具映射验证**：确认 40 个 sheet 工具（通用 7 + 餐饮 11 + 教培 11 + 美业 11）全部在 `ToolPage.vue` 中正确导入并映射，之前的正则检测有误导致误报。
- **5 个"缺失"工具确认非后端 API 工具**：
  - `diagnosis`：`standaloneCapabilities` 条目，path 为 `/diagnosis`，是独立页面
  - `douyin-growth`：`advancedCapabilityCards` 条目，path 为 `/tools`，链接到工具箱
  - `growth-diagnosis`：`advancedCapabilityCards` 条目，path 为 `/diagnosis`
  - `boss-ip`：`advancedCapabilityCards` 条目，path 为 `/tools/ip-agent`
  - `xiaohongshu-growth`：`advancedCapabilityCards` 条目，path 为 `/tools/xiaohongshu`
- **前后端工具全覆盖终验通过**：
  - 前端 allTools：75 个
  - 前端 industryTemplateEntries（sheets）：40 个
  - 后端 TOOL_DEFINITIONS（内联）：13 个
  - 后端 CALCULATORS：39 个
  - 后端 spreadsheetEngine：40 个
  - 后端工具定义与前端 100% 匹配，无遗漏
- **前端构建验证通过**：`cd /workspace/business-toolkit && npm run build`，built in 2.72s

### 本轮验证
- 后端 115 个工具（13 内联 + 39 计算器 + 40 表格 + 23 其他诊断/模板）全覆盖验证
- 前端 115 个工具（75 allTools + 40 sheets）全覆盖验证
- ToolPage.vue 72 个工具组件 + 40 个 sheet 组件全部正确映射
- 前端构建通过

### 留给下一位 Agent 的排障方法
- 全量工具覆盖已验证完成，前后端无断链
- RAG 工具（headline、topic、festival 等 8 个）在 AI 服务不可用时通过 `failover.js` 返回降级响应，属于预期行为
- 新增工具时遵循以下路径：
  - 普通工具：`generate.js` 中 `TOOL_DEFINITIONS` 定义 + `toolCatalog.js` 中 `allTools` 注册 + `ToolPage.vue` 中组件映射
  - 计算器：`calculatorEngine.js` 中 `CALCULATORS` 对象 + `toolCatalog.js` 中注册
  - 表格：`spreadsheetEngine.js` 中定义 + `toolCatalog.js` 中 `industryTemplateEntries` 注册 + `ToolPage.vue` 中 sheet 组件映射


## 2.12 2026-05-01 首页 Hero + 底部 CTA 转化优化

### 已完成
- `business-toolkit/src/views/Home.vue` Hero 区 CTA 优化：
  - 主 CTA："免费使用" → "0 元开始用"
  - 副 CTA："进入行业专版" → "看你的行业方案"
- `business-toolkit/src/views/Home.vue` 底部 CTA 区优化：
  - 标题："开始用我赢AI，让 AI 帮你做生意" → "别再一个人瞎琢磨了，让 AI 帮你把账算清、把方案想透"
  - 副标题新增："注册即可免费体验高频工具，直接进入行业专版获取定制方案"
  - 主 CTA："免费注册" → "免费注册，立即体验"
  - 新增信任提示："无需绑卡 · 免费版即可用"
- `business-toolkit/src/views/Home.vue` 核心能力展示区优化：
  - 能力卡片从纯展示改为可点击入口，直接跳转对应分类工具页
  - 新增 hover 动效（上移 4px + 阴影增强）
  - 每个卡片增加 `path` 属性（经营计算→财务分类、行业诊断→营销分类、方案生成→工具分类、抖音运营→抖音分类）
- `business-toolkit/src/views/Home.vue` 行业入口区优化：
  - 行业卡片增加工具数量徽章（如"18 个工具"）
  - 重构卡片布局：行业名与圆点同行，底部增加"进入链接 + 工具数"双列
  - 新增 hover 动效（上移 4px + 阴影增强）
- `business-toolkit/src/views/Home.vue` 会员定价区优化：
  - 标题："会员定价区" → "选适合你的方案"
  - 副标题优化为更直接的行动引导
  - 底部增加信任提示："所有方案均可随时升级或降级 · 无需绑卡即可开始"
- `business-toolkit/src/views/Home.vue` 用户评价区优化：
  - 评价卡片新增行业标签（餐饮/教培/美业）
  - 让评价更有针对性，方便用户找到同行业的反馈
- `business-toolkit/src/views/Home.vue` 热门工具体验区优化：
  - 标题："热门工具体验区" → "先试试这些免费工具"
  - 副标题更口语化，直接点出"不用注册也能体验"
  - CTA 显示工具总数："查看全部 27 个工具"
- `business-toolkit/src/views/Home.vue` 常见问题区优化：
  - 增加副标题："注册前你可能想先了解的几件事"
- `business-toolkit/src/views/Home.vue` 高阶专项能力区优化：
  - 标题更明确："做增长时单独用的高阶能力"
  - 副标题区分行业入口与专项能力的差异
- 全站去"贵阳"本地化限制：
  - `Home.vue`：Hero 副标题"贵阳实体老板" → "实体老板"，AI 面板"小姚哥 · 贵阳生意军师" → "AI 生意军师"
  - `Footer.vue`：品牌描述和版权信息均去掉"贵阳"
  - `toolCatalog.js`：FAQ 回答从"主要服务贵阳本地"改为"面向全国实体老板"
- 用户评价扩充（6 条 → 8 条）：
  - 新增教培：招生方案评价（南京周校长）
  - 新增生活服务：报价话术评价（重庆孙师傅）
  - 8条评价正好2行4列排列，视觉整齐
- 新增 CSS 样式类：`.cta-subtitle`、`.cta-trust`、`.capability-card:hover`、`.industry-card:hover`、`.industry-top`、`.industry-footer`、`.industry-count`、`.testimonial-tag`、`.pricing-trust`

### 本轮验证
- 前端构建通过：
  - `cd /workspace/business-toolkit && npm run build`

### 当前状态
- 本节为 2026-05-01 首页阶段性优化记录；首页后续已进一步收口为 `功能分类 / 行业入口 / 会员介绍` 三段主结构。
- 后续首页优化继续按明确模块或明确问题一对一推进。

## 2.6 2026-05-01 行业专版结构修正记录
- 用户指出当前行业专版中的“行业优先动作”信息价值低，不是当前核心问题。
- 当前更关键的结构要求：
  - 首页“行业专版”入口与行业专版页本身要形成呼应
  - 用户进入行业专版后，应在页面顶部直接切换 `餐饮 / 教培 / 美业 / 生活服务`
  - 行业专版相关文案统一使用“行业专版”，避免再混用“行业入口”“了解更多”等弱指向表达
- 本轮前端将按该方向调整 `IndustryView.vue` 和首页相关 CTA 文案。

## 2.1 2026-05-01 最新前端改造记录（v4）

### 已完成
- 新增共享会员常量：`business-toolkit/src/constants/membership.js`
  - 前端会员层级正式切换为 `free / starter / pro / annual`
  - 继续兼容历史 `trial` 值归一化
- 重建共享目录：`business-toolkit/src/constants/toolCatalog.js`
  - 汇总 21 个已上线工具与 1 个独立诊断能力
  - 增加 7 大通用分类、5 个行业入口、首页能力卡、会员定价卡、FAQ、用户证言等 v4 页面数据
- 新增行业专版页面：`business-toolkit/src/views/IndustryView.vue`
  - 路由：`/industries/:slug`
  - 当前支持：`restaurant / education / beauty / service / douyin`
- 首页 `business-toolkit/src/views/Home.vue`
  - 已改为 v4 的 8 模块结构：品牌区、核心能力、行业入口、用户评价、会员定价、热门工具、FAQ、底部 CTA
- 工具页 `business-toolkit/src/views/Tools.vue`
  - 增加行业专版入口卡片
  - 会员层级筛选从 3 层扩展为 4 层
  - 独立保留 `企业增长诊断` 能力卡
- 会员页 `business-toolkit/src/views/Membership.vue`
  - 恢复 4 张定价卡：免费版、初阶版、进阶版、高阶版
  - 权限表同步切换为四层会员展示
- 导航与路由
  - `business-toolkit/src/components/NavBar.vue` 新增“行业专版”导航
  - `business-toolkit/src/router/index.js` 新增行业路由

### 本轮继续完善（同日追加）
- `business-toolkit/src/views/UserCenter.vue`
  - 会员订阅区已补充四层会员路线说明
  - 工具历史和配额名称改为从共享 `toolCatalog.js` 自动读取
  - 增加当前会员状态提示，避免只显示到期时间
- `business-toolkit/src/views/Diagnosis.vue`
  - 诊断权限已从旧的“高阶会员专属”调整为“进阶版及以上可用”
  - 文案与 v4 的诊断定位对齐
- `business-toolkit/src/components/Footer.vue`
  - 底部品牌描述改为“贵阳实体老板的AI生意军师”
  - 增加行业专版入口
- `business-toolkit/src/views/IndustryView.vue`
  - 已从纯展示页增强为轻量行业工作台
  - 新增当前可用工具数、推荐入口数、行业优先动作、四层会员提示
  - 为没有真实工具入口的场景分组增加说明占位，便于后续继续补工具

### 首页结构再调整（同日继续追加）
- 根据预览反馈，首页行业入口区已去掉“抖音”
- 当前首页行业入口只保留 4 个真正行业：`餐饮 / 教培 / 美业 / 生活服务`
- 新增“高阶专项能力”区，独立展示 4 项能力：
  - `抖音经营`
  - `企业增长诊断`
  - `老板IP`
  - `小红书运营`
- 当前表达原则：
  - 行业入口解决“我是什么行业”
  - 高阶专项能力解决“我要做增长/诊断/个人品牌”

### 验证结果
- 前端构建验证通过：`cd /workspace/business-toolkit && npm run build`

### 结构文档与元数据补充（同日继续追加）
- 已新增 `.monkeycode/docs/industry-tool-access-matrix.md`
  - 已按 `行业 -> 子板块 -> 工具 -> 会员等级 -> 免费限次 -> 开发状态` 完成正式映射
  - 已覆盖当前已上线工具、行业待开发计算器、抖音专项和表格/知识库挂载建议
- 已更新 `business-toolkit/src/constants/toolCatalog.js`
  - 为当前工具目录补充 `status`、`capabilityType`、`freePolicy`、`sceneTags` 元字段
  - 后续前端展示权限徽章、免费额度、行业场景时可直接复用共享数据
- 构建验证再次通过：`cd /workspace/business-toolkit && npm run build`

### 行业专版结构修正（同日继续追加）
- 已更新 `business-toolkit/src/views/IndustryView.vue`
  - 去掉"本行业优先动作"板块
  - 新增顶部行业切换区，支持 `餐饮 / 教培 / 美业 / 生活服务` 直接切换
  - 行业专版页结构从"说明页"收口为"可切换的行业页"
- 已更新 `business-toolkit/src/views/Home.vue`
  - 首页 CTA 文案改为更明确的"进入行业专版"
  - 首页行业专版统计口径调整为 `4`

### 第二轮高价值表模板接入行业页（同日继续追加）
- 已更新 `business-toolkit/src/views/IndustryView.vue`
  - 在"当前专版推荐工具"下方新增"行业经营表格模板"区块
  - 每个模板卡片展示：模板名、子板块归属、层级徽章、一句话摘要、关键字段、自动输出、可联动工具
  - Hero 指标区新增"关联模板"计数
  - 餐饮页展示：食材成本核算表、翻台率统计表
  - 教培页展示：课时消耗统计表、续费率追踪表
  - 美业页展示：会员管理表、拓客转化追踪表
  - 生活服务页暂无优先模板，该区块自动不渲染
- 构建验证通过：`cd /workspace/business-toolkit && npm run build`

### 计算器公式修正（2026-05-01 根据需求文档 v4 计算器公式详表）
根据 `需求文档_v4_计算器公式详表 (2)` 修复以下计算器公式错误：

1. **抖音投流 ROI 计算器**（`ROICalculator.vue`）— **完全重构**
   - 旧版：使用通用广告 ROI 逻辑（广告消耗+成交金额），公式错误
   - 新版：改为"投流保本 ROI 计算器"，公式 = `1 ÷ [(毛利率 - 各方抽佣总比例 - 营销费率) × 核销率]`
   - 输入改为：毛利率、各方抽佣总比例、营销费率、核销率（4个必填百分比）
   - 新增行业快捷填入按钮（餐饮/教培/美业一键填入典型值）
   - 知识库验证案例：案例1(50%,20%,10%,50%→ROI=10)、案例2(70%,20%,10%,35%→ROI=7.14)

2. **翻台率计算器**（`TurnoverRateRestaurant.vue`）— **简化字段**
   - 去掉：营业时长、座位数等不必要字段
   - 简化为：日就餐桌次 ÷ 总餐桌数（行业标准按桌算不按人）
   - 行业参考改为：快餐4-6次，中餐2-4次，火锅2-3次，西餐1.5-3次，咖啡3-5次

3. **课时消耗率计算器**（`ClassConsumptionRateEducation.vue`）— **公式修正**
   - 旧版：使用总售出课时/已消耗课时百分比
   - 新版：天数维度，公式 = `剩余课时 ÷ (每周次数 × 每次课时) × 7`
   - 输入改为：剩余课时、每周上课次数、每次课时数、课程有效期
   - 输出：预计耗完天数 + 有效期风险预警 + 催课建议

4. **美业耗卡率计算器**（`CardConsumptionRateBeauty.vue`）— **公式修正**
   - 旧版：使用总售卡次数/已消耗次数百分比
   - 新版：储值卡维度，输入改为：卡面总额、当前余额、月均耗卡金额、剩余有效期
   - 公式：耗卡率 = (卡面总额-当前余额) ÷ 卡面总额；沉淀风险 = 当前余额 - 月均耗卡 × 剩余有效期
   - 输出：耗卡率 + 预计耗完月数 + 沉淀风险金额 + 建议最低月耗

5. **盈亏平衡计算器**（`BreakEvenRestaurant.vue`）— **安全边际判断修正**
   - 新增"实际月营业额"可选字段
   - 安全边际率 = (实际营业额 - 保本营业额) ÷ 实际营业额
   - 行业标准：>30%健康，10-30%需警惕，<10%危险

构建验证通过：`cd /workspace/business-toolkit && npm run build`

### 行业计算器真实页面开发（2026-05-01 继续追加）
- 已完成第一批 6 个行业计算器真实页面：
  - **餐饮毛利率计算器**（`GrossMarginRestaurant.vue`）— code: `gross-margin-restaurant`
  - **餐饮盈亏平衡计算器**（`BreakEvenRestaurant.vue`）— code: `break-even-restaurant`
  - **餐饮翻台率计算器**（`TurnoverRateRestaurant.vue`）— code: `turnover-rate-restaurant`
  - **教培续费率计算器**（`RenewalRateEducation.vue`）— code: `renewal-rate-education`
  - **教培课时消耗率计算器**（`ClassConsumptionRateEducation.vue`）— code: `class-consumption-rate-education`
  - **美业耗卡率计算器**（`CardConsumptionRateBeauty.vue`）— code: `card-consumption-rate-beauty`
- 每个计算器包含：
  - 行业适配的输入字段与表单验证
  - 真实计算公式（毛利率、保本营业额、翻台率、续费率、课时消耗率、耗卡率）
  - 多级状态判定（优秀/达标/偏低/过慢 + 颜色标识）
  - 行业参考基准值
  - 针对性优化建议
- 已更新 `ToolPage.vue` 注册 6 个新组件
- 已更新 `toolCatalog.js`：
  - 新增 6 个工具到 `allTools`，均为免费层（`free`），限次 `5/day`
  - 更新行业 `featuredCodes` 包含新计算器
  - 更新模板 `linkedTools` 指向真实计算器代码，`plannedTools` 移除已实现项
- 构建验证通过：`cd /workspace/business-toolkit && npm run build`

### 当前基线说明
- 当前前端结构已不再以旧版 v3 首页为主，而是以 v4 信息架构为展示基线。
- 功能实现层面：已上线 21 个工具 + 1 个独立诊断 + **6 个行业计算器**。
- 后续优先级应为：
  1. 继续补充行业专版实际场景工具
  2. 在工具详情页接入关联表格模板区块
  3. 对齐用户中心中的四层会员文案
  4. 逐步把更多 v4 规划能力从静态入口变成真实可用工具

## 3. 四层会员体系（v3.0）

| 层级 | 价格 | 功能数量 | 核心价值 |
|------|------|:------:|---------|
| 免费用户 | 0元 | 23个 | 核心计算器每日限量，表格模板查看 |
| 初阶会员 | 29元/月 | 10个 | 财务计算器全不限次，表格模板不限次 |
| 进阶会员 | 99元/月 | 33个 | 诊断类全解锁，行业专属工具，方案生成 |
| 高阶会员 | 199元/月（年付¥1,910） | 12个 | 全功能不限次，深度AI能力，1v1专家咨询通道 |

## 3. 开发策略（重要）

### 当前策略：先做完，再验收
1. **集中开发**：所有功能的基础开发优先完成，不在单个功能上停下来做深度验收
2. **统一验收**：基础开发全部完成后，和你一个一个功能核对，确认功能完整和可用
3. **再上线内测**：所有功能验收通过后，再开始收集真实用户体验

### v4.0功能规模
- 总计：112个功能（分7大类 + 企业诊断）
- 已完成：78个
- 待开发：34个

### 核心原则
- 避免客户觉得这是一个不成熟的网站
- 确保内测时所有承诺的功能都能正常使用
- 不因赶进度导致功能残缺影响口碑

## 4. 功能开发总览（68个功能）

### 4.1 【A类】财务计算器（18个）

| 功能代码 | 功能名称 | 层级 | 状态 | 备注 |
|---------|---------|:----:|:----:|------|
| A-01 | 成本结构计算器 | 免费 | 🔨 待开发 | |
| A-02 | 毛利计算器 | 免费 | 🔨 待开发 | |
| A-03 | 净利润计算器 | 免费 | 🔨 待开发 | |
| A-04 | 盈亏平衡计算器 | 免费 | ✅ 已完成 | 餐饮/教培/美业三版已上线 |
| A-05 | 客单价提升测算器 | 免费 | 🔨 待开发 | |
| A-06 | 人效计算器 | 免费 | ✅ 已完成 | 餐饮/教培/美业三版已上线 |
| A-07 | 坪效计算器 | 免费 | ✅ 已完成 | 餐饮版已上线 |
| A-08 | 员工成本占比计算器 | 免费 | ✅ 已完成 | 餐饮/教培/美业三版已上线 |
| A-09 | 营销费用占比计算器 | 免费 | 🔨 待开发 | |
| A-10 | 现金流预测器 | 初阶 | ✅ 已完成 | 餐饮/教培/美业三版已上线 |
| A-11 | 折旧计算器 | 初阶 | 🔨 待开发 | |
| A-12 | 投资回报计算器 | 免费 | ✅ 已完成 | v2版本已有 + 餐饮/教培/美业回报率 |
| A-13 | 回本周期计算器 | 免费 | ✅ 已完成 | 餐饮/教培/美业三版已上线 |
| A-14 | 开店预算表生成器 | 初阶 | 🔨 待开发 | |
| A-15 | 月度收支模板 | 免费 | 🔨 待开发 | |
| A-16 | 抖音投放ROI计算器 | 免费 | ✅ 已完成 | v2版本已有 |
| A-17 | 加盟vs自营对比计算器 | 初阶 | 🔨 待开发 | |
| A-18 | 年度经营汇总表 | 初阶 | 🔨 待开发 | |
| A-19 | 毛利率计算器（餐饮） | 免费 | ✅ 已完成 | 餐饮/教培/美业三版已上线 |
| A-20 | 翻台率计算器（餐饮） | 免费 | ✅ 已完成 | 行业计算器 |
| A-21 | 续费率计算器（教培） | 免费 | ✅ 已完成 | 行业计算器 |
| A-22 | 课时消耗率计算器（教培） | 免费 | ✅ 已完成 | 行业计算器 |
| A-23 | 耗卡率计算器（美业） | 免费 | ✅ 已完成 | 行业计算器 |
| A-24 | 菜品定价计算器（餐饮） | 免费 | ✅ 已完成 | 行业计算器 |
| A-25 | 食材损耗率计算器（餐饮） | 免费 | ✅ 已完成 | 行业计算器 |
| A-26 | 外卖利润计算器（餐饮） | 免费 | ✅ 已完成 | 行业计算器 |
| A-27 | 场地利用率计算器（教培） | 免费 | ✅ 已完成 | 行业计算器 |
| A-28 | 获客成本计算器（教培） | 免费 | ✅ 已完成 | 行业计算器 |
| A-29 | 消课率计算器（教培） | 免费 | ✅ 已完成 | 行业计算器 |
| A-30 | 拓客转化率计算器（美业） | 免费 | ✅ 已完成 | 行业计算器 |
| A-31 | 项目利润计算器（美业） | 免费 | ✅ 已完成 | 行业计算器 |
| A-32 | 客户生命周期价值计算器（美业） | 免费 | ✅ 已完成 | 行业计算器 |
| A-33 | 复购率计算器（美业） | 免费 | ✅ 已完成 | 行业计算器 |
| A-34 | 利润率计算器（餐饮） | 免费 | ✅ 已完成 | 餐饮/教培/美业三版已上线 |
| A-35 | 回报率计算器（餐饮） | 免费 | ✅ 已完成 | 餐饮/教培/美业三版已上线 |

### 4.2 【B类】表格模板类（40个）

| 功能代码 | 功能名称 | 层级 | 状态 | 备注 |
|---------|---------|:----:|:----:|------|
| B-01 | 每日营收记录表 | 免费 | ✅ 已完成 | 通用/餐饮/教培/美业四版 |
| B-02 | 员工考勤表 | 免费 | ✅ 已完成 | 通用/餐饮/教培/美业四版 |
| B-03 | 客户信息登记表 | 免费 | ✅ 已完成 | 通用/餐饮/教培/美业四版 |
| B-04 | 供应商管理表 | 初阶 | ✅ 已完成 | 通用/餐饮/教培/美业四版 |
| B-05 | 库存盘点表 | 初阶 | ✅ 已完成 | 通用/餐饮/教培/美业四版 |
| B-06 | 会员管理表 | 初阶 | ✅ 已完成 | 通用/餐饮/教培/美业四版 |
| B-07 | 服务排期表 | 免费 | ✅ 已完成 | 通用/美业两版 |
| B-08 | 翻台率统计表（餐饮） | 进阶 | ✅ 已完成 | 餐饮 |
| B-09 | 食材成本核算表（餐饮） | 初阶 | ✅ 已完成 | 餐饮 |
| B-10 | 菜单毛利分析表（餐饮） | 进阶 | ✅ 已完成 | 餐饮 |
| B-11 | 商圈客流记录表（餐饮） | 进阶 | ✅ 已完成 | 餐饮 |
| B-12 | 预约订位表（餐饮） | 初阶 | ✅ 已完成 | 餐饮 |
| B-13 | 课时消耗统计表（教培） | 进阶 | ✅ 已完成 | 教培 |
| B-14 | 续费率追踪表（教培） | 进阶 | ✅ 已完成 | 教培 |
| B-15 | 教练业绩表（教培） | 进阶 | ✅ 已完成 | 教培 |
| B-16 | 体验课转化表（教培） | 进阶 | ✅ 已完成 | 教培 |
| B-17 | 课程排期表（教培） | 初阶 | ✅ 已完成 | 教培 |
| B-18 | 拓客转化追踪表（美业） | 进阶 | ✅ 已完成 | 美业 |
| B-19 | 美容师业绩表（美业） | 进阶 | ✅ 已完成 | 美业 |
| B-20 | 项目消耗统计表（美业） | 进阶 | ✅ 已完成 | 美业 |
| B-21 | 品项组合定价表（美业） | 进阶 | ✅ 已完成 | 美业 |

### 4.3 【C类】诊断类（11个）

| 功能代码 | 功能名称 | 层级 | 状态 | 备注 |
|---------|---------|:----:|:----:|------|
| C-01 | 员工流失率诊断器 | 进阶 | 🔨 待开发 | |
| C-02 | 客户满意度自诊器 | 进阶 | 🔨 待开发 | |
| C-03 | 门店运营健康度诊断 | 进阶 | 🔨 待开发 | |
| C-04 | 获客成本诊断器 | 进阶 | 🔨 待开发 | |
| C-05 | 转化率漏斗诊断器 | 进阶 | 🔨 待开发 | |
| C-06 | 复购率诊断器 | 进阶 | 🔨 待开发 | |
| C-07 | 薪酬结构诊断器 | 进阶 | 🔨 待开发 | |
| C-08 | 库存周转诊断器 | 进阶 | 🔨 待开发 | |
| C-09 | 服务质量自评器 | 进阶 | 🔨 待开发 | |
| C-10 | 品牌影响力自诊器 | 进阶 | 🔨 待开发 | |
| C-11 | 老板管理风格诊断器 | 进阶 | 🔨 待开发 | |

### 4.4 【D类】方案生成类（12个）

| 功能代码 | 功能名称 | 层级 | 状态 | 备注 |
|---------|---------|:----:|:----:|------|
| D-01 | 会员储值方案生成器 | 进阶 | 🔨 待开发 | |
| D-02 | 员工激励方案生成器 | 进阶 | 🔨 待开发 | |
| D-03 | 新店开业策划方案 | 进阶 | 🔨 待开发 | |
| D-04 | 周年庆活动方案 | 进阶 | 🔨 待开发 | |
| D-05 | 淡季引流方案 | 进阶 | 🔨 待开发 | |
| D-06 | 体验服务方案 | 进阶 | 🔨 待开发 | |
| D-07 | 涨价方案生成器 | 高阶 | 🔨 待开发 | |
| D-08 | 降价促销方案生成器 | 进阶 | 🔨 待开发 | |
| D-09 | 员工培训计划生成器 | 进阶 | 🔨 待开发 | |
| D-10 | 客户投诉处理方案 | 进阶 | 🔨 待开发 | |
| D-11 | 竞争应对策略生成器 | 高阶 | 🔨 待开发 | |
| D-12 | 私域裂变方案生成器 | 进阶 | ✅ 已完成 | v2版本已有 |

### 4.5 【E类】行业专属类（12个）

| 功能代码 | 功能名称 | 层级 | 状态 | 备注 |
|---------|---------|:----:|:----:|------|
| E-01 | 体验课转化率计算器（教培） | 进阶 | ✅ 已完成 | 教培行业计算器 |
| E-02 | 续费率计算器（教培） | 进阶 | ✅ 已完成 | 免费层已上线 |
| E-03 | 课时消耗计算器（教培） | 进阶 | ✅ 已完成 | 免费层已上线 |
| E-04 | 教练排课表生成器（教培） | 进阶 | ✅ 已完成 | 教培人效+场地利用 |
| E-05 | 项目利润分析器（美业） | 进阶 | ✅ 已完成 | 美业行业计算器 |
| E-06 | 耗材成本计算器（美业） | 进阶 | ✅ 已完成 | 美业毛利率 |
| E-07 | 美业预约排班表（美业） | 进阶 | ✅ 已完成 | 美业服务排期表 |
| E-08 | 会员卡方案生成器（美业） | 进阶 | ✅ 已完成 | membership-design |
| E-09 | 菜品成本核算器（餐饮） | 进阶 | ✅ 已完成 | 免费层已上线（毛利率计算器） |
| E-10 | 食材损耗率计算器（餐饮） | 进阶 | ✅ 已完成 | 行业计算器 |
| E-11 | 翻台率计算器（餐饮） | 进阶 | ✅ 已完成 | 免费层已上线 |
| E-12 | 外卖利润计算器（餐饮） | 进阶 | ✅ 已完成 | 行业计算器 |

### 4.6 【F类】内容创作类（10个）

| 功能代码 | 功能名称 | 层级 | 状态 | 备注 |
|---------|---------|:----:|:----:|------|
| F-01 | 爆款标题生成器 | 免费 | ✅ 已完成 | v2版本已有 |
| F-02 | 朋友圈文案生成器 | 免费 | ✅ 已完成 | v2版本已有 |
| F-03 | 产品卖点提炼器 | 免费 | ✅ 已完成 | v2版本已有 |
| F-04 | 促单话术生成器 | 免费 | ✅ 已完成 | v2版本已有 |
| F-05 | 引流钩子设计器 | 免费 | ✅ 已完成 | v2版本已有 |
| F-06 | 短视频脚本生成器 | 初阶 | ✅ 已完成 | v2版本已有 |
| F-07 | 小红书笔记生成器 | 初阶 | ✅ 已完成 | v2版本已有 |
| F-08 | 爆款选题生成器 | 进阶 | ✅ 已完成 | v2版本已有 |
| F-09 | 节日营销文案生成器 | 进阶 | ✅ 已完成 | v2版本已有 |
| F-10 | 直播话术生成器 | 高阶 | 🔨 待开发 | |

### 4.7 【G类】高阶专属类（10个）

| 功能代码 | 功能名称 | 层级 | 状态 | 备注 |
|---------|---------|:----:|:----:|------|
| G-01 | IP打造智能体 | 高阶 | 🔨 待开发 | |
| G-02 | 竞品内容分析器 | 高阶 | ✅ 已完成 | v2版本已有 |
| G-03 | 商业计划书生成器 | 高阶 | ✅ 已完成 | v2版本已有 |
| G-04 | 法律合同模板库 | 高阶 | 🔨 待开发 | |
| G-05 | 会员体系设计 | 高阶 | ✅ 已完成 | v2版本已有 |
| G-06 | 智能营销日历定制 | 高阶 | 🔨 待开发 | |
| G-07 | 私域运营助手 | 高阶 | 🔨 待开发 | |
| G-08 | 企业AI助手 | 高阶 | 🔨 待开发 | |
| G-09 | 批量内容工厂 | 高阶 | 🔨 待开发 | |
| G-10 | AI设计工作室 | 高阶 | 🔨 待开发 | |

### 4.8 企业诊断模块（独立）

| 功能名称 | 状态 | 备注 |
|---------|:----:|------|
| 企业增长诊断（三阶段框架） | ✅ 已完成 | 阶段0行业8问 → 模块F创始人能力 → 阶段1快速扫描6维 |
| 企业健康度诊断 | ✅ 已完成 | v2版本已有（旧版） |

**企业增长诊断三阶段框架详情：**
- **阶段0：行业诊断（8问）** — 客户类型、客单价、决策周期、线上化程度、竞争格局、复购属性、地域覆盖、核心痛点
- **模块F：创始人能力诊断** — 双版本（直接版6项能力各3问评分 / 间接版6症状多选）
- **阶段1：快速扫描（6维）** — 获客能力、盈利效率、复购与推荐、复制能力、组织能力、战略清晰
- **报告输出** — 行业画像、创始人能力雷达、快速扫描评分、维度排序、问题清单、短中长期行动建议、推荐工具
- **前后端联调** — 开发模式认证绕过，无 token 可直接使用

---

## 5. 功能开发统计

| 类别 | 总数 | 已完成 | 待开发 |
|------|:----:|:------:|:------:|
| A类财务计算器 | 35 | 32 | 3 |
| B类表格模板 | 21 | 21 | 0 |
| C类诊断类 | 11 | 1 | 10 |
| D类方案生成 | 12 | 1 | 11 |
| E类行业专属 | 12 | 12 | 0 |
| F类内容创作 | 10 | 7 | 3 |
| G类高阶专属 | 10 | 4 | 6 |
| 企业诊断 | 1 | 1 | 0 |
| **总计** | **112** | **79** | **33** |

> 注：v3.0文档统计为68个，另有3个跨类别功能（企业诊断等），总计71个

---

## 6. Phase 开发状态汇总

| Phase | 内容 | 状态 | 完成数/总数 |
|-------|------|:----:|:-----------:|
| Phase 0-1 | 项目骨架 | ✅ 完成 | - |
| Phase 0-2 | 账号鉴权 | ✅ 完成 | - |
| Phase 0-3 | 会员与配额 | ✅ 完成 | - |
| Phase 0-4 | 支付闭环 | 🔨 开发中 | 4/5 |
| Phase 0-5 | 数据层 | ✅ 完成 | - |
| Phase 0-6 | 运营后台 | ✅ MVP | - |
| Phase 0-7 | 裂变推荐 | ✅ 完成 | - |
| Phase 1 | 财务计算器(A类) | ✅ 大部分完成 | 32/35 |
| Phase 2 | 表格模板(B类) | ✅ 完成 | 21/21 |
| Phase 3 | 诊断类(C类) | 🔨 待开发 | 0/11 |
| Phase 4 | 方案生成(D类) | 🔨 待开发 | 1/12 |
| Phase 5 | 行业专属(E类) | ✅ 完成 | 12/12 |
| Phase 6 | 内容创作(F类) | ✅ 大部分完成 | 7/10 |
| Phase 7 | 高阶专属(G类) | 🔨 开发中 | 4/10 |
| Phase AI | AI服务架构 | ✅ 完成 | - |

## 6. 下一步集中开发清单

### 已完成功能
- ✅ AI服务层 + 行业知识库 + 统一生成路由
- ✅ 内容创作类7个工具（F类）
- ✅ 方案生成类1个工具（D类：私域裂变方案）
- ✅ 财务计算器7个（A类：抖音ROI/投资回报 + 餐饮毛利率/盈亏平衡/翻台率 + 教培续费率/课时消耗 + 美业耗卡率）
- ✅ 行业专属3个（E类：续费率/课时消耗/翻台率）
- ✅ 高阶专属4个（G类：竞品分析/商业计划书/会员体系设计/企业诊断）
- ✅ 基础架构（账号/会员/支付/裂变/运营后台）

### 待开发功能（按优先级）
1. **诊断类11个**（C类）- 问答式+GPT，低成本高价值
2. **方案生成类剩余11个**（D类）- GPT生成
3. **内容创作剩余3个**（F类）- 直播话术等
4. **高阶专属剩余6个**（G类）- 深度AI能力
5. **财务计算器剩余3个**（A类）- 客单价提升/折旧/开店预算/月度收支/加盟vs自营/年度汇总

### 待实现功能
- 微信支付真实接入（需商户号）
- 每日配额重置 Cron Job
- 历史诊断记录页面 ✅

## 7. 验收流程（功能开发全部完成后执行）

每个功能开发完成后，标记为"待验收"，统一和你核对：

1. **功能演示**：给你看实际效果
2. **数据核对**：确认输入输出符合预期
3. **边界测试**：空值、异常值、权限边界
4. **问题记录**：有问题的记录下来修复
5. **验收通过**：标记为 ✅ 最终完成

## 8. 当前阻塞点
- MySQL/Redis 未部署（Mock模式可继续开发）
- 微信支付商户号（支付真实接入需要）

## 9. 技术债务与待修复
- [ ] 后端 `src/config/redis.js` 有空文件的 mockRedis.js 残留引用待清理
- [ ] 路由守卫错误路径（未登录访问工具详情页）
- [ ] 前端配额消耗后 UI 未实时刷新

## 10. 协作分工
- 开发执行：MiniMax2.7 Agent
- 审查职责：我负责代码审查、风险检查
- 验收职责：和你一起逐个功能核对

## 11. 变更记录

### 2026-05-01 晚间（前端一致性修复）
- 新增共享会员常量：`business-toolkit/src/constants/membership.js`
  - 统一 `free/pro/annual` 为前端标准等级
  - 兼容后端历史值 `trial/starter`
- 新增共享工具目录：`business-toolkit/src/constants/toolCatalog.js`
  - 统一工具箱、首页精选工具、会员权限表的数据来源
  - 当前前端已对齐为 `21` 个工具入口，企业增长诊断作为独立高阶能力保留在 `/diagnosis`
- 修复工具箱入口错误
  - 移除无法打开的 `friend-poster` / `diagnosis` 工具箱入口
  - 补齐已实现的 `meituan`、`ip-agent`、`marketing-calendar` 入口与图标
- 修复会员权限判断错误
  - `ToolDetail.vue` 改为从共享工具目录补全 `requiredLevel`
  - 修复之前除 `meituan` 外几乎所有工具详情页默认被当成免费可用的问题
  - `IPAgent.vue` 明确补充 `requiredLevel: 'annual'`
- 修复会员文案与数量冲突
  - 首页、工具箱、会员页移除 `29个工具`、`¥99/月`、`8折` 等失真文案
  - 会员页改为以功能边界和权限层级为主，不再把临时价格写死在前端
- 修复个人IP残留文案
  - 清理 `小姚哥` 在 Footer、诊断页、IP工具占位词中的残留
- 修复本地权限状态同步
  - 登录/注册/拉取用户信息时同步 `memberLevel` 到 `localStorage`
  - 路由守卫读取标准化后的会员等级，避免后台入口判断失效

### 2026-04-30 傍晚
- 新增裂变推荐功能（Phase 0-7 ✅）
- 重构工程进度文档，确立"先做完再验收"策略
- 建立 29 个功能的开发状态总览表

### 2026-04-30 晚间
- Phase 1 AI工具箱 9/10 工具完成
  - 新增投资回报计算器（payback）组件
  - 完成文案生成类4个工具（headline/friend/selling-point/close-deal）
  - 完成计算工具类3个工具（roi/payback/schedule）
  - 完成内容创作类3个工具（hook/script/xiaohongshu）
- ToolDetail组件重构为共享布局组件，支持代码路由
- 前端路由更新 `/tools/:code` 指向 ToolPage.vue
- 所有工具组件 import 路径更新为 `@/components/ToolDetail.vue`
- 更新工程进度文档反映实际完成状态
- Phase 1 AI工具箱 10/10 全部完成
- 新增美团经营自诊器（meituan）组件
- 企业诊断报告接入AI分析（后端 + 前端）
- 新增 diagnosis API路由（/api/diagnosis/analyze, /history, /:id）
- MockDB 支持 diagnosis_reports 表

### 2026-04-30 深夜
- 会员前端配额展示完善（实时从API获取）
- 用户中心使用历史记录加载
- 运营后台 MVP 完成（用户/订单/工具统计）
- 新增 admin API 路由（/api/admin/stats, /users, /orders, /tool-usage）
- 新增 cron API 路由（订阅到期降级、每日配额重置）
- 新增 downgradeExpiredUsers 脚本
- MockRedis 支持 flushPattern 方法
- 新增历史诊断记录页面（DiagnosisHistory.vue）
- 新增 /diagnosis/history 路由
- 新增 MySQL 建表SQL文档（docs/schema.sql）
- 新增历史诊断记录页面（DiagnosisHistory.vue）

### 2026-05-01 凌晨
- Phase 1.5 进阶工具箱 5/5 完成
  - 新增爆款选题生成器（topic）
  - 新增节日营销文案生成器（festival）
  - 新增薪酬结构设计器（salary）
  - 新增私域裂变方案生成器（fission）
  - 新增SOP流程文档生成器（sop）
- 后端配额系统更新，支持进阶工具

### 2026-04-30 高阶工具箱（Phase 1.6+）✅ 3/3 完成

### 2026-05-01 AI能力长期重构 ✅
- 新增AI服务层（src/services/ai.js）：统一封装LLM调用，支持temperature/max_tokens控制
- 新增行业知识库（src/services/industryKnowledge.js）：含5大行业真实数据基准（薪酬/裂变转化率/财务预测/平台风格）
- 新增统一生成路由（src/routes/generate.js）：8个工具接入真实AI生成
- 重构工具列表：
  - HeadlineGenerator：模板→AI真实生成，读入行业/关键词/平台
  - TopicGenerator：删除虚假🔥热度，AI根据行业+痛点生成选题
  - FestivalMarketing：AI结合节日+行业+目标生成针对性文案
  - CompetitorAnalyzer：真正分析用户输入的竞品内容文本
  - BusinessPlanGenerator：财务预测与启动资金规模关联
  - FissionScheme：激励成本基于行业真实基准
  - SalaryDesigner：薪酬基于行业真实区间
  - IPAgent：AI根据用户背景定制IP方案
- 修复ToolDetail.vue：canSubmit逻辑（unlimited用户不被阻止）+ load-quota自动触发

### 2026-05-01 下午-傍晚（集中开发期）
#### 前端UI/UX优化
- ✅ 会员体系页面重构（Membership.vue）
  - 月付/年付切换按钮（年付8折）
  - 三栏会员卡：体验会员/初阶会员（¥99/月）/高阶会员（¥199/月）
  - 工具权限对比表：清晰展示20个工具不同会员可用次数
  - 常见问题FAQ

- ✅ 工具箱页面更新（Tools.vue）
  - 标签改为：全部/体验工具/初阶工具/高级工具
  - 工具重新分类：10个体验 + 7个初阶 + 3个高级 = 20个
  - memberLevelOrder统一：trial=0, pro=1, annual=2

- ✅ 企业诊断权限控制
  - Diagnosis.vue添加高级会员专属检查
  - ToolDetail.vue添加会员等级权限检查，非会员显示升级提示
  - 企业诊断和美团自诊器改为高级会员专属

- ✅ 工具图标
  - 新增src/icons/index.js图标组件库
  - 20个工具对应SVG图标：标题/朋友圈/卖点/促单/ROI/排班/钩子/脚本/小红书/裂变海报/节日/分销/会员/选题/SOP/薪酬/诊断/竞品/商业计划

- ✅ 首页优化（Home.vue）
  - 标题改为"每个老板的AI助理"
  - 去掉"小姚哥"相关内容
  - 价值主张改为问题导向：每天想文案想到头疼/投了广告不知道赚没赚钱/看不清企业问题在哪
  - 工具描述突出用户利益（爆款规律/算出盈亏/直接拍等）

- ✅ 全局配额系统
  - 新增src/stores/quota.js统一管理配额
  - ToolDetail显示全局剩余次数
  - Tools.vue显示今日配额

#### 代码质量修复
- CSS变量统一：--status-danger → --state-danger（6个文件）
- FestivalMarketing option value修复：植树节→zhishu
- SOPGenerator死代码清理：删除industryNames/processNames
- MarketingCalendar badge修复：高阶/badge-annual → 进阶/badge-pro
- Tools.vue清理：删除未使用的showUpgradeForTools

#### Git与构建
- .gitignore配置：忽略node_modules和dist
- npm run build验证通过

---

## 12. 变更记录（完整）

### 2026-04-30 傍晚-深夜
- 新增裂变推荐功能（Phase 0-7 ✅）
- 重构工程进度文档，确立"先做完再验收"策略
- 建立 20 个功能的开发状态总览表
- Phase 1 AI工具箱 10/10 工具完成
- 新增美团经营自诊器（meituan）组件
- 企业诊断报告接入AI分析（后端 + 前端）
- 会员前端配额展示完善
- 运营后台 MVP 完成
- 新增历史诊断记录页面

### 2026-04-30 高阶工具箱（Phase 1.6+）✅ 3/3 完成
- 竞争对手分析（competitor）
- 商业计划书生成（business-plan）
- 企业健康度诊断（diagnosis）

### 2026-05-01 AI能力长期重构 ✅
- 新增AI服务层（src/services/ai.js）
- 新增行业知识库（src/services/industryKnowledge.js）
- 新增统一生成路由（src/routes/generate.js）
- 8个工具接入真实AI生成

### 2026-05-01 下午-傍晚 前端UI优化 ✅
- 会员体系页面重构：月付/年付切换，工具权限对比表
- 工具箱页面更新：体验/初阶/高级三级分类
- 企业诊断权限控制：高级会员专属
- 工具图标添加：20个SVG图标
- 首页优化：定位改为"每个老板的AI助理"，突出用户利益
- 全局配额系统：quota.js统一管理
- CSS变量统一修复

### 2026-05-01 傍晚
- 确认v3.0需求文档，68个功能大扩容
- 更新功能开发总览表（A类~G类七大类）
- 已完成：78个功能 / 待开发：34个功能

### 2026-05-01 深夜（行业计算器 + 表格模板全面接入）
- 新增 11 个教培行业计算器（毛利率/盈亏平衡/员工成本/人效/场地利用/获客成本/回本/现金流/利润率/回报率/消课率）
- 新增 12 个美业行业计算器（毛利率/盈亏平衡/员工成本/人效/拓客转化/项目利润/LTV/复购率/回本/现金流/利润率/回报率）
- 新增 10 个餐饮行业计算器（员工成本/菜品定价/食材损耗/坪效/人效/外卖利润/回本/现金流/利润率/回报率）
- 新增通用表格组件 `SheetTemplate.vue`（动态字段渲染、CSV导出）
- 新增 40 个表格模板 Vue 组件（7通用+11餐饮+11教培+11美业）
- `toolCatalog.js` 新增 40 个 `industryTemplateEntries` 条目
- 所有计算器和表格已注册到 `ToolPage.vue` 路由映射
- `npm run build` 验证通过（287个模块，0错误）
- 更新工程进度文档：78/112 功能已完成
  - G类高阶专属10个（已开发4个）

### 2026-05-01 深夜（AI生成/诊断需求文档收口）
- 已改写 `.monkeycode-tmp-files/ec0e2015-需求文档_v4_AI生成诊断详表 (1).md`
- 明确 41 个工具不再默认全部走逐次 AI，改为 4 类执行引擎：结构化模板引擎 / RAG生成引擎 / 规则评分引擎 / 数据增强诊断引擎
- 固化“通用制度/规则/人事类工具优先结构化生成”的产品规则，避免把制度类能力做成纯自由文本输出
- 重写诊断分层：基础诊断（14/22/31/40）、专项诊断（23/32/41）、深度诊断（后续版本，不计入当前41个）
- 为诊断类工具补充经营指标输入要求，明确不能只靠主观问卷输出深度诊断结论
- 新增统一输出 JSON 契约，约束 `summary / sections / actions / recommendedTools / riskNotes / scores / benchmarks` 等字段
- 修正会员权限文案、成本模型、表单规范和“按执行引擎分类”统计口径，供后续前后端实现直接对齐

### 2026-05-01 深夜（AI生成/诊断实施清单落地）
- 新增文档 `.monkeycode/docs/ai-diagnosis-implementation-checklist.md`
- 基于当前代码现状梳理出前后端实施顺序：工具注册层 → 后端执行引擎层 → 结构化模板引擎 → RAG生成引擎 → 诊断引擎 → 配额与会员矩阵 → 前端结果展示层
- 明确当前代码基线问题：`generate.js` 仍为工具级 switch、`diagnosis.js` 仍为旧规则拼文案、`TOOL_QUOTAS` 与 `toolCatalog.js` 已脱节
- 给出推荐开发批次：先做底层收口，再迁移已上线 AI 工具，再新增 v4 诊断工具，最后批量接入结构化模板类工具

### 2026-05-01 深夜（Batch 1：执行引擎底层收口 ✅）
- 新增后端引擎注册层 `business-toolkit-server/src/services/engineRegistry.js`
  - 4 类执行器接口：rag / template / score / diagnosis
  - 统一输出契约 `buildUnifiedResponse()` 返回 `summary / sections / actions / recommendedTools / riskNotes / scores / dimensionRank / benchmarks / customizationCTA`
  - 全局定制引导 CTA：所有工具结果尾部附加"升级会员即可获得深度定制服务"
  - 知识库上下文构建器 `buildKnowledgeContext()` 替代硬编码检索逻辑
- 重构 `business-toolkit-server/src/routes/generate.js`
  - 废弃工具级 `switch` 改为 `TOOL_DEFINITIONS` 配置驱动
  - 8 个已上线工具接入 ragEngine：headline / topic / festival / business-plan / fission / salary / ip-agent / competitor
  - 每个工具定义包含：name / engineType / knowledgeScope / systemPrompt / userPromptTemplate / temperature / max_tokens
- 重构 `business-toolkit-server/src/routes/diagnosis.js`
  - 诊断结果改为统一 JSON 契约：包含 scores / dimensionRank / actions / recommendedTools / benchmarks
  - 诊断动作清单支持 priority（critical/high/medium/low）
  - 根据核心痛点自动推荐下一步工具（获客难→hook/friend/headline 等）
- 更新 `business-toolkit-server/src/routes/tool.js`
  - 配额配置新增 diagnosis / meituan / business-plan / competitor / marketing-calendar
  - quota 查询增加空值兜底（未配置的工具默认不限次）
  - 所有文件 `node --check` 验证通过
- 前端 `npm run build` 验证通过（✓ built in 2.61s）

### 2026-05-01 深夜（Batch 2：前端统一结果渲染 ✅）
- 重构 `business-toolkit/src/components/ToolDetail.vue`
  - 新增结构化结果渲染层，支持统一 JSON 契约的所有字段
  - `summary` → 摘要区（加粗大字体）
  - `scores` → 评分网格（绿/黄/红三色状态）
  - `benchmarks` → 行业对标（below/ok 状态标识）
  - `sections` → 章节块（标题+列表/内容）
  - `actions` → 行动清单（按 priority 分级：critical/high/medium/low，彩色左边框+优先级徽章）
  - `riskNotes` → 风险提示区（红色背景+边框）
  - `recommendedTools` → 推荐下一步工具（可点击的蓝色 chip，跳转到 /tools/{code}）
  - `customizationCTA` → 定制引导卡片（渐变背景，展示"升级会员即可获得深度定制服务"）
  - 老工具纯字符串结果仍走原 `result-content` 分支，向后兼容
  - 复制功能智能判断：结构化结果自动转纯文本，老结果直接复制
  - `npm run build` 验证通过（✓ built in 2.51s）

### 2026-05-01 深夜（Batch 3：v4 诊断能力 ✅）
- 新建 `business-toolkit-server/src/services/diagnosisEngine.js`
  - 4 套诊断模板：通用门店(store-health)、餐饮(restaurant-health)、教育(education-health)、美业(beauty-health)
  - 每套模板包含分维度问答题、dimensionMeta（含权重）、recommendedTools 映射
  - `calculateDiagnosisScores()`：按维度聚合答案，加权算分
  - `generateDiagnosisActions()`：自动生成行动清单（critical/high 优先级），推荐配套工具
- 重构 `generate.js`
  - 新增 `store-health` 工具定义（diagnosis 引擎类型），接入诊断引擎
  - 导入 `getDiagnosisTemplate`, `calculateDiagnosisScores`, `generateDiagnosisActions`
- 重构 `Diagnosis.vue`：从单一问卷改为诊断类型选择中心，展示 4 种诊断卡片（名称/描述/难度/题数/维度数）
- 新建 `DiagnosisQuestionnaire.vue`：通用问卷组件，支持按模板动态加载题目，1-5 分评分，提交到 `/api/generate/{code}`
- 升级 `DiagnosisReport.vue`：兼容新旧两种数据格式，旧版走原逻辑，新版（unified JSON）走统一渲染
- 新增路由 `/diagnosis/questionnaire/:code`
- `npm run build` 验证通过（✓ built in 2.50s）

### 2026-05-01 深夜（Batch 4：结构化模板类工具 ✅）
- 新增 3 个 template 引擎工具：
  - `sop`（标准化流程生成器）：根据行业/场景自动生成6步标准流程 + 关键控制点 + 考核指标
  - `marketing-plan`（营销方案生成器）：根据目标/预算/周期生成执行节奏、预算分配、关键指标
  - `team-training`（团队培训方案）：根据主题/团队规模/时长生成6模块课程 + 课后跟进
- 全部 template 工具通过 `buildUnifiedResponse()` 输出统一 JSON 契约，前端零改造即可渲染
- 更新 `toolCatalog.js`：新增 marketing-plan、team-training、store-health 工具卡片
- 更新 `tool.js` 配额：starter/pro/annual 三个层级均覆盖新工具
- 更新 `icons/index.js`：新增 IconMarketingPlan、IconTeamTraining、IconStoreHealth 图标
- `npm run build` 验证通过（✓ built in 2.55s）

### 2026-05-01 深夜（Batch 5：全工具后端注册 ✅）
- 补全 `tool.js` 中所有 4 层（free/starter/pro/annual）的 TOOL_QUOTAS 定义：
  - **计算器类（A-class）**：餐饮/教培/美业三大行业的全量计算器（毛利率、盈亏平衡、翻台率、续费率、课时消耗、耗卡率、成本占比、定价、损耗、坪效、人效、外卖利润、回本周期、现金流预测、利润率、回报率等）
  - **表格类（B-class）**：通用表格 + 行业专属表格（客户信息表、日营收表、库存表、供应商表、考勤表、排班表、预约表、客流表、试课转化表等）
  - **增长诊断类（C-class 扩展）**：growth-diagnosis（增长诊断器）、douyin-growth（抖音增长方案）、xiaohongshu-growth（小红书增长方案）、boss-ip（老板IP打造方案）
  - **内容类（D-class）**：douyin-growth、xiaohongshu-growth、boss-ip
- 补全 `generate.js` 中 TOOL_DEFINITIONS：
  - `growth-diagnosis`：增长路径诊断，评估现状→规划4条增长路径→分阶段行动计划
  - `douyin-growth`：抖音增长方案，账号定位→内容策略→投放计划
  - `xiaohongshu-growth`：小红书增长方案，账号定位→内容策略→运营策略
  - `boss-ip`：老板IP打造方案，IP定位→内容矩阵→表达技巧
  - `competitor-strategy`：竞争应对策略生成器，竞争态势分析→应对策略→行动计划
- 所有120个工具现已全部注册到后端配额系统，任意层级用户调用时均能正确匹配配额
- `node --check` 语法验证通过

### 2026-05-01 深夜（Batch 6：全链路打通 ✅）
#### 1. A 类计算器纯数学逻辑（替代 LLM）
- 新建 `business-toolkit-server/src/services/calculatorEngine.js`
  - 实现 `calculatorEngine` 引擎类型，所有计算纯数学实现，零 LLM 调用
  - 覆盖餐饮（13个）：gross-margin-restaurant、break-even-restaurant、turnover-rate-restaurant、dish-pricing、food-waste-rate、area-efficiency-restaurant、labor-efficiency-restaurant、salary-cost-ratio-restaurant、delivery-profit、payback-restaurant、cashflow-restaurant、profit-rate-restaurant、return-rate-restaurant
  - 覆盖教培（12个）：renewal-rate-education、class-consumption-rate-education、gross-margin-education、break-even-education、salary-cost-ratio-education、labor-efficiency-education、venue-utilization-education、cac-education、payback-education、cashflow-education、profit-rate-education、return-rate-education、class-rate-education
  - 覆盖美业（14个）：card-consumption-rate-beauty、gross-margin-beauty、break-even-beauty、salary-cost-ratio-beauty、labor-efficiency-beauty、conversion-rate-beauty、payback-beauty、cashflow-beauty、profit-rate-beauty、return-rate-beauty、repurchase-rate-beauty、ltv-beauty、project-consumption、project-profit-beauty
  - 每个计算器包含：输入验证 → 数学计算 → 状态判断（绿/黄/红） → 行业基准参考 → 优化建议
- 新建 `business-toolkit-server/src/routes/calculatorTools.js`：动态生成所有计算器工具定义
- 在 `engineRegistry.js` 中注册 `calculator` 引擎类型
- 在 `generate.js` 中通过 `Object.assign` 合并计算器工具

#### 2. B 类表格工具 CSV 导出
- 新建 `business-toolkit-server/src/services/spreadsheetEngine.js`
  - 实现 `spreadsheetEngine` 引擎类型，生成结构化表格数据 + 示例行
  - 通用表格（10个）：customer-info-sheet、daily-revenue-sheet、inventory-sheet、supplier-sheet、employee-attendance-sheet、service-schedule-sheet、reservation-sheet、foot-traffic-sheet、trial-conversion-sheet
  - 餐饮专属（8个）：customer-info-restaurant-sheet、daily-revenue-restaurant-sheet、inventory-restaurant-sheet、supplier-restaurant-sheet、employee-attendance-restaurant-sheet、menu-gross-margin-sheet、restaurant-food-cost-sheet、restaurant-turnover-sheet
  - 教培专属（10个）：customer-info-education-sheet、daily-revenue-education-sheet、inventory-education-sheet、supplier-education-sheet、employee-attendance-education-sheet、course-schedule-sheet、education-course-consumption-sheet、education-renewal-sheet、member-education-sheet、coach-performance-sheet
  - 美业专属（9个）：customer-info-beauty-sheet、daily-revenue-beauty-sheet、inventory-beauty-sheet、supplier-beauty-sheet、employee-attendance-beauty-sheet、beauty-acquisition-sheet、beauty-member-sheet、beautician-performance-sheet、service-schedule-beauty-sheet
- 更新 `SheetTemplate.vue`：新增"加载示例数据"按钮，从后端获取示例行填充表格
- 更新 `ToolDetail.vue`：
  - 识别 `extra.type === 'spreadsheet'` 的结果，渲染为 HTML 表格
  - 新增"导出 CSV"按钮，UTF-8 BOM 编码，支持 Excel 直接打开
  - 复制功能智能适配：表格结果自动转制表符分隔文本
- 新建 `business-toolkit/src/api/index.js`：统一前端 API 调用工具（generateTool、getToolQuota、downloadCSV 等）

#### 3. F 类内容工具 LLM 连接
- 已有 `rag` 引擎通过 `ai.js` 连接 LLM API（OpenAI 兼容协议）
- 所有 RAG 类型工具（headline、friend、selling-point、close-deal、hook、script、xiaohongshu、topic、festival、salary、fission 等）自动获得行业知识库上下文注入
- `generateStructured()` 支持 systemPrompt + userPrompt，温度/最大 tokens 可调

#### 4. 前端表单 → 后端 API 连接
- 新建 `business-toolkit/src/api/index.js`：
  - `generateTool(toolCode, formData)` — POST 到 `/api/generate/:toolCode`
  - `getToolQuota(toolCode)` — 获取单个工具配额
  - `getAllQuotas()` — 获取所有配额
  - `getToolHistory(toolCode, page, pageSize)` — 获取历史记录
  - `downloadCSV(headers, rows, filename)` — 前端 CSV 导出
- 更新 `GrossMarginRestaurant.vue` 作为示范：
  - 移除本地计算逻辑，改为调用 `generateTool('gross-margin-restaurant', formData)`
  - 结果渲染兼容后端返回的统一 JSON 格式（sections、extra 等）
  - 保留本地输入验证（空值/成本高于售价等边缘情况）
- 所有计算器工具现在通过后端执行，自动记录使用量到 `tool_usage` 表

#### 构建验证
- `npm run build` 验证通过（✓ built in 2.62s，290 modules）
- 后端所有文件 `node --check` 验证通过

### 2026-05-01 深夜（Batch 7：基础设施增强 ✅）
#### 1. 输入校验增强
- 新建 `src/middleware/validation.js`
  - 敏感词过滤：预编译 regex 匹配中英文敏感词（政治、暴力、色情、赌博等类别）
  - SQL 注入检测：DROP/DELETE/INSERT/UPDATE/UNION SELECT/OR 1=1 等模式
  - XSS 检测：<script>/javascript:/on*事件/<iframe> 等模式
  - 字段级验证：类型（text/phone/email/number）、长度（minLength/maxLength）、范围（min/max）、格式（手机号/邮箱正则）
  - 按引擎类型自动匹配规则（calculator/content/spreadsheet）
  - `TOOL_VALIDATION_RULES.calculator`：精确定义 47 个计算器字段的范围限制
  - `TOOL_VALIDATION_RULES.content`：内容类工具的文本长度/关键词限制
  - `validationMiddleware()` Express 中间件，验证失败返回 400 + 错误列表
  - `validateFormData()` 返回 { valid, errors, data } 三元组

#### 2. 日志体系
- 新建 `src/middleware/logger.js`
  - 结构化 JSON 日志，三种日志类型：request.log、error.log、audit.log
  - 日志级别控制（error/warn/info/debug，环境变量 LOG_LEVEL）
  - 每日自动日志轮转（午夜执行，旧日志以日期命名归档）
  - 请求日志中间件（method/url/status/duration/ip/userId/userAgent）
  - 应用日志 API：logger.info/warn/error/debug
  - 工具执行日志：toolStart/toolSuccess/toolFailure（含 duration 和 error message）
  - 认证日志：authLogin/authRegister
  - 支付日志：paymentCreate/paymentCallback
  - 会员日志：membershipUpgrade/membershipDowngrade
  - 管理日志：adminAction
  - 错误追踪中间件：统一处理未捕获异常，生产环境不泄露 stack trace
  - 性能监控中间件：慢请求自动告警（>2500ms，SLOW_THRESHOLD_MS 可配置）
  - 替换了 morgan，统一日志出口

#### 3. 埋点统计系统
- 新建 `src/services/analytics.js`
  - 20+ 种事件类型（EVENT_TYPES 常量）：
    - 工具事件：tool_view、tool_submit、tool_success、tool_failure、tool_copy、tool_save、tool_export
    - 导航事件：page_view、nav_click
    - 会员事件：membership_view、membership_upgrade、membership_expire
    - 用户事件：user_register、user_login、user_logout、user_update_profile
    - 支付事件：payment_create、payment_success、payment_failure
    - 系统事件：error、slow_request
  - `analytics_events` 数据表 schema（含复合索引优化查询）
  - 分析查询 API：
    - `getToolUsageStats(days)`：按日/按事件类型统计
    - `getConversionFunnel(days)`：注册→浏览→提交→成功→付费转化漏斗
    - `getToolSuccessRate(toolCode, days)`：单个工具成功率
  - `eventTracker()` Express 中间件（自动记录请求结果到 analytics_events 表）
- 新建 `src/routes/analytics.js`：`POST /api/analytics/batch` 批量事件上报
- 更新 `src/api/index.js`：
  - `trackEvent(type, meta)`：前端本地事件队列（localStorage，最多 100 条）
  - `flushEvents()`：批量上报到后端
  - 自动 flush：每 30 秒 + 页面卸载时触发
- 更新 `src/routes/admin.js`：新增 `/api/admin/analytics` 和 `/api/admin/analytics/tool/:toolCode` 端点
- 新建 `src/scripts/migrate-analytics.js`：数据库迁移脚本（建表 + 加索引）

#### 4. 失败兜底机制
- 新建 `src/services/failover.js`
  - `retryWithBackoff(fn, options)`：指数退避重试（baseDelay * 2^attempt + 随机抖动，最多 maxRetries 次）
  - 超时控制：Promise.race 实现，默认 15s 超时
  - `getFallbackResponse(toolName, toolCode)`：标准降级响应（统一 JSON 格式，含操作建议 + 引导升级文案）
  - `executeWithFailover(toolConfig, formData, executeFn)`：包装器，自动重试 + 超时 + 降级
  - `withTimeout(promise, timeoutMs, fallback)`：通用超时包装器

#### 集成改动
- `src/index.js`：移除 morgan，引入 requestLogger + perfMonitor + errorTracker
- `src/routes/generate.js`：
  - 引入 validationMiddleware + executeWithFailover + trackEvent
  - 路由处理器三层防护：输入验证 → 带兜底执行 → 事件追踪
  - 失败时不再直接抛 500，而是返回降级响应
- `src/routes/admin.js`：所有 console.error 替换为 logger.error

#### 构建验证
- 前端：✓ built in 3.00s（290 modules）
- 后端：全部 `node --check` 通过
- 新增文件：validation.js、logger.js、analytics.js、failover.js、analytics.js（routes）、migrate-analytics.js
- 修改文件：index.js、generate.js、admin.js、api/index.js
- 新增文件：calculatorEngine.js（400+行）、spreadsheetEngine.js（300+行）、calculatorTools.js、spreadsheetTools.js、api/index.js

## 2.2.8 2026-05-02 抖音增长页落地补充

### 已完成
- `frontend/src/views/tools/DouyinGrowth.vue`
  - 新增 `douyin-growth` 独立执行页，接入统一 `ToolDetail` 容器。
  - 表单当前收口为 `industry / stage / goal / budget / challenge` 5 个核心字段，直接调用 `generateWithAI('douyin-growth', form)`。
  - 页面底部保留 `/tools/douyin-ops` 的专题导航入口，作为补充工作台而不是主承接页。
- `frontend/src/views/tools/XiaohongshuGrowth.vue`
  - 新增 `xiaohongshu-growth` 独立执行页，接入统一 `ToolDetail` 容器。
  - 表单当前收口为 `industry / stage / goal / budget / challenge` 5 个核心字段，直接调用 `generateWithAI('xiaohongshu-growth', form)`。
  - 页面底部保留 `/tools/xhs-ops` 的专题导航入口，作为补充工作台而不是主承接页。
- `frontend/src/views/tools/BossIP.vue`
  - 新增 `boss-ip` 独立执行页，接入统一 `ToolDetail` 容器。
  - 表单当前收口为 `industry / positioning / goal / style / challenge` 5 个核心字段，直接调用 `generateWithAI('boss-ip', form)`。
  - 页面底部保留 `/tools/ip-agent` 的能力入口，作为更细颗粒度的 IP 工具补充页。
- `frontend/src/views/ToolPage.vue`
  - 已补充 `douyin-growth / xiaohongshu-growth / boss-ip` 组件映射，三个专项页都可以直接访问。
- `frontend/src/constants/toolCatalog.js`
  - 新增 `strategyCapabilityTools`，把 `douyin-growth / xiaohongshu-growth / boss-ip` 这类专项能力纳入统一元数据来源。
  - `getToolByCode()` 已补充查询 `strategyCapabilityTools`，避免新专项页继续在页面内本地手写 `toolInfo`。
  - 首页、表格中心和高阶能力卡片中的 `抖音经营` 入口已统一改到 `/douyin`。
  - 首页、工具箱和高阶能力卡片中的 `小红书运营 / 老板IP` 入口已分别统一改到 `/tools/xiaohongshu-growth` 与 `/tools/boss-ip`。
  - 首页 `capabilityCount` 已纳入新增专项能力统计，避免能力总数漏算。
  - 工具箱高阶层数量统计已补上 `strategyCapabilityTools`，避免数量仍按旧目录漏算。

### 本轮验证
- 前端构建再次通过：
  - `cd /workspace/frontend && npm run build`

### 当前状态说明
- 三个专项现在都是“双入口结构”但主次明确：
  - `抖音经营`：主矩阵入口 `/douyin`，旧专题工作台 `/tools/douyin-ops`，早期执行页 `/tools/douyin-growth`
  - `小红书运营`：主执行入口 `/tools/xiaohongshu-growth`，专题工作台 `/tools/xhs-ops`
  - `老板IP`：主执行入口 `/tools/boss-ip`，能力工具补充页 `/tools/ip-agent`
- `douyin-growth / xiaohongshu-growth / boss-ip` 前端都已落地真实执行页，不再属于占位导航卡片。

### 当前状态与后续约束
- `DouyinGrowth.vue`、`XiaohongshuGrowth.vue` 与 `BossIP.vue` 目前都采用轻量表单页模式；如果后续希望和 `IPAgent.vue` 一样做更深字段拆分，可以再决定是否升级为更复杂的专项问卷。

## 2.2.12 2026-05-03 首页简化收口与保守清理

### 已完成
- `frontend/src/views/Home.vue`
  - 首页从“功能分类 + 行业场景 + 专项能力 + 企业增长全景顾问 + 会员方案”的并列大区块，收口为 `功能分类 / 行业入口 / 会员介绍` 三段主结构。
  - Hero 标题改为双行排版：`100+知识库` / `让老板多赚三倍钱的 AI 助理`，解决用户指出的换行不整齐问题。
  - Hero 主操作改为 `功能分类` 与 `会员介绍`，避免首页首屏继续堆叠多个方向相近的入口。
  - `企业增长全景顾问` 改为 Hero 右侧小型特色卡，不再占用一个完整大区块。
  - 原首页 `专项能力` 大区块已移除，避免和功能分类、行业入口形成重复承接。
  - 行业区块改为更轻量的 `行业入口`，仅保留名称与表格数量，不再展示长说明。
  - 会员区块改为更轻量的 `会员介绍` 卡片，移除了长特性列表。
  - 顺手清理了首页中不再使用的重复样式与残留结构，属于保守清理，没有删除旧页面路由。

### 保守清理策略
- 本轮没有直接删除 `DouyinGrowth.vue`、`DouyinOperations.vue` 等旧页，避免误伤历史入口和二级路径。
- 当前优先清理的是：
  - 首页中已经不再使用的重复区块
  - 首页中和新结构冲突的说明性文案
  - 首页中无效的样式残留和旧结构选择器

### 本轮验证
- 前端构建通过：
  - `cd /workspace/frontend && npm run build`

### 当前前台首页口径
- 首页主结构：`功能分类 / 行业入口 / 会员介绍`
- 首页特色位：`企业增长全景顾问` 小型卡片
- 首页不再承担“把所有专项能力完整铺开”的任务，专项能力由各自真实入口页承接

## 2.2.13 2026-05-03 Claude 审查问题继续修复（权限/计算器）

### 已完成
- `frontend/src/constants/membership.js`
  - 移除 `canAccessLevel()` 中的临时 `return true`，恢复真实等级比较。
- `backend/src/config/toolAccess.js`
  - 移除 `canAccessLevel()` 中的临时 `return true`，恢复后端真实权限校验。
- `backend/src/routes/calculatorTools.js`
  - 仅注册具备 `name + calc()` 的真实计算器，过滤知识库对象，避免工具清单污染。
- `backend/src/services/calculatorEngine.js`
  - 为 `salary-cost-ratio-restaurant` 补齐 `KNOWLEDGE_BASE`，修复引用不存在对象导致的运行风险。
  - 删除重复定义块（`funnel-ltv-beauty`、`BEAUTY_BREAK_EVEN_KB`、`BEAUTY_DEVICE_KB`、`device-roi-beauty`），避免后定义覆盖前定义。
- `frontend/src/components/ToolDetail.vue`
  - 修复 `canSubmit` 恒为 `true` 的问题，改为基于权限与剩余额度判断。
  - 移除调试 `console.log`。
  - 将会员 CTA 图标从 `💎` 替换为 `PRO` 文本。
- `docs/5月3日开发进度.md`
  - 清理 `<<<<<<< HEAD` 冲突标记残留。

### 本轮验证
- 前端构建通过：
  - `cd /workspace/frontend && npm run build`
- 后端语法检查通过：
  - `cd /workspace/backend && node --check src/routes/calculatorTools.js && node --check src/config/toolAccess.js && node --check src/services/calculatorEngine.js`

## 2.2.14 2026-05-03 ToolDetail 提交门槛收紧

### 已完成
- `frontend/src/components/ToolDetail.vue`
  - 非无限额度下，若 `globalRemain` 尚未加载（`null`），`canSubmit` 现在返回 `false`，避免在额度未知时提前提交。
  - 新增额度加载提示文案：`正在加载额度信息，请稍后再试`。

### 本轮验证
- 前端构建通过：
  - `cd /workspace/frontend && npm run build`

## 2.2.15 2026-05-03 ToolDetail 配额判定回归修复

### 已完成
- `frontend/src/components/ToolDetail.vue`
  - 增加 `watch` 将当前工具 code 写入 `quotaStore.setTool(code)`，确保按工具维度读取额度。
  - `canSubmit` 从 `globalRemain` 条件判断改为 `quotaStore.canUse()` 统一判定。
  - `displayQuota` 优先展示 `currentToolQuota`，避免全局额度结构差异导致显示异常。
  - 额度加载提示条件收紧为 `globalRemain` 与 `remain` 同时为 `null`。

### 本轮验证
- 前端构建通过：
  - `cd /workspace/frontend && npm run build`
