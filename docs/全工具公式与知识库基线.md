# 全工具公式与知识库基线

## 0. 文档目的

本文件记录每个工具的正确名称、输入字段、公式、阈值、知识库来源和恢复依据。未来发生回退时，以本文档作为快速定位正确实现的基线。

## 1. 维护规则

- 每恢复或审核一个工具，必须同步更新本文档。
- 每个工具都要写明正确版本依据，不能只写“参考线上”或“参考本地”。
- 公式、阈值、知识库路径必须可追溯到具体文件。
- 知识库路径必须指向当前工作区真实存在的文件，缺失路径要记录替代方案。
- 所有计算类工具必须记录公式和适用边界。

## 2. 审核状态

| 状态 | 含义 |
|---|---|
| 待审核 | 尚未逐一核对代码、公式和知识库 |
| 已核对 | 已记录公式、阈值和知识库，但尚未恢复代码 |
| 已恢复 | 代码已按基线恢复 |
| 已验证 | 已完成接口、语法或构建验证 |

## 3. 单工具记录模板

### `[tool-code]` 工具名称

| 字段 | 内容 |
|---|---|
| 审核状态 | 待审核 |
| 正确名称 |  |
| 所属模块 |  |
| 前端入口/页面 |  |
| 后端执行文件 |  |
| 输入字段 |  |
| 核心公式 |  |
| 阈值/基准 |  |
| 内置知识库 |  |
| 外部知识库映射 |  |
| 知识库文件是否存在 |  |
| 正确版本依据 |  |
| 恢复动作 |  |
| 验收方式 |  |
| 备注 |  |

## 4. 已核对样本：员工成本智能体

### `salary-cost-ratio-restaurant` 人工成本占比智能体（餐饮版）

| 字段 | 内容 |
|---|---|
| 审核状态 | 已核对 |
| 正确名称 | 人工成本占比智能体（餐饮版） |
| 所属模块 | 餐饮经营 / 人工成本 |
| 前端入口/页面 | 待逐一核对 `frontend/src/views/tools/` 与 `toolCatalog.js` |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | `storeType`、`revenue`、`front`、`back`、`mgmt` |
| 核心公式 | `frontTotal = sum(front.count * front.salary)`；`backTotal = sum(back.count * back.salary)`；`mgmtTotal = sum(mgmt.count * mgmt.salary)`；`totalLabor = frontTotal + backTotal + mgmtTotal`；`laborRatio = totalLabor / revenue * 100`；`frontRatio = frontTotal / revenue * 100`；`backRatio = backTotal / revenue * 100`；`mgmtRatio = mgmtTotal / revenue * 100`；`frontEff = revenue / frontHeadCount`；`backEff = revenue / backHeadCount`；`totalEff = revenue / totalCount` |
| 阈值/基准 | 快餐/简餐：人工占比 18%-25%，前厅人效 70000，后厨人效 90000；中档正餐：22%-30%，前厅 60000，后厨 80000；高端餐厅：28%-35%，前厅 50000，后厨 70000；管理层成本占比高于 12% 触发提示；前后场人数比大于 1.2 或小于 0.4 触发结构失衡提示 |
| 内置知识库 | `CALCULATORS.KNOWLEDGE_BASE.restaurantTypes`、`CALCULATORS.KNOWLEDGE_BASE.adviceTemplates` |
| 外部知识库映射 | 当前本地 `kb-mapping.json` 缺少 `salary-cost-ratio-restaurant` 映射，需补齐 |
| 知识库文件是否存在 | 可用候选：`knowledge-base/02_行业知识库/餐饮行业/餐饮行业知识库_综合v3.0.md`、`knowledge-base/02_行业知识库/餐饮行业/行业特征与诊断/行业对标数据库.md`、`knowledge-base/02_行业知识库/餐饮行业/行业特征与诊断/门店诊断改善方案库.md` |
| 正确版本依据 | 优先采用 `audit-freeze/remote-tree/backend/backups/static-benchmark-final-20260512225736/calculatorEngine.js`，保留 `benchmarks/actions/riskNotes` 深度输出结构；本地当前版本已回退为计算器命名和浅输出 |
| 恢复动作 | 恢复“智能体”命名、深度输出结构、建议渲染、风险提示；补外部知识库映射 |
| 验收方式 | `node --check backend/src/services/calculatorEngine.js`；调用 `/api/generate/salary-cost-ratio-restaurant` 典型参数；核对返回含 `actions/riskNotes/extra.suggestions` |
| 备注 | 当前计算路径不自动注入 `kb-mapping`，后续需要升级计算器链路为“计算结果 + KB 增强解释” |

### `salary-cost-ratio-education` 员工成本占比智能体（教培版）

| 字段 | 内容 |
|---|---|
| 审核状态 | 已核对 |
| 正确名称 | 员工成本占比智能体（教培版） |
| 所属模块 | 教培经营 / 员工成本 |
| 前端入口/页面 | 待逐一核对 `frontend/src/views/tools/` 与 `toolCatalog.js` |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | `totalSalary`、`monthlyRevenue` |
| 核心公式 | `ratio = totalSalary / monthlyRevenue * 100`；`remainingGross = monthlyRevenue - totalSalary` |
| 阈值/基准 | `<=35%` 人工结构较稳；`35%-45%` 人工结构偏紧；`>45%` 人工结构承压 |
| 内置知识库 | 阈值和经营解释内置在 `salary-cost-ratio-education.calc` |
| 外部知识库映射 | 本地存在 `salary-cost-ratio-education`，当前指向 `02_行业知识库/教培行业/教培行业知识库_综合v3.0.md` 的 `人力成本`、`薪酬结构` section |
| 知识库文件是否存在 | 当前未找到 `教培行业知识库_综合v3.0.md`；可用候选：`knowledge-base/02_行业知识库/教培行业/行业特征与诊断/课程结构分析.md`、`knowledge-base/02_行业知识库/教培行业/制度模板/教练培训制度.md`、`knowledge-base/02_行业知识库/教培行业/行业特征与诊断/行业对标数据库.md` |
| 正确版本依据 | 优先采用 `audit-freeze/remote-tree/backend/backups/static-benchmark-final-20260512225736/calculatorEngine.js`，其 benchmark 使用“经验观察：<=35% 较稳，35%-45% 需看排课密度，>45% 通常承压” |
| 恢复动作 | 恢复“智能体”命名、`benchmarks/actions/riskNotes`、`remainingGross`、审慎风险说明；修复缺失知识库路径 |
| 验收方式 | `node --check backend/src/services/calculatorEngine.js`；调用 `/api/generate/salary-cost-ratio-education` 典型参数；核对返回含 `benchmarks` 与 `remainingGross` |
| 备注 | 本地当前版本是浅层计算器输出，需要恢复深度解释和风险提示 |

### `salary-cost-ratio-beauty` 员工成本占比智能体（美业版）

| 字段 | 内容 |
|---|---|
| 审核状态 | 已核对 |
| 正确名称 | 员工成本占比智能体（美业版） |
| 所属模块 | 美业经营 / 员工成本 |
| 前端入口/页面 | 待逐一核对 `frontend/src/views/tools/` 与 `toolCatalog.js` |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | 浅入口：`totalSalary`、`monthlyRevenue`；深岗位结构入口还包含 `storeType`、`revenue`、`bedCount`、`beauticians`、`consultants`、`managers`、`receptions` |
| 核心公式 | 浅入口：`ratio = totalSalary / monthlyRevenue * 100`；`remainingGross = monthlyRevenue - totalSalary`；深结构：`totalLabor = beauticianTotal + consultantTotal + managerTotal + receptionTotal`；`laborRatio = totalLabor / revenue * 100`；`beauticianEff = revenue / beauticianCount`；`totalEff = revenue / totalCount`；`bedEff = revenue / bedCount` |
| 阈值/基准 | 浅入口：`<=30%` 人工结构较稳；`30%-40%` 人工结构偏紧；`>40%` 人工结构承压；深结构：小型店 25%-35%，单床目标 8000；中型店 30%-40%，单床目标 10000；大型店 35%-45%，单床目标 12000 |
| 内置知识库 | 浅入口阈值内置在函数内；深结构使用 `CALCULATORS.BEAUTY_LABOR_KB` |
| 外部知识库映射 | 当前本地 `kb-mapping.json` 缺少 `salary-cost-ratio-beauty` 映射，需补齐 |
| 知识库文件是否存在 | 可用候选：`knowledge-base/02_行业知识库/美业行业/行业特征与诊断/行业对标数据库.md`、`knowledge-base/02_行业知识库/美业行业/行业特征与诊断/门店健康度评分.md`、`knowledge-base/06_通用支撑库/人事管理/美业_岗位职责模板.md` |
| 正确版本依据 | 优先采用 `audit-freeze/remote-tree/backend/backups/static-benchmark-final-20260512225736/calculatorEngine.js`，其风险提示包含“城市薪酬、项目时长、提成结构和排客饱和度校准” |
| 恢复动作 | 恢复“智能体”命名、`benchmarks/actions/riskNotes`、深岗位结构建议；补外部知识库映射 |
| 验收方式 | `node --check backend/src/services/calculatorEngine.js`；调用 `/api/generate/salary-cost-ratio-beauty` 典型参数；核对返回含 `benchmarks/actions/riskNotes` |
| 备注 | 线上同文件还存在基于 `BEAUTY_LABOR_KB` 的更深岗位结构逻辑，应在恢复时确认是否有对应工具 code 和前端入口 |

## 4.1 已恢复首批餐饮计算智能体

### 首批恢复范围说明

| 项目 | 内容 |
|---|---|
| 恢复批次 | 2026-05-13 首批餐饮经营工具 |
| 对比来源 | 本地 `backend/src/services/calculatorEngine.js`、线上 `audit-freeze/remote-tree/backend/src/services/calculatorEngine.js`、深度备份 `audit-freeze/remote-tree/backend/backups/static-benchmark-final-20260512225736/calculatorEngine.js`、本地/线上 `frontend/src/constants/toolCatalog.js`、本地 `backend/src/config/kb-mapping.json` |
| 恢复原则 | 名称优先采用线上 `toolCatalog.js` 的“智能体”版本；深度输出结构优先采用 `static-benchmark-final-20260512225736/calculatorEngine.js`；本地已有 KB 映射保留并更新名称 |
| 备注 | 当前计算器执行链路仍以 JS 内置规则为主，`kb-mapping.json` 主要作为生成链路/后续 KB 增强的映射基线 |

### `gross-margin-restaurant` 品类毛利智能体（餐饮版）

| 字段 | 内容 |
|---|---|
| 审核状态 | 已恢复 |
| 正确名称 | 品类毛利智能体（餐饮版） |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | `storeName`、`categories`，其中 `categories[]` 包含 `name`、`revenue`、`cost` |
| 核心公式 | `profit = revenue - cost`；`margin = profit / revenue * 100`；`overallMargin = totalProfit / totalRevenue * 100`；`profitRatio = categoryProfit / totalProfit * 100` |
| 阈值/基准 | 火锅 55%-65%；炒菜/凉菜/小吃 60%-70%；酒水/饮品 70%-80%；主食 50%-60%；甜品/奶茶/咖啡 65%-75%；低于品类经验参考触发复核建议 |
| 外部知识库映射 | `backend/src/config/kb-mapping.json` 的 `gross-margin-restaurant`，路径 `02_行业知识库/餐饮行业/餐饮行业知识库_综合v3.0.md`，sections 为 `成本结构`、`定价策略` |
| 知识库文件是否存在 | 存在：`knowledge-base/02_行业知识库/餐饮行业/餐饮行业知识库_综合v3.0.md` |
| 正确版本依据 | `static-benchmark-final-20260512225736/calculatorEngine.js` 保留 `actions`、`riskNotes` 和审慎经验区间表述；线上 `toolCatalog.js` 名称为“品类毛利智能体（餐饮版）” |
| 恢复动作 | 后端名称、低毛利建议文案、`actions`、`riskNotes` 已恢复；前端工具目录名称已恢复；KB 映射名称已同步 |

### `turnover-rate-restaurant` 翻台率智能体（餐饮版）

| 字段 | 内容 |
|---|---|
| 审核状态 | 已恢复 |
| 正确名称 | 翻台率智能体（餐饮版） |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | `totalCustomers`、`tableCount`、`mealPeriod` |
| 核心公式 | `turnoverRate = totalCustomers / tableCount`；`perTable = totalCustomers / tableCount` |
| 阈值/基准 | `>=4` 周转较高；`2.5-4` 接近经验区间；`<2.5` 偏低；快餐 4-6 次/天，正餐 2-3 次/天，火锅 3-4 次/天 |
| 外部知识库映射 | `turnover-rate-restaurant`，路径 `02_行业知识库/餐饮行业/餐饮行业知识库_综合v3.0.md`，sections 为 `运营效率`、`翻台管理` |
| 知识库文件是否存在 | 存在 |
| 正确版本依据 | 深度备份保留 `actions`、`riskNotes` 和“业态经验参考”；线上 `toolCatalog.js` 名称为“翻台率智能体（餐饮版）” |
| 恢复动作 | 后端深度结构、前端名称、KB 映射名称已恢复 |

### `food-waste-rate` 食材损耗率智能体

| 字段 | 内容 |
|---|---|
| 审核状态 | 已恢复 |
| 正确名称 | 食材损耗率智能体 |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | `purchaseAmount`、`usedAmount`、`period` |
| 核心公式 | `waste = purchaseAmount - usedAmount`；`wasteRate = waste / purchaseAmount * 100`；`wasteMoney = waste` |
| 阈值/基准 | `<=5%` 正常；`5%-10%` 偏高；`>10%` 严重 |
| 外部知识库映射 | `food-waste-rate`，路径 `02_行业知识库/餐饮行业/餐饮行业知识库_综合v3.0.md`，sections 为 `食材管理`、`损耗控制` |
| 知识库文件是否存在 | 存在 |
| 正确版本依据 | 深度备份保留损耗监控 `actions` 和质量平衡 `riskNotes`；线上 `toolCatalog.js` 名称为“食材损耗率智能体” |
| 恢复动作 | 后端深度结构、前端名称、KB 映射名称已恢复 |

### `labor-efficiency-restaurant` 人效智能体（餐饮版）

| 字段 | 内容 |
|---|---|
| 审核状态 | 已恢复 |
| 正确名称 | 人效智能体（餐饮版） |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | `monthlyRevenue`、`employeeCount`、`totalSalary` |
| 核心公式 | `revenuePerEmployee = monthlyRevenue / employeeCount`；`salaryRatio = totalSalary / monthlyRevenue * 100` |
| 阈值/基准 | 人工占比 `<=20%` 合理；`20%-25%` 偏高；`>25%` 严重超标 |
| 外部知识库映射 | `labor-efficiency-restaurant`，路径 `02_行业知识库/餐饮行业/餐饮行业知识库_综合v3.0.md`，sections 为 `人力成本`、`效率分析` |
| 知识库文件是否存在 | 存在 |
| 正确版本依据 | 深度备份保留 `actions`、`riskNotes`；工具命名采用线上“人效智能体（餐饮版）” |
| 恢复动作 | 后端深度结构、KB 映射名称已恢复；该工具当前未在 `toolCatalog.js` 独立展示，仅作为目录/链路候选工具保留 |

### `delivery-profit` 外卖利润智能体

| 字段 | 内容 |
|---|---|
| 审核状态 | 已恢复 |
| 正确名称 | 外卖利润智能体 |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | `price`、`platformFee`、`packageCost`、`foodCost`、`deliverySubsidy` |
| 核心公式 | `platformFeeAmount = price * platformFee / 100`；`totalCost = foodCost + packageCost + platformFeeAmount + deliverySubsidy`；`profit = price - totalCost`；`margin = profit / price * 100` |
| 阈值/基准 | `>=30%` 盈利；`15%-30%` 微利；`<15%` 亏损 |
| 外部知识库映射 | `delivery-profit`，路径 `02_行业知识库/餐饮行业/餐饮行业知识库_综合v3.0.md`，sections 为 `外卖运营`、`利润分析` |
| 知识库文件是否存在 | 存在 |
| 正确版本依据 | 深度备份保留单均利润 `actions` 和平台费用波动 `riskNotes`；线上 `toolCatalog.js` 名称为“外卖利润智能体” |
| 恢复动作 | 后端深度结构、前端名称、KB 映射名称已恢复 |

### `delivery-analysis` 外卖经营综合分析器

| 字段 | 内容 |
|---|---|
| 审核状态 | 已恢复 |
| 正确名称 | 外卖经营综合分析器 |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | `monthlyOrders`、`avgOrderValue`、`platformFeeRate`、`foodCostRate`、`packageCostPerOrder`、`deliverySubsidyPerOrder`、`monthlyMarketing`、`monthlyFixed`、`repeatRate`、`dineInRevenue`、`dineInMargin` |
| 核心公式 | `platformFeeAmount = avgOrderValue * platformFeeRate / 100`；`foodCost = avgOrderValue * foodCostRate / 100`；`profitPerOrder = avgOrderValue - foodCost - packageCost - platformFeeAmount - deliverySubsidy`；`marginPerOrder = profitPerOrder / avgOrderValue * 100`；`monthlyRevenue = monthlyOrders * avgOrderValue`；`monthlyNetProfit = monthlyGrossProfit - monthlyMarketing - monthlyFixed`；`netMargin = monthlyNetProfit / monthlyRevenue * 100`；`breakEvenOrders = (monthlyMarketing + monthlyFixed) / contributionPerOrder` |
| 阈值/基准 | 单件利润率 `<0` 每单亏损；`0%-15%` 偏低；`>=15%` 健康；复购率 `<15%` 偏低，`>=30%` 较高；营销 ROI 目标 `>=3`；外卖营收占比 `>60%` 触发平台依赖风险 |
| 外部知识库映射 | `delivery-analysis`，路径 `02_行业知识库/餐饮行业/餐饮行业知识库_综合v3.0.md`，sections 为 `外卖运营`、`平台策略` |
| 知识库文件是否存在 | 存在 |
| 正确版本依据 | 深度备份保留外卖单均利润、月度固定成本、渠道周复盘 `actions` 和平台佣金/放量风险 `riskNotes`；前端名称线上本地一致 |
| 恢复动作 | 后端 `actions`、`riskNotes` 与 KB 映射名称已恢复 |

### `profit-rate-restaurant` 利润率智能体（餐饮版）

| 字段 | 内容 |
|---|---|
| 审核状态 | 已恢复 |
| 正确名称 | 利润率智能体（餐饮版） |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | `revenue`、`foodCost`、`laborCost`、`rent`、`otherCost` |
| 核心公式 | `totalCost = foodCost + laborCost + rent + otherCost`；`profit = revenue - totalCost`；`profitRate = profit / revenue * 100`；`foodRate = foodCost / revenue * 100`；`laborRate = laborCost / revenue * 100`；`rentRate = rent / revenue * 100`；`otherRate = otherCost / revenue * 100` |
| 阈值/基准 | `>=18%` 盈利较强；`10%-18%` 利润可控；`<10%` 利润承压；经验观察 18%-25% 较强，10%-18% 需看成本结构，<10% 需重点复盘 |
| 外部知识库映射 | `profit-rate-restaurant`，路径 `02_行业知识库/餐饮行业/餐饮行业知识库_综合v3.0.md`，sections 为 `利润分析`、`成本结构` |
| 知识库文件是否存在 | 存在 |
| 正确版本依据 | 深度备份保留 `benchmarks`、统计口径、经营解释、`actions`、`riskNotes`；线上 `toolCatalog.js` 名称为“利润率智能体（餐饮版）” |
| 恢复动作 | 后端深度结构、前端名称、KB 映射名称已恢复 |

### `dish-pricing` 菜品定价智能体（产品结构设计版）

| 字段 | 内容 |
|---|---|
| 审核状态 | 已恢复 |
| 正确名称 | 菜品定价智能体（产品结构设计版） |
| 后端执行文件 | `backend/src/services/calculatorEngine.js` |
| 输入字段 | `storeType`、`dishes`，其中 `dishes[]` 包含 `name`、`role`、`cost`、`pricingMethod`、`targetMargin`、`markupRate`、`competitorPrice`、`psyPrice` |
| 核心公式 | 按目标毛利：`suggestedPrice = cost / (1 - targetMargin)`；按加价率：`suggestedPrice = cost * (1 + markupRate)`；按竞品：`suggestedPrice = competitorPrice * 0.95`；`margin = (suggestedPrice - cost) / suggestedPrice * 100`；`predictedMargin = trafficMargin * 0.2 + mainMargin * 0.6 + imageMargin * 0.2` |
| 阈值/基准 | 店型目标成本率：快餐 50%，正餐 35%，高端 28%；引流菜目标占比 20%，主推菜 60%，形象菜 20%；非引流菜毛利率 `<30%` 触发提价/成本优化；全部菜品毛利 `>65%` 触发客流风险提示 |
| 外部知识库映射 | `dish-pricing`，路径 `02_行业知识库/餐饮行业/餐饮行业知识库_综合v3.0.md`，sections 为 `定价策略`、`成本核算` |
| 知识库文件是否存在 | 存在 |
| 正确版本依据 | 深度备份名称为“菜品定价智能体（产品结构设计版）”，保留结构分层、套餐建议和深度输出；线上 `toolCatalog.js` 为智能体命名 |
| 恢复动作 | 后端名称、`actions`、`riskNotes`、前端名称、KB 映射名称已恢复 |

### 历史清单别名/缺失项

| 工具 code | 审核状态 | 结论 |
|---|---|---|
| `food-cost-rate` | 已核对 | 当前本地、线上、深度备份的 `calculatorEngine.js` 与 `toolCatalog.js` 均未发现该执行项。当前实际成本类工具为 `gross-margin-restaurant`、`food-yield-rate`、`food-waste-rate`、`dish-pricing`，本批暂不新增未存在工具。 |
| `gross-margin` | 已核对 | 历史泛化名称，当前餐饮实际工具 code 为 `gross-margin-restaurant`。 |
| `turnover-rate` | 已核对 | 历史泛化名称，当前餐饮实际工具 code 为 `turnover-rate-restaurant`。 |
| `dish-profit` | 已核对 | 当前本地、线上、深度备份未发现该执行项。当前菜品利润/结构实际承载在 `dish-pricing` 与 `dish-contribution`。本批恢复 `dish-pricing`，后续单独审核 `dish-contribution`。 |
| `dish-structure` | 已核对 | 当前菜品结构实际承载在 `dish-pricing` 与 `dish-contribution`。本批恢复 `dish-pricing`，后续单独审核 `dish-contribution`。 |

## 4.2 已恢复第二批教培计算智能体

### 第二批恢复范围说明

| 项目 | 内容 |
|---|---|
| 恢复批次 | 2026-05-13 第二批教培经营工具 |
| 对比来源 | 本地 `backend/src/services/calculatorEngine.js`、线上 `audit-freeze/remote-tree/frontend/src/constants/toolCatalog.js`、深度备份 `audit-freeze/remote-tree/backend/backups/static-benchmark-final-20260512225736/calculatorEngine.js`、本地 `backend/src/config/kb-mapping.json`、真实存在的 `knowledge-base/02_行业知识库/教培行业/**/*.md` |
| 恢复原则 | 整个教培区段采用深度备份的 `benchmarks/actions/riskNotes` 输出结构；前端名称采用线上“智能体”命名；KB 映射保留本地多出的工具键，并把缺失综合文件改为真实存在的分主题知识库文件 |
| 实际范围 | 本批恢复 13 个教培工具：`renewal-rate-education`、`class-consumption-rate-education`、`gross-margin-education`、`break-even-education`、`salary-cost-ratio-education`、`labor-efficiency-education`、`venue-utilization-education`、`cac-education`、`payback-education`、`cashflow-education`、`profit-rate-education`、`return-rate-education`、`class-rate-education` |
| 备注 | `breakeven-students-education` 当前未在本地、线上或深度备份中发现执行项；当前招生保本能力由 `break-even-education` 承载 |

| 工具 code | 正确名称 | 输入字段 | 核心公式 | 阈值/基准 | 外部知识库映射 | 恢复动作 |
|---|---|---|---|---|---|---|
| `renewal-rate-education` | 续费率智能体（教培版） | `expiredStudents`、`renewedStudents`、`renewalWindowDays`、`avgTuition`、`newStudents`、`totalStudents`、`periodLabel` | `rate = renewedStudents / cohortSize * 100`；`overallRetention = (totalStudents - cohortSize + renewedStudents + newStudents) / totalStudents * 100`；`renewalRevenue = renewedStudents * avgTuition`；`atRiskRevenue = lostStudents * avgTuition` | `>=85%` 续费较强；`72%-85%` 接近经验参考；`60%-72%` 续费偏弱；`<60%` 续费承压 | `营销案例/续费活动方案.md`、`行业特征与诊断/校区诊断改善方案库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `class-consumption-rate-education` | 课时消耗率智能体（教培版） | `totalPurchased`、`consumed`、`period`、`avgTuition`、`totalStudents`、`scheduledHours`、`attendanceRate` | `rate = consumed / totalPurchased * 100`；`remaining = totalPurchased - consumed`；`unearnedRevenue = remaining * avgTuition`；`dailyBurn = consumed / period`；`burnRate = consumed / totalPurchased * (30 / period) * 100` | `>=70%` 消化较快；`50%-70%` 接近经验参考；`<50%` 消化慢；月消耗 `>=30%` 可继续观察 | `制度模板/课时管理制度.md`、`行业特征与诊断/校区健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `gross-margin-education` | 毛利率智能体（教培版） | `courseFee`、`teacherCost`、`venueCost`、`materialCost` | `totalCost = teacherCost + venueCost + materialCost`；`profit = courseFee - totalCost`；`margin = profit / courseFee * 100`；分别计算教师、场地、物料占比 | 一对一 40%-50%；小班 50%-65%；大班 60%-75%；`>=65%` 毛利健康；`50%-65%` 毛利偏紧；`<50%` 毛利风险 | `行业特征与诊断/行业对标数据库.md`、`行业特征与诊断/课程结构分析.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `break-even-education` | 盈亏平衡点智能体（教培版） | `fixedCost`、`coursePrice`、`costPerStudent` | `contribution = coursePrice - costPerStudent`；`students = ceil(fixedCost / contribution)`；`revenue = students * coursePrice`；`safeRevenue = revenue * 1.2` | 以固定成本、单人贡献毛利和 20% 安全边际计算保本招生/营收线 | `行业特征与诊断/行业对标数据库.md`、`行业特征与诊断/校区健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `salary-cost-ratio-education` | 员工成本占比智能体（教培版） | `totalSalary`、`monthlyRevenue` | `ratio = totalSalary / monthlyRevenue * 100`；`remainingGross = monthlyRevenue - totalSalary` | `<=35%` 人工结构较稳；`35%-45%` 人工结构偏紧；`>45%` 人工结构承压 | `制度模板/教练培训制度.md`、`行业特征与诊断/行业对标数据库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `labor-efficiency-education` | 人效智能体（教培版） | `monthlyRevenue`、`teacherCount` | `revenuePerTeacher = monthlyRevenue / teacherCount`；`revenuePerDay = revenuePerTeacher / 26` | `>=30000` 人效较高；`20000-30000` 人效可控；`<20000` 人效偏低 | `制度模板/教练培训制度.md`、`行业特征与诊断/校区健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `venue-utilization-education` | 场地利用率智能体 | `totalHours`、`bookedHours`、`rooms` | `availableHours = totalHours * rooms`；`utilization = bookedHours / availableHours * 100`；`idleHours = availableHours - bookedHours` | `>=70%` 场地效率较高；`50%-70%` 场地效率一般；`<50%` 场地闲置偏多 | `行业特征与诊断/校区健康度评分.md`、`行业特征与诊断/校区诊断改善方案库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `cac-education` | 获客成本智能体（教培版） | `totalMarketingCost`、`newStudents`、`avgTuition` | `cac = totalMarketingCost / newStudents`；`tuitionRatio = cac / avgTuition` | `<=800` 获客成本较低；`800-2000` 获客成本可控；`>2000` 获客偏贵；`CAC / 首单学费` 通常建议控制在 0.5 以下 | `营销案例/招生策划方案.md`、`营销案例/体验课引流方案.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `payback-education` | 投资回本周期智能体（教培版） | `totalInvestment`、`monthlyProfit` | `months = totalInvestment / monthlyProfit`；`annualReturn = monthlyProfit * 12 / totalInvestment * 100` | `<=12` 回本较快；`12-18` 回本可控；`>18` 回本偏慢；10-18 个月常见，`>24` 通常偏慢 | `行业特征与诊断/行业对标数据库.md`、`行业特征与诊断/校区健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `cashflow-education` | 现金流预测智能体（教培版） | `initialCash`、`monthlyRevenue`、`monthlyCost`、`months` | `monthlyProfit = monthlyRevenue - monthlyCost`；逐月 `cash += monthlyProfit`；当 `cash <= 0` 记录断裂月份；亏损时 `runwayMonths = initialCash / abs(monthlyProfit)` | 按逐月现金余额判断资金安全或断裂月份，重点提示预收款和课耗确认风险 | `制度模板/课时管理制度.md`、`行业特征与诊断/校区健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `profit-rate-education` | 利润率智能体（教培版） | `revenue`、`teacherCost`、`venueCost`、`marketingCost`、`otherCost` | `totalCost = teacherCost + venueCost + marketingCost + otherCost`；`profit = revenue - totalCost`；`profitRate = profit / revenue * 100`；分别计算成本占比 | `>=25%` 净利较强；`15%-25%` 净利可控；`<15%` 净利承压 | `行业特征与诊断/行业对标数据库.md`、`行业特征与诊断/校区诊断改善方案库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `return-rate-education` | 回报率智能体（教培版） | `investment`、`return` | `roi = (return - investment) / investment * 100`；`netProfit = return - investment` | `>=300%` 值得持续；`150%-300%` 需要优化；`<150%` 投入承压 | `营销案例/招生策划方案.md`、`营销案例/转介绍方案.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `class-rate-education` | 消课率智能体（教培版） | `totalClasses`、`consumedClasses`、`period` | `rate = consumedClasses / totalClasses * 100`；`remaining = totalClasses - consumedClasses` | `>=75%` 健康；`50%-75%` 需关注；`<50%` 偏低 | `制度模板/课时管理制度.md`、`行业特征与诊断/校区健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |

## 4.3 已恢复第三批美业计算智能体

### 第三批恢复范围说明

| 项目 | 内容 |
|---|---|
| 恢复批次 | 2026-05-13 第三批美业经营工具 |
| 对比来源 | 本地 `backend/src/services/calculatorEngine.js`、线上 `audit-freeze/remote-tree/frontend/src/constants/toolCatalog.js`、深度备份 `audit-freeze/remote-tree/backend/backups/static-benchmark-final-20260512225736/calculatorEngine.js`、本地 `backend/src/config/kb-mapping.json`、真实存在的 `knowledge-base/02_行业知识库/美业行业/**/*.md` |
| 恢复原则 | 整个美业区段采用深度备份的 `benchmarks/actions/riskNotes` 与专项 KB 输出结构；前端名称采用线上“智能体”命名；KB 映射新增 20 个美业工具键，并全部指向当前真实存在的美业分主题知识库文件 |
| 实际范围 | 本批恢复 20 个美业工具：`card-consumption-rate-beauty`、`gross-margin-beauty`、`break-even-beauty`、`salary-cost-ratio-beauty`、`labor-efficiency-beauty`、`conversion-rate-beauty`、`project-profit-beauty`、`ltv-beauty`、`repurchase-rate-beauty`、`payback-beauty`、`cashflow-beauty`、`profit-rate-beauty`、`return-rate-beauty`、`project-structure-beauty`、`labor-structure-beauty`、`card-debt-beauty`、`funnel-ltv-beauty`、`breakeven-profit-beauty`、`device-roi-beauty`、`member-card-design-beauty` |
| 备注 | `member-card-profit-beauty` 与 `bed-utilization-beauty` 当前未在本地、线上或深度备份中发现执行项；会员卡利润能力由 `member-card-design-beauty` 承载，床位效率能力由 `labor-structure-beauty` 的 `bedEff` 承载 |

| 工具 code | 正确名称 | 输入字段 | 核心公式 | 阈值/基准 | 外部知识库映射 | 恢复动作 |
|---|---|---|---|---|---|---|
| `card-consumption-rate-beauty` | 耗卡率智能体（美业版） | `totalCards`、`consumedCards`、`period`、`avgCardValue`、`totalCustomers`、`scheduledAppointments`、`showUpRate` | `rate = consumedCards / totalCards * 100`；`remaining = totalCards - consumedCards`；`unearnedRevenue = remaining * avgCardValue`；`earnedRevenue = consumedCards * avgCardValue`；`dailyBurn = consumedCards / period`；`estimatedDaysToFinish = remaining / dailyBurn` | `>=60%` 消耗较稳；`40%-60%` 消耗偏慢；`<40%` 沉淀风险；爽约/取消率高于 20% 触发风险提示 | `营销案例/会员卡方案.md`、`行业特征与诊断/门店健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `gross-margin-beauty` | 毛利率智能体（美业版） | `servicePrice`、`productCost`、`laborCost` | `totalCost = productCost + laborCost`；`profit = servicePrice - totalCost`；`margin = profit / servicePrice * 100`；分别计算耗材和人工占比 | `>=70%` 项目毛利健康；`50%-70%` 项目毛利可控；`<50%` 项目毛利承压；基础护理 60%-70%，特色项目 70%-85%，医美 50%-65% | `行业特征与诊断/品项结构分析.md`、`行业特征与诊断/行业对标数据库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `break-even-beauty` | 盈亏平衡点智能体（美业版） | `fixedCost`、`avgRevenue`、`avgCostRate` | `monthlyBE = fixedCost / (1 - avgCostRate / 100)`；`dailyBE = monthlyBE / 30`；`dailyOrders = ceil(dailyBE / avgRevenue)`；`safeRevenue = monthlyBE * 1.15` | 按固定成本、变动成本率和 15% 安全边际计算门店保本营收线 | `行业特征与诊断/行业对标数据库.md`、`行业特征与诊断/门店健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `salary-cost-ratio-beauty` | 员工成本占比智能体（美业版） | `totalSalary`、`monthlyRevenue` | `ratio = totalSalary / monthlyRevenue * 100`；`remainingGross = monthlyRevenue - totalSalary` | `<=30%` 人工结构较稳；`30%-40%` 人工结构偏紧；`>40%` 人工结构承压 | `制度模板/美容师服务规范.md`、`行业特征与诊断/行业对标数据库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `labor-efficiency-beauty` | 人效智能体（美业版） | `monthlyRevenue`、`employeeCount` | `revenuePerEmployee = monthlyRevenue / employeeCount`；`dailyRevenuePerEmployee = revenuePerEmployee / 26` | `>=30000` 人效较高；`20000-30000` 人效可控；`<20000` 人效偏低 | `制度模板/美容师服务规范.md`、`行业特征与诊断/门店健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `conversion-rate-beauty` | 拓客转化率智能体（美业版） | `visitors`、`converted`、`trialVisitors`、`trialConverted`、`newVisitors`、`newConverted`、`avgOrderValue`、`totalRevenue` | `overallRate = converted / visitors * 100`；`trialRate = trialConverted / trialVisitors * 100`；`newRate = newConverted / newVisitors * 100`；`oldRate = oldConverted / oldVisitors * 100`；`missedRevenue = (visitors - converted) * avgOrderValue` | `>=40%` 转化较强；`25%-40%` 接近经验参考；`<25%` 偏低；体验转化约 40% 可复盘放大 | `营销案例/拓客引流方案.md`、`营销案例/品项升级方案.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `project-profit-beauty` | 项目利润智能体（美业版） | `servicePrice`、`productCost`、`laborCost`、`overheadCost` | `totalCost = productCost + laborCost + overheadCost`；`profit = servicePrice - totalCost`；`margin = profit / servicePrice * 100` | `>=60%` 高利润；`40%-60%` 正常；`<40%` 偏低 | `行业特征与诊断/品项结构分析.md`、`营销案例/品项升级方案.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `ltv-beauty` | 客户生命周期价值智能体（美业版） | `avgOrderValue`、`purchaseFrequency`、`customerLifespan`、`cac`、`serviceGrossMargin`、`retentionCost` | `grossLtv = avgOrderValue * purchaseFrequency * customerLifespan`；`grossMarginLtv = grossLtv * serviceGrossMargin / 100`；`netLtv = grossMarginLtv - cac - retentionCost * customerLifespan`；`ltvCacRatio = grossMarginLtv / cac` | `LTV/CAC >=5` 回报较强；`>=3` 接近可放量；`<3` 需优化；毛利口径 LTV 经验观察 3000-15000 元 | `营销案例/会员日活动方案.md`、`营销案例/会员卡方案.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `repurchase-rate-beauty` | 复购率智能体（美业版） | `totalCustomers`、`repurchasedCustomers`、`periodDays`、`serviceCycleDays`、`avgOrderValue`、`dormantCustomers` | `rate = repurchasedCustomers / totalCustomers * 100`；`lostCustomers = totalCustomers - repurchasedCustomers`；`estimatedRepeatRevenue = repurchasedCustomers * avgOrderValue`；`dormantRevenue = dormantCustomers * avgOrderValue` | `>=45%` 复购较强；`30%-45%` 接近经验参考；`20%-30%` 复购偏弱；`<20%` 复购承压 | `营销案例/会员日活动方案.md`、`行业特征与诊断/门店诊断改善方案库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `payback-beauty` | 投资回本周期智能体（美业版） | `totalInvestment`、`monthlyProfit` | `months = totalInvestment / monthlyProfit`；`annualReturn = monthlyProfit * 12 / totalInvestment * 100` | `<=8` 回本较快；`8-15` 回本可控；`>15` 回本偏慢；`>24` 通常风险偏高 | `行业特征与诊断/行业对标数据库.md`、`行业特征与诊断/门店健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `cashflow-beauty` | 现金流预测智能体（美业版） | `initialCash`、`monthlyRevenue`、`monthlyCost`、`months` | `monthlyProfit = monthlyRevenue - monthlyCost`；逐月 `cash += monthlyProfit`；当 `cash <= 0` 记录断裂月份；亏损时 `runwayMonths = initialCash / abs(monthlyProfit)` | 按逐月现金余额判断资金安全或断裂月份，重点提示充值现金流和耗卡确认风险 | `营销案例/会员卡方案.md`、`行业特征与诊断/门店健康度评分.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `profit-rate-beauty` | 利润率智能体（美业版） | `revenue`、`productCost`、`laborCost`、`rent`、`otherCost` | `totalCost = productCost + laborCost + rent + otherCost`；`profit = revenue - totalCost`；`profitRate = profit / revenue * 100`；分别计算产品、人工、房租和其他成本占比 | `>=20%` 净利较强；`10%-20%` 净利可控；`<10%` 净利承压 | `行业特征与诊断/行业对标数据库.md`、`行业特征与诊断/门店诊断改善方案库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `return-rate-beauty` | 回报率智能体（美业版） | `investment`、`return` | `roi = (return - investment) / investment * 100`；`netProfit = return - investment` | `>=200%` 值得持续；`150%-200%` 需要优化；`<150%` 投入承压 | `营销案例/拓客引流方案.md`、`营销案例/会员日活动方案.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `project-structure-beauty` | 美业品项结构与利润智能体 | `beautyType`、`projects[]`，项目包含 `name`、`price`、`role`、`laborRate`、`productRate` | 单项目 `profit = price - product - labor`；`margin = profit / price * 100`；`overallMargin = totalProfit / totalRevenue * 100`；按 `traffic/retention/profit/retail` 统计结构占比 | 项目毛利 `>=70%` 优秀，`50%-70%` 良好，`30%-50%` 预警，`<30%` 风险；项目角色占比按 `BEAUTY_KNOWLEDGE_BASE.projectRoles` 判断 | `行业特征与诊断/品项结构分析.md`、`营销案例/品项升级方案.md` | 后端专项 KB 结构、前端名称、KB 映射均已恢复 |
| `labor-structure-beauty` | 美业人工成本与人效分析器 | `storeType`、`revenue`、`bedCount`、`beauticians`、`consultants`、`managers`、`receptions` | `totalLabor = beauticianTotal + consultantTotal + managerTotal + receptionTotal`；`laborRatio = totalLabor / revenue * 100`；`beauticianEff = revenue / beauticianCount`；`totalEff = revenue / totalCount`；`bedEff = revenue / bedCount` | 小型店人工占比 25%-35%、单床目标 8000；中型店 30%-40%、单床目标 10000；大型店 35%-45%、单床目标 12000；顾问占比 >15% 和管理层占比 >12% 触发提示 | `制度模板/美容师服务规范.md`、`行业特征与诊断/行业对标数据库.md` | 后端专项 KB 结构、前端名称、KB 映射均已恢复 |
| `card-debt-beauty` | 美业卡项负债与实收智能体 | `periodStartDebt`、`monthSales`、`monthConsumption`、`monthRefund` | `periodEndDebt = periodStartDebt + monthSales - monthConsumption - monthRefund`；`consumptionRate = monthConsumption / (periodStartDebt + monthSales) * 100`；`netCashFlow = monthSales - monthRefund`；`refundRatio = monthRefund / monthSales * 100` | 耗卡率 `>=15%` 健康；`10%-15%` 需关注；`<10%` 风险；退费率 `>10%` 触发风险提示 | `营销案例/会员卡方案.md`、`行业特征与诊断/门店健康度评分.md` | 后端专项 KB 结构、前端名称、KB 映射均已恢复 |
| `funnel-ltv-beauty` | 美业拓客转化与 LTV 智能体 | `newVisitors`、`experienceCount`、`retainedCount`、`repurchasedCount`、`totalMarketingCost`、`avgOrderValue`、`purchaseFrequency`、`customerLifespan` | `visitToExperience = experienceCount / newVisitors * 100`；`experienceToRetain = retainedCount / experienceCount * 100`；`retainToRepurchase = repurchasedCount / retainedCount * 100`；`cac = totalMarketingCost / newVisitors`；`ltv = avgOrderValue * purchaseFrequency * customerLifespan`；`ltvCacRatio = ltv / cac` | 进店到体验 30%-50%；体验到留客 25%-40%；留客到复购 40%-60%；`LTV/CAC >=5` 极强，`>=3` 健康，`<3` 需优化；CAC 高于 500 触发提示 | `营销案例/拓客引流方案.md`、`营销案例/会员日活动方案.md` | 后端专项 KB 结构、前端名称、KB 映射均已恢复 |
| `breakeven-profit-beauty` | 美业盈亏平衡与净利预测器 | `rent`、`fixedSalary`、`utilities`、`otherFixed`、`productRate`、`laborCommissionRate`、`platformRate`、`revenue`、`targetProfit` | `totalFixed = rent + fixedSalary + utilities + otherFixed`；`totalVariableRate = productRate + laborCommissionRate + platformRate`；`contributionRate = 1 - totalVariableRate / 100`；`breakevenRevenue = totalFixed / contributionRate`；`targetRevenue = (totalFixed + targetProfit) / contributionRate` | 房租占比 15%-25%；人工占比 30%-40%；产品耗材 5%-15%；平台/营销 3%-8%；保本业绩线高于 100000 触发成本压力提示 | `行业特征与诊断/行业对标数据库.md`、`行业特征与诊断/门店诊断改善方案库.md` | 后端专项 KB 结构、前端名称、KB 映射均已恢复 |
| `device-roi-beauty` | 美容仪器投资回报智能体 | `deviceCost`、`deviceLifespan`、`costPerSession`、`operatorCommissionRate`、`pricePerSession`、`sessionsPerMonth` | `monthlyDepreciation = deviceCost / (deviceLifespan * 12)`；`commissionPerSession = pricePerSession * operatorCommissionRate / 100`；`profitPerSession = pricePerSession - costPerSession - commissionPerSession`；`monthlyProfit = profitPerSession * sessionsPerMonth - monthlyDepreciation`；`paybackMonths = deviceCost / monthlyProfit`；`annualROI = monthlyProfit * 12 / deviceCost * 100` | `<=6` 个月快速回本；`6-12` 个月正常；`>12` 个月偏慢；按 `breakEvenSessions = monthlyDepreciation / profitPerSession` 判断保本服务次数 | `营销案例/品项升级方案.md`、`行业特征与诊断/行业对标数据库.md` | 后端专项 KB 结构、前端名称、KB 映射均已恢复 |
| `member-card-design-beauty` | 会员储值卡设计智能体 | `rechargeAmount`、`giftAmount`、`marginRate` | `totalBalance = rechargeAmount + giftAmount`；`discountRate = rechargeAmount / totalBalance * 10`；`afterDiscountMargin = marginRate * discountRate / 10`；`marginLoss = marginRate - afterDiscountMargin`；`giftRatio = giftAmount / rechargeAmount * 100` | 9.5 折以上让利轻微；9-8.5 折常规促销；8-7.5 折力度较大；7 折以下严重侵蚀利润；折后毛利 `<40%` 和赠送比例 `>30%` 触发风险提示 | `营销案例/会员卡方案.md`、`营销案例/会员日活动方案.md` | 后端专项 KB 结构、前端名称、KB 映射均已恢复 |

## 4.4 已恢复第四批营销推广计算智能体

### 第四批恢复范围说明

| 项目 | 内容 |
|---|---|
| 恢复批次 | 2026-05-13 第四批营销推广、ROI 与 LTV 工具 |
| 对比来源 | 本地 `backend/src/services/calculatorEngine.js`、线上 `audit-freeze/remote-tree/frontend/src/constants/toolCatalog.js`、深度备份 `audit-freeze/remote-tree/backend/backups/static-benchmark-final-20260512225736/calculatorEngine.js`、本地 `backend/src/config/kb-mapping.json`、真实存在的投放运营/引流获客/私域运营/行业营销知识库文件 |
| 恢复原则 | 从 `// ====== 营销推广计算器 ======` 起整体采用深度备份区段，恢复 `actions/riskNotes/benchmarks/scores` 等深度输出；前端名称采用线上智能体命名；KB 映射新增或更新为真实存在的知识库文件 |
| 实际范围 | 本批恢复 13 个营销推广工具：`channel-cac`、`campaign-roi`、`referral-roi`、`conversion-funnel`、`retention-rate`、`marketing-budget`、`churn-rate`、`ltv-restaurant`、`ltv-education`、`promotion-profit`、`return-rate-restaurant`；同步前端通用入口 `roi`、`payback` 为智能体命名 |
| 备注 | `roi`、`payback` 当前均由前端组件本地计算承载；`festival`、`fission` 走 `/api/generate/:toolCode` 的 RAG 链路；`marketing-plan`、`meituan` 走 `/api/generate/:toolCode` 的 template 链路 |

| 工具 code | 正确名称 | 输入字段 | 核心公式 | 阈值/基准 | 外部知识库映射 | 恢复动作 |
|---|---|---|---|---|---|---|
| `return-rate-restaurant` | 回报率智能体（餐饮版） | `investment`、`return` | `roi = (return - investment) / investment * 100`；`netProfit = return - investment` | `>=200%` 回报较强；`100%-200%` 需看毛利与复购；`<100%` 通常承压 | `餐饮行业/营销案例/节假日促销方案.md`、`投放运营/美团经营基准数据库.md`、`餐饮行业知识库_综合v3.0.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `channel-cac` | 多渠道获客成本智能体 | `channels[]`，渠道包含 `name`、`cost`、`leads`、`converted` 或 `conversions` | `effectiveAcquisitions = converted > 0 ? converted : leads`；`cac = cost / effectiveAcquisitions`；`conversionRate = converted / leads * 100`；`avgCac = totalCost / totalEffectiveAcquisitions` | 经验参考：餐饮 CAC 30-80 元，教培 100-300 元，美业 80-200 元；需结合成交率、LTV、退款率和复购质量校准 | `投放运营/微信朋友圈广告投放实操指南.md`、`引流获客/微信生态全链路营销指南.md`、`投放运营/美团经营基准数据库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `campaign-roi` | 活动效果追踪智能体 | `campaignName`、`totalCost`、`days`、`newVisitors`、`orders`、`revenue`、`grossMargin` | `grossProfit = revenue * grossMargin / 100`；`netProfit = grossProfit - totalCost`；`roi = revenue / totalCost`；`roiPct = netProfit / totalCost * 100`；`cac = totalCost / newVisitors`；`conversionRate = orders / newVisitors * 100` | `roiPct >=100%` 活动回报较强；`0%-100%` 有盈利；`<0%` 亏损；转化率 `<20%` 触发机制优化提示 | `引流获客/餐饮引流方案案例.md`、`美业行业/营销案例/会员日活动方案.md`、`教培行业/营销案例/招生策划方案.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `referral-roi` | 转介绍效果智能体 | `oldCustomers`、`newCustomers`、`rewardCost`、`newRevenue`、`otherCAC` | `referralRate = newCustomers / oldCustomers * 100`；`referralCAC = rewardCost / newCustomers`；`kValue = newCustomers / oldCustomers`；`netGain = newCustomers * newRevenue - rewardCost`；`savingPct = (otherCAC - referralCAC) / otherCAC * 100` | `referralRate >=30%` 转介绍率较强；`15%-30%` 良好；`<15%` 偏低；`kValue <0.3` 触发简化推荐流程提示 | `教培行业/营销案例/转介绍方案.md`、`私域运营专项库/话术_转介绍裂变.md`、`私域运营专项库/理论层/F_裂变增长引擎.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `conversion-funnel` | 营销转化漏斗智能体 | `stages[]`，环节包含 `name`、`count` | 环节转化率 `rates[i] = stages[i].count / stages[i-1].count * 100`；`overallRate = last.count / first.count * 100`；识别最大流失环节 `biggestDrop` | 低于平均环节转化率的环节进入重点优化；最大流失环节优先修复 | `话术转化/餐饮促单话术案例.md`、`话术转化/美业促单话术案例.md`、`话术转化/教培促单话术案例.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `retention-rate` | 客户留存率智能体 | `period`、`startCustomers`、`newCustomers`、`endActive`、`dormantDays` | `retainedExisting = min(startCustomers, endActive - newCustomers)`；`retentionRate = retainedExisting / startCustomers * 100`；`churnedCount = startCustomers - retainedExisting`；`dormantCount = max(0, totalCustomers - endActive - churnedCount)` | 30 天：`>=60%` 健康、`45%-60%` 一般；60 天：`>=50%` 健康、`35%-50%` 一般；90 天：`>=40%` 健康、`25%-40%` 一般 | `私域运营专项库/理论层/B_客户生命周期管理.md`、`SOP_客户分层打标流程.md`、`SOP_沉睡客户激活流程.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `marketing-budget` | 营销预算分配智能体 | `totalBudget`、`goal`、`channels[]` | 按目标权重归一分配预算；`amount = totalBudget * pct`；`estCustomers = amount / cac`；`blendedCAC = totalBudget / totalEstCustomers` | 预算目标支持 `acquisition`、`retention`、`brand`、`balanced`；按真实 CAC 和成交质量每周滚动调整 | `投放运营/微信朋友圈广告投放实操指南.md`、`投放运营/薯条投放全指南.md`、`投放运营/美团经营基准数据库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `churn-rate` | 客户流失率智能体 | `startCustomers`、`churned`、`avgOrder`、`freq` | `churnRate = churned / startCustomers * 100`；`monthlyLossValue = churned * avgOrder * freq`；`annualLoss = monthlyLossValue * 12`；`retainCostPerCustomer = avgOrder * freq * 0.3`；`newCustomerCost = avgOrder * 3` | `<=5%` 流失率健康；`5%-15%` 偏高；`>15%` 严重 | `私域运营专项库/理论层/B_客户生命周期管理.md`、`SOP_沉睡客户激活流程.md`、`话术_复购引导.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `ltv-restaurant` | 客户终身价值智能体（餐饮版） | `avgOrder`、`frequency`、`retentionMonths`、`grossMargin`、`cac` | `monthlyValue = avgOrder * frequency`；`ltv = monthlyValue * retentionMonths * grossMargin / 100`；`ltvCacRatio = ltv / cac`；`paybackVisits = cac / (avgOrder * grossMargin / 100)` | `LTV/CAC >=5` 回报较强；`>=3` 接近可放量；`1-3` 偏低；`<1` 获客成本高于客户价值 | `餐饮行业知识库_综合v3.0.md`、`餐饮行业/营销案例/会员储值方案.md`、`私域运营专项库/案例库/餐饮私域案例_瑞幸.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `ltv-education` | 客户终身价值智能体（教培版） | `hourlyFee`、`monthlyHours`、`retentionMonths`、`extraIncomePct`、`cac` | `monthlyFee = hourlyFee * monthlyHours`；`monthlyExtra = monthlyFee * extraIncomePct / 100`；`ltv = (monthlyFee + monthlyExtra) * retentionMonths`；`ltvCacRatio = ltv / cac` | `LTV/CAC >=5` 回报较强；`>=3` 接近经验参考；`1-3` 偏低；`<1` 获客成本过高 | `教培行业/营销案例/续费活动方案.md`、`教培行业/营销案例/转介绍方案.md`、`教培行业/行业特征与诊断/行业对标数据库.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `promotion-profit` | 促销活动利润智能体 | `normalPrice`、`normalMargin`、`discount`、`promoOrders`、`normalOrders`、`days` | `promoPrice = normalPrice * discount / 100`；`costPerOrder = normalPrice * (1 - normalMargin / 100)`；`promoMargin = normalMargin - (100 - discount)`；`promoMarginAmount = promoPrice - costPerOrder`；`breakEvenIncrease = normalOrders * (100 - discount) / (discount - (100 - normalMargin))` | 折后单件毛利 `<=0` 为单件亏损；日均增量低于保本增量为增量不足；达到保本增量为活动盈利 | `餐饮行业/营销案例/节假日促销方案.md`、`美业行业/营销案例/会员日活动方案.md`、`餐饮行业/营销案例/外卖满减方案.md` | 后端深度结构、前端名称、KB 映射均已恢复 |
| `roi` | 投流 ROI 智能体 | `grossMargin`、`commissionRate`、`marketingRate`、`writeOffRate` | `effectiveMargin = grossMargin - commissionRate - marketingRate`；`breakEvenROI = 1 / (effectiveMargin * writeOffRate)` | 前端建议区间：`<=3` 投流空间充足；`3-6` 合理；`6-10` 压力偏大；`>10` 很难盈利 | 当前保留 `roi` KB 映射，但主执行链路为前端本地计算 | 前端名称已恢复；已确认由 `frontend/src/views/tools/ROICalculator.vue` 本地计算承载 |
| `payback` | 回本周期智能体 | `franchiseFee`、`equipment`、`initialInventory`、`decoration`、`loanInterest`、`otherInvestment`、`labor`、`rent`、`utilities`、`otherOperation`、`monthlyRevenue`、`variableCostRate` | `totalInvestment = 前期投入求和`；`monthlyNetProfit = monthlyRevenue - fixedOperation - monthlyRevenue * variableCostRate`；`paybackMonths = ceil(totalInvestment / monthlyNetProfit)`；`annualROI = monthlyNetProfit * 12 / totalInvestment * 100` | 前端建议区间：`<=6个月` 很短；`<=12个月` 可控；`12-18个月` 中等；`18-24个月` 偏长；`>24个月` 谨慎 | 当前未发现 `payback` 外部 KB 映射，主执行链路为前端本地计算 | 前端名称已恢复；已确认由 `frontend/src/views/tools/PaybackCalculator.vue` 本地计算承载 |

## 5. 待逐一审核清单

以下清单根据当前盘点结果先按模块列出，后续逐一展开为第 3 节模板格式。

### 餐饮计算智能体

| 工具 code | 审核状态 | 备注 |
|---|---|---|
| `food-cost-rate` | 已核对 | 当前无执行项，见历史清单别名/缺失项 |
| `gross-margin` | 已核对 | 历史泛化名称，实际工具为 `gross-margin-restaurant` |
| `gross-margin-restaurant` | 已恢复 | 品类毛利 |
| `profit-rate-restaurant` | 已恢复 | 净利率 |
| `salary-cost-ratio-restaurant` | 已核对 | 员工成本样本已记录 |
| `labor-efficiency-restaurant` | 已恢复 | 人效 |
| `food-waste-rate` | 已恢复 | 食材损耗 |
| `turnover-rate` | 已核对 | 历史泛化名称，实际工具为 `turnover-rate-restaurant` |
| `turnover-rate-restaurant` | 已恢复 | 翻台/周转 |
| `delivery-profit` | 已恢复 | 外卖利润 |
| `delivery-analysis` | 已恢复 | 外卖综合经营 |
| `dish-pricing` | 已恢复 | 菜品定价与结构设计 |
| `dish-profit` | 已核对 | 当前无执行项，见历史清单别名/缺失项 |
| `dish-structure` | 已核对 | 当前由 `dish-pricing`/`dish-contribution` 承载 |

### 教培计算智能体

| 工具 code | 审核状态 | 备注 |
|---|---|---|
| `renewal-rate-education` | 已恢复 | 续费率，见第二批教培记录 |
| `class-consumption-rate-education` | 已恢复 | 课时消耗，见第二批教培记录 |
| `breakeven-students-education` | 已核对 | 当前无执行项，招生保本能力由 `break-even-education` 承载 |
| `gross-margin-education` | 已恢复 | 毛利率，见第二批教培记录 |
| `break-even-education` | 已恢复 | 盈亏平衡，见第二批教培记录 |
| `salary-cost-ratio-education` | 已恢复 | 员工成本，见第二批教培记录 |
| `labor-efficiency-education` | 已恢复 | 人效，见第二批教培记录 |
| `venue-utilization-education` | 已恢复 | 场地利用，见第二批教培记录 |
| `cac-education` | 已恢复 | 获客成本，见第二批教培记录 |
| `payback-education` | 已恢复 | 投资回本，见第二批教培记录 |
| `cashflow-education` | 已恢复 | 现金流，见第二批教培记录 |
| `profit-rate-education` | 已恢复 | 利润率，见第二批教培记录 |
| `return-rate-education` | 已恢复 | 回报率，见第二批教培记录 |
| `class-rate-education` | 已恢复 | 消课率，见第二批教培记录 |

### 美业计算智能体

| 工具 code | 审核状态 | 备注 |
|---|---|---|
| `card-consumption-rate-beauty` | 已恢复 | 耗卡率，见第三批美业记录 |
| `gross-margin-beauty` | 已恢复 | 毛利率，见第三批美业记录 |
| `break-even-beauty` | 已恢复 | 盈亏平衡，见第三批美业记录 |
| `salary-cost-ratio-beauty` | 已恢复 | 员工成本，见第三批美业记录 |
| `labor-efficiency-beauty` | 已恢复 | 人效，见第三批美业记录 |
| `conversion-rate-beauty` | 已恢复 | 拓客转化，见第三批美业记录 |
| `project-profit-beauty` | 已恢复 | 项目利润，见第三批美业记录 |
| `ltv-beauty` | 已恢复 | 客户生命周期价值，见第三批美业记录 |
| `repurchase-rate-beauty` | 已恢复 | 复购率，见第三批美业记录 |
| `payback-beauty` | 已恢复 | 回本周期，见第三批美业记录 |
| `cashflow-beauty` | 已恢复 | 现金流，见第三批美业记录 |
| `profit-rate-beauty` | 已恢复 | 利润率，见第三批美业记录 |
| `return-rate-beauty` | 已恢复 | 回报率，见第三批美业记录 |
| `project-structure-beauty` | 已恢复 | 品项结构，见第三批美业记录 |
| `labor-structure-beauty` | 已恢复 | 人工结构和床位产出，见第三批美业记录 |
| `card-debt-beauty` | 已恢复 | 卡项负债与实收，见第三批美业记录 |
| `funnel-ltv-beauty` | 已恢复 | 拓客漏斗与 LTV，见第三批美业记录 |
| `breakeven-profit-beauty` | 已恢复 | 保本利润，见第三批美业记录 |
| `device-roi-beauty` | 已恢复 | 仪器 ROI，见第三批美业记录 |
| `member-card-design-beauty` | 已恢复 | 会员储值卡设计，见第三批美业记录 |
| `member-card-profit-beauty` | 已核对 | 当前无执行项，能力由 `member-card-design-beauty` 承载 |
| `bed-utilization-beauty` | 已核对 | 当前无执行项，床位产出能力由 `labor-structure-beauty` 承载 |

### ROI、营销推广与通用工具

| 工具 code | 审核状态 | 备注 |
|---|---|---|
| `roi` | 已核对 | 前端名称已恢复为投流 ROI 智能体；已确认由 `ROICalculator.vue` 本地计算承载 |
| `campaign-roi` | 已恢复 | 活动 ROI，见第四批营销推广记录 |
| `return-rate-restaurant` | 已恢复 | 餐饮回报率，已补回深度结构，见第四批营销推广记录 |
| `return-rate-education` | 已恢复 | 教培回报率，见第二批教培记录 |
| `payback` | 已核对 | 前端名称已恢复为回本周期智能体；已确认由 `PaybackCalculator.vue` 本地计算承载 |
| `festival` | 已核对 | 前端通过 `generateWithAI('festival')` 调用 `/api/generate/festival`，后端 `generate.js` 为 RAG 定义；本地与线上主定义一致 |
| `fission` | 已核对 | 前端通过 `generateWithAI('fission')` 调用 `/api/generate/fission`；线上 `backend/src/tools/marketing.js` 为知识库 + AI 主链路 |
| `marketing-plan` | 已核对 | 前端 `TemplateScheme` 组件调用 `/api/generate/marketing-plan`；后端 `generate.js` 为 template 定义 |
| `meituan` | 已核对 | 当前前端 `MeituanDiagnoser.vue` 本地生成结构化结果；后端 `generate.js` 同时保留 template 定义，属于双链路并存 |
| `channel-cac` | 已恢复 | 多渠道获客成本，见第四批营销推广记录 |
| `referral-roi` | 已恢复 | 转介绍效果，见第四批营销推广记录 |
| `conversion-funnel` | 已恢复 | 营销转化漏斗，见第四批营销推广记录 |
| `retention-rate` | 已恢复 | 客户留存，见第四批营销推广记录 |
| `marketing-budget` | 已恢复 | 营销预算分配，见第四批营销推广记录 |
| `churn-rate` | 已恢复 | 客户流失，见第四批营销推广记录 |
| `ltv-restaurant` | 已恢复 | 餐饮客户终身价值，见第四批营销推广记录 |
| `ltv-education` | 已恢复 | 教培客户终身价值，见第四批营销推广记录 |
| `promotion-profit` | 已恢复 | 促销活动利润，见第四批营销推广记录 |

### 小红书专项

| 工具/页面 | 审核状态 | 备注 |
|---|---|---|
| 小红书 17 个页面 | 待审核 | 其中 13 个占位页需补真实能力 |

### 私域专项

| 工具/页面 | 审核状态 | 备注 |
|---|---|---|
| `PrivateAgentHub.vue` | 已恢复 | 本地存在、线上缺失；已接入前端 `/private` 路由 |
| `frontend/src/views/private/*.vue` | 已恢复 | 本地 18 个页面已接入 `/private/*` 子路由，并挂载后端 `/api/private` |

## 6. 更新记录

- 2026-05-13：创建本文档，先记录员工成本三个样本工具，并建立全工具逐一审核模板。
- 2026-05-13：完成首批餐饮工具恢复记录，覆盖 `gross-margin-restaurant`、`turnover-rate-restaurant`、`food-waste-rate`、`labor-efficiency-restaurant`、`delivery-profit`、`delivery-analysis`、`profit-rate-restaurant`、`dish-pricing`，并标注 `food-cost-rate`、`gross-margin`、`turnover-rate`、`dish-profit`、`dish-structure` 的历史别名/缺失状态。
- 2026-05-13：完成第二批教培工具恢复记录，覆盖 13 个教培工具，修正教培 KB 映射从缺失综合文件改为真实存在的分主题知识库文件，并标注 `breakeven-students-education` 由 `break-even-education` 承载。
- 2026-05-13：完成第三批美业工具恢复记录，覆盖 20 个美业工具，新增美业 KB 映射并校验全部知识库路径存在，标注 `member-card-profit-beauty` 与 `bed-utilization-beauty` 的承载工具。
- 2026-05-13：完成第四批营销推广工具恢复记录，整体恢复营销推广深度区段，覆盖 11 个后端执行工具并同步 13 个前端智能体命名，新增/更新营销推广 KB 映射。
- 2026-05-13：完成第五批工具链路核对，确认 `festival`/`fission` 为 RAG 链路，`marketing-plan` 为 template 链路，`roi`/`payback` 为前端本地计算，`meituan` 为前端本地诊断 + 后端 template 并存。
- 2026-05-13：补核 `return-rate-restaurant` 与 `return-rate-education`，确认教培版已恢复，餐饮版补回深度 `benchmarks/actions/riskNotes` 输出结构。
- 2026-05-13：完成私域专项前端恢复接线，补齐 `/private` 与 17 个子页面路由，并在后端挂载 `/api/private` 到 `privateAgents.js`。
- 2026-05-13：批量恢复前端剩余 9 个工具命名从"计算器"到"智能体"，toolCatalog 中 `name:` 字段不再包含"计算器"。
